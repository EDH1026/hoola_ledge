# 게임 장부 (hoola_ledge) — PRD v2.23

- 최초 작성: 2026-08-13 / 최종 개정: 2026-08-16 (v2.23)
- 저장소: https://github.com/EDH1026/hoola_ledge
- 배포: Vercel 프로젝트 `hoola` (도메인: `hoola-three.vercel.app`), 팀 `HYSNESTR`
- 문서의 목적: 현재 구현된 기능/구조를 정리하고 다음 릴리스의 범위를 확정하여 Claude Code 작업의 기준 문서로 사용

### 버전 이력

| 버전 | 내용 |
| --- | --- |
| v1.0 | 최초 구현 — 참가자 풀, 게임 기록(드래그 앤 드롭), 최소 거래 정산, 통계, 공유 비밀번호 인증, Vercel 배포 |
| v2.0 | 관리자 모드, 과거 누적기록, 게임 종목/시간 자동기록/점수/N차전, 게임 소프트 삭제, Win-Lose 용어 통일, 정산 카드 UI + 기부, 상대 전적, 기부 랭킹, 관리자 롤백 (구현 완료) |
| v2.10 | 버그 수정(모바일 드래그, 기부 방향, 관리자 탭 노출, 정산 안전장치) + 게임 구간별 인별 점수 합계 + 이스포츠 스타일 통계/대시보드 확장 (구현 완료) |
| v2.11 | 저장소를 Vercel Blob(JSON 파일)에서 Supabase(Postgres)로 전환 (구현 완료) |
| v2.12 | 게임 기록을 관리자 전용으로 수정(edit)할 수 있는 기능 추가 — §11 (구현 완료) |
| v2.13 | 승무패/승률A·B 표기, 기록실 top3·공동순위 개편, 통계 커스텀 기간, 게임 목록 기본 필터를 오늘로, 관리자 완전삭제, 변제(자유 정산) — §13 (구현 완료, 스키마 변경 없음) |
| v2.14 | 게임 기록 수정·삭제를 일반 이용자에게도 개방(날짜·시간 제외), 기록 후 2시간이 지나면 수정·삭제·정산취소 불가 — §15 (관리자는 시간 제한 예외) |
| v2.15 | 티어 시스템 전면 재설계 — 승률A 컷 방식을 폐기하고 기대 대비 성과(승 지수·패 지수) 기반 분기별·종목별 티어로 교체, 개인별 성향은 뱃지 대신 최근 90일 롤링 2축 산점도(성향 맵)로 표현 — §16 (구현 완료, 스키마 변경 없음) |
| v2.16 | **하루의 재정의(06:00~30:00, 24시간 표기 통일)** + 대시보드·새 게임 기록·정산·통계 화면 전면 개편 + **통산기록 탭 신설** — §18 (구현 완료, 스키마 변경 없음) |
| v2.17 | **v2.16 영업일 규칙의 기존 데이터 소급 적용(백필)** — 앞으로 기록될 게임에만 적용되던 06:00 경계를 이미 저장된 기록에도 적용해, 자정을 넘겨 기록된 과거 게임이 실제 게임 밤으로 되돌아가게 한다 + 관리자 수정 화면의 영업일 정합성 경고 — §20 (구현 완료, 스키마 변경 없음) |
| v2.18 | **영업일과 표시 시각의 분리** — 그룹핑·필터는 영업일(06:00 경계)을 쓰되, 개별 기록의 타임스탬프는 실제 달력 벽시계로 표시한다. 8/15 01:00 게임은 8/14로 묶이되 화면에는 "8/15 01:00"으로 나와야 한다 — §22 (구현 완료) |
| v2.19 | **UX/UI 전면 정비** — 디자인 토큰·공용 컴포넌트 도입, 색 대비 하한(AA) 강제, 타입 스케일 4단계, Pretendard, 게임 연속 기록 모드, `/games` 상단 압축, 44px 터치 타깃, 필터 URL 동기화, 차트·히트맵 가독성 — §24 (스키마 변경 없음) |
| v2.20 | **라이브 게임밤 보드** — 게임 밤이 진행 중일 때만 나타나는 실시간 현황 화면. 대시보드 최상단 카드 + 테이블 위에 올려두는 전체화면 모드(`/tonight`) — §26. **구현 완료 (2026-08-16)**, 스키마 변경 없음. **v2.21에서 "최근 경기일" 카드로 개편되며 `/tonight`은 폐기** |
| v2.21 | **대시보드 재구성 + 차트·기록 정비** — v2.20 게임밤 보드를 "최근 경기일" 카드로 개편(진행 중이면 경과, 끝났으면 최근 경기일 결과)하고 "최근 게임" 카드를 접이식으로 흡수, 자동 갱신을 수동 갱신으로, 카드 순서 재배치, 누적 순점수 추이에 게임별·분기별 추가 + 범례를 선 끝 이름 라벨로 교체, 명예의 전당의 순득점 기록을 하루 득실차로 교체 — §28. **구현 완료 (2026-08-16)**, 스키마 변경 없음 |
| v2.22 | **통계 차트 여백 정리 + 누적 추이 시작점 수정** — 세 차트의 좌우 여백(기본 YAxis 폭 60px 등)을 줄여 플롯이 카드를 꽉 채우게 하고, 누적 순점수 추이가 참가자의 **첫 승/패**가 아니라 **첫 참석**부터 그려지도록 수정 — §30. **구현 완료 (2026-08-16)**, 스키마 변경 없음 |
| v2.23 | **탄소배출권 세계관 리브랜딩 + 운영원칙 신설 + UX 마이너** — 채권·채무·정산·금액 등 금전/도박 연상 표현을 배출권 이전 용어로 교체, 1점 = 10kg CO₂e 규칙과 감축 행동 목록을 담은 `/principles` 탭 신설, 헤더에서 관리자·로그아웃을 푸터로 분리, 새 게임 기록 버튼 단일화, 승률 각주, 천적/밥을 마진 기준으로, 티어 지표 설명 추가 — §32 (이번 작업 범위, 스키마 변경 없음) |

## 1. 제품 개요

매번 참석자가 달라지는 소규모 모임 게임(보드게임 등)에서, 각 게임의 결과를 기록하고 그로부터 발생하는 **탄소배출권 이전 관계**를 자동으로 정리해주는 웹 앱이다(v2.23 세계관 — §32). 참가자 풀은 고정되어 있지만 게임마다 실제 참석자는 그중 일부로 달라진다. 접근은 팀 전체가 공유하는 비밀번호로 제한되며, 장부 정합성에 영향을 주는 기능은 별도의 관리자 비밀번호로 한 번 더 보호된다.

### 핵심 규칙

- 매 게임마다 전체 등수가 아니라 **Win(1위)과 Lose(최하위)만** 기록한다.
- **Lose가 Win에게 해당 게임의 점수를 지급**한다. 점수는 기본 1점이며 게임별로 다르게(예: 2점) 지정할 수 있다. **1점은 10kg CO₂e 배출권에 해당하며**, 보유량이 부족하면 실제 감축 행동을 인증해 확보한다(§32.4).
- 게임 결과 입력 UI는 Lose 참가자 카드를 Win 참가자 카드 위로 **드래그 앤 드롭**(또는 탭-투-탭)하는 방식이며, 확인 후 결과가 기록되고 배출권 장부가 갱신된다.
- 새 게임을 등록할 때 참석자 선택의 기본값은 **직전 게임의 참석자와 동일**하며, 필요 시 체크박스로 변경할 수 있다.
- 게임 날짜·시각은 사용자가 입력하지 않고 **기록하는 순간의 서버 시각(Asia/Seoul)이 그대로 저장**된다. 이 값은 관리자만 예외적으로 고칠 수 있다(§11).
- **하루는 06:00~30:00(다음 날 06:00)을 한 단위로 본다(v2.16, §18.1).** 게임 밤이 자정을 넘겨 이어지는 걸 반영한 것으로, 08/15 04:00에 기록된 게임은 08/14의 기록이고 08/15 07:00에 기록된 게임은 08/15의 기록이다. 이 정의는 게임 날짜, "오늘" 필터·요약, N차전 순서, 분기 경계 등 **앱 전체에 공통으로 적용**된다. 모든 시각 표기는 오전/오후가 아니라 **24시간제**를 쓴다.
- **단, 영업일은 묶는 기준일 뿐 표시하는 값이 아니다(v2.18, §22).** 개별 기록의 타임스탬프는 실제로 벽시계가 가리켰던 시각 그대로 보여준다 — 08/15 01:00에 친 게임은 08/14의 게임 밤으로 집계되지만, 화면에는 "08/15 01:00"으로 표시된다. 날(day) 단위 라벨(경기일 요약, 명예의 전당, 기간 필터)은 영업일을 쓰고, 특정 기록 하나의 시각은 달력 벽시계를 쓴다.
- 게임이 쌓이면 참가자 간에 다대다 배출권 이전 관계가 형성되는데, 이를 **최소 이전 횟수**로 자동 간소화한다.
- 배출권 이전에는 실제로 넘긴 기록인 **"배출권 이전"**(`payment`)과, 임의의 두 사람 사이에서 자유롭게 넘겨주는 **"기부"**(`donation`) 두 종류가 있다. 기부하는 사람의 보유량은 감소하고 받는 사람의 보유량은 증가한다.
- **화면에 쓰는 모든 용어는 §32.2 대응표를 따른다.** 내부 식별자(`Settlement`·`payment`·`/settlements` 등)와 DB 스키마는 그대로다 — 바뀌는 것은 표기뿐이다.
- 기간별(일/주/월/년)·종목별 승/패 횟수와 순위, 상대 전적, 티어, 명예의 전당 등을 여러 각도로 볼 수 있는 통계(`/stats`)·통산기록(`/records`, v2.16 신규)·대시보드를 제공한다.
- **티어는 분기(3개월)마다 리셋되며, 종목별로 따로 매겨진다.** 단순 승률이 아니라 참석 인원수로부터 계산한 기대 승·패 대비 성과를 기준으로 한다(§16).
- 참가자 풀 관리, 과거 누적기록 입력, 데이터 롤백은 관리자 모드에서만 접근 가능하며 일반 화면에는 노출되지 않는다.
- **화면은 폰(390px)을 1순위로 설계한다(v2.19, §24).** 읽어야 하는 텍스트는 대비 4.5:1 이상, 누를 수 있는 요소는 히트 영역 44×44px 이상을 만족해야 한다. 색·타입·간격은 컴포넌트에 직접 쓰지 않고 `globals.css`의 시맨틱 토큰과 공용 컴포넌트(`src/components/ui/`)를 경유한다.
- 게임 기록은 **누구나 기록 후 2시간 이내라면** 날짜·시간을 제외한 내용을 수정하거나 삭제할 수 있다. 2시간이 지나면 관리자만 손댈 수 있다(§15). 이 앱은 개인별 로그인이 없고 팀 전체가 공유 비밀번호 하나로 접속하므로, "기록한 본인만" 같은 개인 단위 권한 분리는 두지 않는다.

## 2. 데이터 모델 (애플리케이션 타입)

파일: `src/lib/types.ts`

```ts
interface Participant {
  id: string;
  name: string;
  active: boolean;   // 비활성 참가자는 새 게임 선택 목록에서 제외되나 과거 기록은 유지
  createdAt: string;
}

type GameType = "hoola" | "citadels" | "6nimmt"; // 훌라 / 시타델 / 젝스님트

interface GameResult {
  id: string;
  date: string;           // yyyy-MM-dd — v2.16부터 "영업일"(§18.1, 06:00~30:00) 기준. 기록 시점에 서버가 설정 (일반 이용자 수정 불가, 관리자만 예외)
  time?: string;          // "HH:mm", Asia/Seoul. 이 필드 이전 레코드에는 없음
  gameType?: GameType;    // 이 필드 이전 레코드에는 없음
  points?: number;        // Win이 Lose에게서 가져가는 점수. 없으면 1로 간주
  active?: boolean;       // false = 소프트 삭제(잔액/통계/목록에서 제외). 없으면 true로 간주
  attendeeIds: string[];  // 티어 계산의 기대치(1/n)를 만드는 핵심 필드 — §16
  winnerId: string;       // Win (내부 필드명 유지)
  loserId: string;        // Lose (내부 필드명 유지)
  note?: string;
  createdAt: string;      // ISO datetime, 정렬·관리자 롤백·수정 유예시간(2시간) 판단 기준
}

type SettlementType = "payment" | "donation" | "waiver";  // waiver는 레거시 판독용

interface Settlement {
  id: string;
  type?: SettlementType; // 없으면 "payment"로 간주
  fromId: string;        // 주는 사람 (기부는 잔액 -amount, 실제 정산은 +amount)
  toId: string;          // 받는 사람 (기부는 잔액 +amount, 실제 정산은 -amount)
  amount: number;
  date: string;
  note?: string;
  createdAt: string;     // 취소 유예시간(2시간) 판단 기준
}

interface LedgerAdjustment {   // 과거 누적기록 — 승패 없이 채무자→채권자, 금액만 존재
  id: string;
  fromId: string;  // 채무자
  toId: string;    // 채권자
  amount: number;
  note?: string;
  date: string;
  createdAt: string;
}

interface DB {
  participants: Participant[];
  games: GameResult[];
  settlements: Settlement[];
  adjustments: LedgerAdjustment[];
}
```

옵셔널 필드들은 마이그레이션 없이 기존 레코드를 읽을 수 있도록 설계되어 있다 — 없으면 위 주석의 기본값으로 처리된다. **v2.15의 티어 재설계도 스키마 변경 없이 기존 필드만으로 계산된다**(§16).

## 3. 도메인 로직

### 3.1 잔액 계산 및 최소 거래 간소화 (`src/lib/settle.ts`)

`computeNetBalances(games, settlements, adjustments)`는 참가자별 순잔액을 계산한다. 양수 = 받을 것이 있음(채권자), 음수 = 줄 것이 있음(채무자). 소프트 삭제된 게임(`active === false`)은 내부에서 자동으로 제외된다.

부호 규칙:

| 이벤트 | fromId(주는 쪽) | toId(받는 쪽) | 비고 |
| --- | --- | --- | --- |
| 게임 | Lose `−points` | Win `+points` | 점수 없으면 1로 간주 |
| 실제 정산 (`payment`) | `+amount` | `−amount` | 빚을 갚아 양쪽 잔액이 0에 수렴 |
| 기부 (`donation`, 레거시 `waiver` 포함) | `−amount` | `+amount` | 점수를 그냥 넘겨주는 것 — 게임과 같은 방향 |
| 과거 누적기록 | 채무자 `−amount` | 채권자 `+amount` | 게임과 같은 방향 |

`simplifyDebts(balances)`는 0이 아닌 잔액이 12명(`EXACT_SOLVE_THRESHOLD`) 이하면 완전탐색(branch-and-bound, LeetCode 870 "Optimal Account Balancing"과 동일한 문제)으로 **진짜 최소** 거래 수를 찾는다. 각 거래는 항상 두 사람의 남은 잔액 중 작은 쪽으로 "깔끔하게" 정산되도록 제한해, 자기 잔액보다 더 많이 지불하는 것처럼 보이는 수학적으로만 유효한 해는 배제한다. 12명 초과 시 그리디로 대체된다. `scripts/verify-settle.ts`로 검증되어 있다.

### 3.2 통계 (`src/lib/stats.ts`)

- `computeParticipantStats`: 참가자별 승/패/참석 횟수, 승률A/B, 순포인트.
- `computeHeadToHead` / `computeHeadToHeadMatrix` / `computeNemesisAndVictim`: 상대 전적, 천적/밥.
- `groupGamesByPeriod`, `filterByDatePreset`, `filterGamesByType`: 기간·종목 필터링/그룹핑. v2.16에서 `RangePreset`에 `"today"`가 추가되고, 문자열 기반 영업일 컷오프로 다시 작성되었다(§18.1).
- `computeHotColdPlayers`, `computeRecentForm`, `computeCurrentStreaks`, `computeRecords`.
- **`computeRecentGameDaysSummary` (v2.16 신규, §18.2)**: 기존 `computeTodaySummary`/`TodaySummary`를 대체 — 게임이 있었던 최근 3개 영업일(없는 날은 건너뜀)의 판수와 그날의 득실(순점수) 기준 최다 승자(공동 1위 모두 포함)·득실차.
- **`computeCumulativeNetPointsTrend` (v2.16 신규, §18.5)**: 참가자별 누적 순점수 추이를 `PeriodGrouping` 단위(일/주/월/연도)로 버킷팅. 기존에는 게임 1건당 1점으로 취급했으나(±1), 판돈(`points`)을 반영하도록 함께 수정했다 — 앱의 다른 모든 "순점수"와 일관되게.
- **`computeQuarterlyTiers` (v2.15 신규)**: 분기별·종목별 티어. §16 참조. 기존 `computeTier(winRate, decisiveGames)`를 대체한다.
- v2.16에서 `computeGameTypeStats`/`GameTypeParticipantStat`(종목별 성적)이 제거되었다 — 순위표에 섹션별 종목 필터가 생기며 중복이 되었다(§18.5).

### 3.3 시간/회차 (`src/lib/time.ts`, `src/lib/games.ts`)

- 게임 기록 시각은 Asia/Seoul 기준 벽시계로 서버에서 생성한다.
- "N차전"은 저장하지 않고 조회 시 계산한다.
- `activeGames(games)`가 소프트 삭제 필터링을 담당한다.
- v2.14에서 `EDIT_WINDOW_MS`(2시간)와 `isWithinEditWindow(createdAt)`가 추가된다.
- v2.15에서 `quarterKeyOf(date)` / `currentQuarterKey()` / `formatQuarterKey()`가 추가된다 (§16.5).
- **v2.17 — 소급 적용(§20)**: `seoulWallClockFromIso(iso)`(UTC ISO 순간 → Asia/Seoul `{date, time}`)와 `businessDateFromIso(iso)`가 `time.ts`에 추가된다. `formatInSeoul`/`nowInSeoul`은 이 함수를 재사용하도록 정리된다(동작은 그대로). 판정 로직 자체는 `src/lib/backfill.ts`의 순수 함수 `planBusinessDayBackfill(row)`에 있다 — `time.ts`가 아니라 별도 모듈로 둔 이유는, 이게 일반적인 시간 유틸이 아니라 "저장된 행을 어떻게 소급 보정할지"라는 이 마이그레이션 전용 도메인 로직이기 때문이다. `scripts/backfill-business-day.ts`(`npm run backfill:business-day` = 드라이런, `--apply`는 직접 실행)가 이 함수와 Supabase I/O를 연결한다.
- **v2.16 — 하루 재정의(§18.1)**: `BUSINESS_DAY_START_HOUR`(6), `addDaysToIsoDate(date, days)`(문자열 기반 날짜 산술), `businessDateFromWallClock(date, time)`, `nowInSeoulBusinessDay()`(영업일 기준 "지금"), `minutesSinceBusinessDayStart(time)`(자정을 넘는 같은 영업일 내 시간 정렬용)가 `time.ts`에 추가된다. `todayInSeoul()`은 이름은 그대로지만 이제 **영업일** 기준 오늘을 반환하도록 의미가 바뀌었다 — `nowInSeoul()` 자체는 관리자 롤백 화면의 커트오프처럼 실제 달력 순간이 필요한 곳을 위해 그대로 남아있다. `games.ts`의 `withinDayKey`는 `time` 문자열을 그대로 비교하는 대신 `minutesSinceBusinessDayStart`로 변환해 비교하도록 바뀌어, 자정을 넘긴 게임이 같은 영업일의 더 이른 게임보다 항상 뒤에 정렬된다. `scripts/verify-business-day.ts`(`npm run verify:business-day`)로 검증되어 있다.

## 4. 화면/기능 명세

| 라우트 | 접근 | 설명 |
| --- | --- | --- |
| `/login` | 공개 | 공유 비밀번호 로그인 |
| `/admin-login` | 로그인 | 관리자 비밀번호 입력 → 관리자 세션 발급 |
| `/` (대시보드) | 로그인 | 리더보드, 정산 미리보기, 최근 게임, 핫/콜드 플레이어, 최근 폼, 스트릭, **최근 경기일 요약(§18.2)**, **이번 분기 티어 — 통합+종목별 4블록(§18.3)** |
| `/games/new` | 로그인 | 종목 선택 → 참석자 선택 → 드래그(또는 탭-투-탭)로 Win/Lose 지정 → 점수·메모 확인 후 기록 → **저장 즉시 `/games`로 자동 이동해 기록을 바로 확인(§18.4)** |
| `/games` | 로그인 | 게임 목록, 기간 필터(기본값 오늘 = 영업일), 기간 내 전체·인별 점수 합계, 수정·삭제(2시간 이내, §15), 관리자는 무제한 + 완전삭제 |
| `/settlements` | 로그인 | **참가자별 순 잔액 → 정리된 거래 카드(2단계 확인) → 변제 → 기부 기록 → 기부 랭킹 → 정산·조정 이력 순(§18.6)**, 2시간 이내 취소(§15) |
| `/stats` | 로그인 | 공통 기간 필터(+**오늘**, §18.7) + 섹션별 독립 종목 필터. 순서: 순위표 → (클릭 시) 상대 전적 → 상대 전적 매트릭스 → 천적/밥 → 참가자별 승/패 → 참가자별 누적 순점수 추이(섹션별 추이 단위) → 기간별 게임 수 추이(섹션별 추이 단위) (§18.7) |
| `/records` (통산기록, v2.16 신규) | 로그인 | **분기 티어(§16.7) → 성향 맵(§16.8) → 명예의 전당(구 기록실 + 최고/최저 순득점 + 하루 최다 게임수, §18.8)** — `/stats`에서 이관 |
| `/participants` | **관리자** | 참가자 추가/이름 변경/활성-비활성 |
| `/adjustments` | **관리자** | 과거 누적기록 입력/수정/삭제 |
| `/rollback` | **관리자** | 특정 시각 이후 기록 하드 삭제 |

관리자 전용 라우트는 비관리자 상태에서 상단 네비게이션에 노출되지 않는다. 인증: 모든 `(app)` 라우트는 `src/proxy.ts`에서 세션 쿠키(`gl_session`)를 검사하고, 관리자 전용 라우트는 관리자 쿠키를 추가로 검사한다.

## 5. 아키텍처

### 5.1 스택

- Next.js 16.3.0 (App Router, Turbopack), React 19.2.8
- `proxy.ts` (Next 16의 middleware 대체 파일 컨벤션, Node.js 런타임 기본)
- React Server Components + Server Actions
- `@dnd-kit/core` — 드래그 앤 드롭 / `recharts` — 차트 / `date-fns` / `uuid` / `zod`
- Tailwind CSS v4
- **저장소: Supabase(Postgres)** — v2.11에서 Vercel Blob(JSON 파일)으로부터 전환 완료 (`@supabase/supabase-js`)

### 5.2 저장소 계층 (Supabase/Postgres)

로컬 개발과 프로덕션 모두 동일한 Supabase 프로젝트(`BackRoom`)를 사용한다. 로컬 전용 `data/db.json` 파일 경로는 제거되었다.

**테이블 구조** (`supabase/schema.sql`): `participants`, `games`, `settlements`, `adjustments` — §2의 애플리케이션 타입과 1:1로 대응한다(camelCase↔snake_case). `games.game_type`/`games.time`은 레거시 레코드를 위해 nullable, `games.points`/`games.active`는 NOT NULL. `settlements.type`은 Postgres enum.

**접근 제어**: 모든 테이블에 RLS를 켜두고 정책은 만들지 않는다(=사실상 전체 잠금). 앱은 서버 코드(`src/lib/supabase.ts`)에서만 `SUPABASE_SERVICE_ROLE_KEY`로 접근하며, 이 키는 RLS를 완전히 우회한다 — 브라우저에는 노출되지 않는다. 접근 제어는 앱 레이어의 `SITE_PASSWORD`/`ADMIN_PASSWORD`가 담당한다.

**데이터 접근 계층**: `src/lib/storage.ts`가 테이블별 타겟팅된 쿼리 함수를 제공한다 (`listParticipants`/`insertParticipant`/`updateParticipantName`/`updateParticipantActive`, `listGames`/`insertGame`/`softDeleteGame`/`updateGameRow`/`deleteGameRow`, `listSettlements`/`insertSettlement`/`deleteSettlementRow`, `listAdjustments`/`insertAdjustment`/`updateAdjustmentRow`/`deleteAdjustmentRow`, `getFullDB()`). 각 함수는 Postgres 행을 애플리케이션 타입으로 매핑하며 `timestamptz`/`time` 포맷 차이를 정규화한다.

관리자 롤백만 예외적으로 `rollback_after(cutoff)` RPC로 묶여 하나의 트랜잭션으로 실행된다.

### 5.3 페이지 렌더링

DB를 읽는 모든 `(app)/` 페이지에 `export const dynamic = "force-dynamic"`을 명시해 정적 프리렌더링을 막고 있다.

## 6. 배포 환경 및 트러블슈팅 기록

### 6.1 필수 환경변수

| 변수 | 설명 |
| --- | --- |
| `SITE_PASSWORD` | 앱 접속용 공유 비밀번호 |
| `ADMIN_PASSWORD` | 관리자 모드 비밀번호 |
| `SUPABASE_URL` | Supabase 프로젝트 URL |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase 서비스 롤 키(비밀값). 절대 커밋 금지 |

`BLOB_READ_WRITE_TOKEN`은 더 이상 앱 코드에서 쓰이지 않는다. 환경변수를 추가/변경한 뒤에는 반드시 **Redeploy**해야 반영된다. v2.14·v2.15는 새 환경변수가 필요 없다.

### 6.2 겪었던 문제와 해결

- **Private Blob 스토어 읽기 실패**: `access: "public"` + raw `fetch()`가 Private 모드에서 실패 → `get(pathname, { access: "private" })`로 교체.
- **사이트 전체 404 (`NOT_FOUND`, Runtime Logs에 요청 자체가 안 찍힘)**: Vercel 프로젝트 내부 라우팅 매니페스트 손상 추정. **같은 GitHub 저장소를 가리키는 새 Vercel 프로젝트를 만들어 import**해서 해결.
- **재배포 누락으로 인한 ENOENT**: 환경변수가 실행 중 배포에 반영되지 않아 로컬 파일 쓰기를 시도하다 실패. Redeploy로 해결.

## 7. 알려진 제약사항

- **모든 페이지가 `force-dynamic`이라 캐싱이 없다.** 대시보드/통계처럼 자주 안 바뀌는 화면도 매번 새로 렌더링 + 쿼리한다.
- **통계·정산·대시보드 화면은 필요한 테이블 전체를 가져와 애플리케이션 메모리에서 집계**한다. 데이터가 커지면 SQL 집계(뷰·RPC)로 옮길 필요가 있을 수 있다. v2.15의 티어 계산도 "전체 게임을 처음부터 분기 단위로 접는" 방식이라 이 항목에 해당한다(§16.5). **v2.16부터 대시보드는 이 fold를 통합+종목별 4번(§18.3) 반복한다** — 게임 수가 늘어나면 이 부분이 먼저 무거워질 후보다.
- **드래그 컴포넌트가 마운트 후에만 DndContext를 렌더링**한다 — 초기 로드 시 약간의 레이아웃 시프트.
- **개인별 로그인이 없다.** 팀 전체가 `SITE_PASSWORD` 하나를 공유하므로 "누가 수정했는지" 같은 개인 단위 이력은 구현 불가능하다.
- ~~**v2.16 하루 재정의는 마이그레이션 없이 적용됐다.**~~ → **v2.17에서 해소.** v2.16은 새로 기록되는 게임에만 영업일 규칙을 적용했고, 이미 저장된 기록의 `date`는 달력 날짜 그대로 남겨뒀다. 그 결과 "8/14 저녁에 시작해 8/15 01:00까지 이어진 게임 밤"이 화면에서 이틀로 쪼개져 보이는 문제가 실제로 발생했다 — §20이 이를 백필로 바로잡는다.

## 8. v2.0 요구사항 (구현 완료)

관리자 모드(`ADMIN_PASSWORD` + 별도 쿠키), 과거 누적기록 입력, 게임 종목·자동 시각 기록·점수·N차전, 게임 소프트 삭제, "1등/꼴찌"→"Win/Lose" 용어 통일, 정산 카드 UI + 탕감→기부 확장, 게임 목록 기간 필터, 참가자별 상대 전적, 기부 랭킹, 관리자 데이터 롤백.

## 9. v2.10 요구사항 (구현 완료)

**버그 수정 4건**: ① 모바일 드래그 앤 드롭 미작동(`touch-action`, `DragOverlay`, 탭-투-탭 추가). ② 기부의 잔액 반영 방향이 반대였던 것. ③ 관리자 전용 탭이 비관리자에게 노출되던 것. ④ 정산이 한 번의 클릭으로 크게 실행될 수 있던 것(2단계 확인, 큰 금액 경고, 즉시 취소 동선 — v2.14에서 2시간 유예로 확장).

**기능 추가**: 게임 목록 구간 필터 내 인별 점수 합계, 대시보드 핫/콜드 플레이어·최근 폼·스트릭·오늘의 요약, 통계 화면 티어 뱃지·상대 전적 매트릭스·천적/밥·종목별 성적·기록실.

## 10. v2.11 요구사항 (구현 완료)

저장소를 Vercel Blob에서 **Supabase(Postgres)**로 전환했다. 4개 테이블 스키마(`supabase/schema.sql`), RLS + 서비스 롤 키 접근, `mutateDB` 폐기 후 타겟팅된 쿼리로 재작성, 관리자 롤백을 위한 `rollback_after` RPC 트랜잭션, 기존 데이터 마이그레이션(`scripts/migrate-to-supabase.ts`)까지 완료.

## 11. v2.12 요구사항 (게임 기록 관리자 수정, 구현 완료)

게임 목록(`/games`)에서 관리자가 기존 게임의 종목·참석자·Win/Lose·점수·메모·**날짜·시각**·활성 상태를 고칠 수 있다. 삭제 후 재입력하면 기록 시각이 재입력 시점으로 다시 자동 저장되는 문제를 피하기 위한 기능이다. `updateGame` 액션이 내부에서 관리자 세션을 직접 검증하고, 저장 전 변경 전/후 diff 확인 단계를 거친다. 소프트 삭제된 게임도 관리자에게는 목록에 노출되어 열람·수정·재활성화가 가능하다.

> v2.14에서 이 기능은 "관리자는 무제한, 일반 이용자는 날짜·시간 제외 + 2시간 이내"로 확장된다(§15).

## 13. v2.13 요구사항 (마이너 업데이트 모음, 구현 완료)

### 13.1 승/무/패 표기
3명 이상 게임에서는 Win도 Lose도 아닌 참석("무")이 존재한다. 무는 저장하지 않고 `참여 − 승 − 패`로 계산한다.

### 13.2 승률A / 승률B
승률A = 승/(승+패), 승률B = 승/참여. 두 값을 나란히 표기한다.

### 13.3 종목별 성적: 승률A·B 기준 정렬, "최강" 표기 제거

### 13.4 기록실 개편
단일 게임 최고 점수 삭제, 최장 연승/연패에 기간 표기 추가, top 3 + 공동 순위(dense ranking) 지원.

### 13.5 통계 조회 기간 직접 입력
`RangePreset`에 `"custom"` 추가. `yyyy-MM-dd` **문자열 직접 비교**로 구현했다 — `new Date()` 파싱을 거치면 UTC 자정으로 해석되어 하루가 밀리기 때문. **v2.15의 분기 판정도 같은 이유로 문자열 슬라이스만 쓴다.**

### 13.6 게임 목록 기본 필터 = 오늘, 관리자 완전삭제
`/games` 필터 기본값을 오늘(Asia/Seoul)로. 관리자는 소프트 삭제 외에 완전삭제(하드 삭제)를 인라인 2단계 확인 후 쓸 수 있다.

### 13.7 변제 (자유 정산)
당초 `proxy_payment`(대리 변제) 타입을 만들려 했으나, 대리인을 `fromId`로 잡으면 기존 "실제 정산"과 부호 규칙이 완전히 동일해 원 채무자는 잔액 계산에 관여하지 않는다는 것을 확인했다. 그래서 별도 타입 없이 "실제 정산"을 자유롭게 기록하는 폼(`RepaymentForm`)으로 단순화했고, **스키마 변경이 불필요해졌다**. `normalizeSettlementType()`이 레거시 `proxy_payment`를 `payment`로 정규화한다.

---

## 15. v2.14 요구사항 (게임 기록 일반 사용자 수정/삭제 + 2시간 유예시간)

### 15.1 배경

게임 기록 수정(§11)이 관리자 전용이라 사소한 오기록(참석자 빠짐, 점수 오류, 종목 오선택)을 고치려고 매번 관리자에게 요청해야 한다. 날짜·시간은 서버가 기록한 실제 시각을 신뢰해야 하므로 일반 이용자에게는 자동기록·수정불가를 유지한다. "오기록 정정" 목적을 벗어나 오래된 기록을 고치는 걸 막기 위해 기록 후 2시간 유예시간을 둔다. 정산/기부의 취소도 같은 취지로 제한한다.

### 15.2 요구사항

- **게임 기록**: 일반 이용자도 종목·참석자·Win/Lose·점수·메모를 수정하거나 (소프트) 삭제할 수 있다.
  - **날짜·시간은 일반 이용자에게 수정 불가**(관리자만 예외, §11 유지).
  - **기록 후 2시간이 지나면 일반 이용자는 수정도 삭제도 불가.** 기준은 `createdAt`. 서버 액션이 직접 강제해야 한다 — 버튼 숨김은 UX일 뿐.
  - **관리자는 시간 제한과 무관하게 무제한**(날짜·시간 포함 수정, 완전삭제).
  - 개인별 로그인이 없으므로 "본인이 참여한 게임만" 같은 분리는 불가능하다. 2시간 이내면 로그인한 누구나 수정·삭제할 수 있다.
- **정산/기부**: "방금 기록함 · 취소" 되돌리기의 유효 기간을 2시간으로 명시하고, `deleteSettlement`에도 `createdAt` 기준 2시간 제한을 강제한다(현재는 아무 제한이 없다). 관리자는 예외.
- 시간 제한에 걸린 경우 버튼을 조용히 숨기지 말고 짧게 이유를 안내한다.

### 15.3 구현 방향

- `src/lib/admin.ts`에 `isAdminSession(): Promise<boolean>`(throw하지 않는 버전) 추가.
- `src/lib/time.ts`에 `EDIT_WINDOW_MS`(2시간)와 `isWithinEditWindow(createdAt)` 추가 — `"server-only"` 없는 순수 함수라 클라이언트에서도 재사용.
- `src/lib/storage.ts`에 `getGameById`/`getSettlementById` 추가(유예시간 판단용 `createdAt` 조회).
- `updateGame`은 비관리자 호출을 허용하되, **비관리자 입력의 날짜·시간·활성 상태는 신뢰하지 않고 항상 기존 값을 유지**한다.
- `recordSettlement` 반환값에 `createdAt` 추가.

---

## 16. v2.15 요구사항 (티어 시스템 재설계, 이번 작업 범위)

### 16.1 현행 방식과 그 문제점

현재는 `src/lib/stats.ts`의 `computeTier(winRate, decisiveGames)`가 **승률A(승/(승+패))** 하나만 보고 절대 컷으로 티어를 매긴다 — 브론즈 <35%, 실버 <45%, 골드 <55%, 플래티넘 <65%, 다이아 <75%, 챌린저 ≥75%, 3경기 미만은 랭크 미배정. 이 방식에는 구조적인 문제가 다섯 가지 있다.

1. **참석 인원수를 무시한다.** 4인전에서 Win할 확률은 25%, 5인전은 20%다. 같은 1승인데 난이도가 다른데도 승률A는 똑같이 취급한다. 사용자가 지적한 "보통 4~5명에서 하므로 기대 승률은 20~25%"가 바로 이 지점이다.
2. **표본의 대부분을 버린다.** 5인전 평균 실력자의 실제 분포는 승 20% · 패 20% · **무 60%**다. 승률A는 이 60%를 통째로 버리고 남은 40%만 본다. 즉 기록된 게임의 60%가 티어 계산에 아무 영향을 못 준다.
3. **판돈(`points`)을 무시한다.** 2점짜리 판을 이긴 것과 1점짜리를 이긴 것이 동일하게 1승으로 계산된다.
4. **표본 크기 보정이 없다.** 3경기에서 2승 1패면 승률A 67% → 플래티넘. 한 번의 운으로 최상위 티어가 나온다.
5. **컷이 기대치와 무관한 절대값이다.** 5인전에서 승률A 50%는 정확히 "기대치대로"인데 골드 구간 한복판이고, 승률A 75%(챌린저)는 사실상 도달 불가능한 수치다.

부수적으로, 이 티어는 통계 화면 순위표의 컬럼 하나로만 쓰이는데 화면의 기간·종목 필터에 따라 값이 바뀐다. 즉 "티어"라기보다 그때그때 필터의 승률 표시에 가깝고, 분기·시즌 개념이 없다.

### 16.2 새 티어의 기본 지표 — 승 지수와 패 지수

한 게임 g에 대해 참석 인원 `n_g`, 판돈 `p_g`라 하고, **기대치 `e_g = 1/n_g`** 를 정의한다. 실력이 모두 같다면 Win할 확률도 `1/n_g`, Lose할 확률도 `1/n_g`이다 — 이것이 사용자가 말한 "기대 승률 20~25%, 기대 패배율 20~25%"를 게임마다 정확하게 계산한 값이다(4인전이면 0.25, 5인전이면 0.20, 6인전이면 0.167).

분기 안에서 그 참가자가 뛴 해당 종목 게임 전체에 대해 다음을 누적한다.

| 기호 | 정의 | 의미 |
| --- | --- | --- |
| `E_w` | `Σ e_g` | **기대 승수 = 기대 패수.** 유효 표본 크기 역할도 한다 |
| `E_p` | `Σ p_g · e_g` | 기대 획득 점수 = 기대 상실 점수 |
| `W_p` | 이긴 게임의 `p_g` 합 | 실제 획득 점수 |
| `L_p` | 진 게임의 `p_g` 합 | 실제 상실 점수 |

여기서 두 축이 나온다.

- **승 지수 `WI = W_p / E_p`** — 1.00이면 기대만큼 이겼다는 뜻. 1.50이면 기대보다 50% 더 이겼다.
- **패 지수 `LI = L_p / E_p`** — 1.00이면 기대만큼 졌다. **낮을수록 좋다.**

그리고 티어의 핵심 수치는 두 축의 차이다.

```
PERF = WI − LI = (W_p − L_p) / E_p        (0 = 정확히 기대치)
```

`W_p − L_p`는 이 앱이 이미 계산하는 **순점수**와 같은 값이다. 즉 PERF는 "내 순점수를, 참석 인원수와 판돈으로부터 계산된 기대 변동폭으로 나눈 값"이다. 이 하나의 수식이 앞의 문제 1·2·3을 동시에 해결한다 — 인원수는 `e_g`로, 무는 "이기지도 지지도 않은 게임이 기대치만 쌓고 실적은 안 쌓는" 형태로, 판돈은 `p_g` 가중으로 자동 반영된다.

현실적인 범위 감각: 5인전에서 승 32% · 패 12%인 최상위권은 `WI=1.6, LI=0.6` → **PERF ≈ +1.0**. 승 12% · 패 32%인 최하위권은 **PERF ≈ −1.0**. 평균은 0.

### 16.3 신뢰도 축소 (표본이 적으면 함부로 티어를 주지 않는다)

PERF의 표준오차는 대략 `√(2 / E_w)`이다. 표본이 적으면 PERF가 크게 흔들리므로, 베이지안 축소로 0(평균)쪽으로 당긴다.

```
k = E_w / (E_w + E0),   E0 = 8
```

`E0 = 8`은 "실제 실력의 사전 표준편차를 PERF 기준 0.5로 본다"는 가정과 정확히 같다(`E0 = 2/τ²`, `τ = 0.5`). 실제 예상 표본:

| 종목 | 분기당 본인 참여 게임 | `E_w` | `k` |
| --- | --- | --- | --- |
| 훌라 (회당 5~6판) | 약 80판 | 17.9 | 0.69 |
| 시타델 (회당 1~2판) | 약 22판 | 4.9 | 0.38 |
| 젝스님트 (회당 1~2판) | 약 22판 | 4.9 | 0.38 |

*주 2회 × 13주 = 26세션, 출석률 75%, 판당 4~5인 가정.*

### 16.4 분기 리셋 — 단, 소프트 리셋

티어는 분기(1~3월 Q1, 4~6월 Q2, 7~9월 Q3, 10~12월 Q4)마다 새로 시작한다. 다만 **완전 리셋이 아니라 직전 분기의 성적 일부를 가중치 `γ = 0.35`로 이월**한다(롤 시즌 초 소프트 MMR 리셋과 같은 개념).

```
w_total   = E_w + γ · w_prev
PERF_mix  = (PERF · E_w + PERF_prev · γ · w_prev) / w_total
k         = w_total / (w_total + E0)
```

이월을 넣는 이유는 시뮬레이션 결과 때문이다(§16.6). 순수 리셋(γ=0)에서 티어와 실제 실력의 순위 상관계수는 훌라 0.876 / 시타델 0.578 / 젝스님트 0.396이었는데, γ=0.35에서 **0.929 / 0.712 / 0.533**으로 크게 올랐다. 판수가 적은 시타델·젝스님트에서 특히 효과가 크다. 그러면서도 새 분기가 시작되면 이월 가중치가 35%뿐이라 "다시 올라가야 한다"는 시즌 감각은 유지된다.

부수 효과로, **한 분기를 통째로 쉰 사람은 `E_w = 0`이라 `w_total = γ · w_prev`가 되어 `k`가 줄고 티어가 1000(중앙) 쪽으로 자연스럽게 감쇠**한다. 별도의 비활동 강등 규칙이 필요 없다.

> γ를 0으로 두면 사용자가 처음 말한 "완전 리셋"이 된다. 상수 하나만 바꾸면 되므로 실제로 한 분기 돌려보고 결정해도 된다.

### 16.5 TR(티어 레이팅)과 티어 컷

```
TR = 1000 + 500 × PERF_mix × k
```

| 티어 | TR 구간 |
| --- | --- |
| 챌린저 | ≥ 1270 |
| 마스터 | 1180 ~ 1269 |
| 다이아몬드 | 1090 ~ 1179 |
| 플래티넘 | 1000 ~ 1089 |
| 골드 | 910 ~ 999 |
| 실버 | 820 ~ 909 |
| 브론즈 | < 820 |
| 배치 중 | `w_total < 2.0` (기대 승수 2회분 미만 ≈ 9판 미만) |

**기존 6단계에 "마스터"를 새로 추가해 7단계**로 만든다. 8명이 참여하는 그룹에서 티어가 7단계면 대체로 한 칸에 1명씩 들어가 서열이 눈에 보인다.

계산은 스키마 변경 없이 **전체 게임을 분기 순서대로 접어(fold) 나가는 순수 함수**로 구현한다.

```
computeQuarterlyTiers(participants, games, gameType | "all")
  → Map<quarterKey, TierRow[]>

// quarterKey: "2026-Q3" — g.date.slice(0,4), slice(5,7) 문자열 슬라이스로만 판정.
//   §13.5에서 확인했듯 new Date() 파싱은 UTC 자정 해석으로 하루가 밀릴 수 있다.
// 첫 분기부터 순서대로 돌면서 각 분기의 (PERF_mix, w_total)을 다음 분기 prior로 넘긴다.
// 그 분기에 한 판도 안 뛴 참가자도 반드시 fold에 포함시켜야 감쇠가 동작한다.
```

### 16.6 시뮬레이션 검증

이 설계는 몬테카를로로 컷을 맞췄다. 8명, 주 2회 × 13주, 훌라 5~6판/시타델·젝스님트 각 1~2판/세션, 출석률 75%, 판당 4~5인, 20%는 2점짜리. 실력은 Plackett-Luce(θ를 −0.5~+0.5로 8등분, 최상위 승 32%·패 12% 수준)로 부여했다. 각 조건 700~3000분기 반복.

**훌라 — 티어별 분포 (실력차가 실제로 있는 세계)**

| 실력 순위 | TR 평균 | 브론즈 | 실버 | 골드 | 플래 | 다이아 | 마스터 | 챌린저 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 8위(최약) | 636 | 98% | 2% | · | · | · | · | · |
| 7위 | 739 | 79% | 17% | 4% | · | · | · | · |
| 6위 | 846 | 40% | 33% | 22% | 4% | 1% | · | · |
| 5위 | 952 | 9% | 23% | 37% | 23% | 7% | 1% | · |
| 4위 | 1064 | 1% | 5% | 19% | 36% | 28% | 10% | 2% |
| 3위 | 1156 | · | 1% | 6% | 18% | 37% | 26% | 13% |
| 2위 | 1253 | · | · | · | 4% | 18% | 33% | 45% |
| 1위(최강) | 1359 | · | · | · | · | 2% | 15% | 82% |

**핵심 지표**

| 종목 | 실력↔티어 순위상관 | P(8명이 브론즈~챌린저 전 구간에 분포) | P(실력이 완전히 같은데도 전 구간 분포) |
| --- | --- | --- | --- |
| 훌라 | 0.93 | **93.7%** | 1.5% |
| 시타델 | 0.71 | 31.0% | 5.2% |
| 젝스님트 | 0.53 | 15.0% | 5.0% |

사용자 요구("8명이 챌린저부터 브론즈까지 분포될 가능성이 현실적으로 존재")는 **주력 종목인 훌라에서 93.7%로 충족**된다. 동시에 실력차가 전혀 없는 세계에서 전 구간이 벌어질 확률은 1.5%뿐이라, 티어가 순전히 운으로 갈리지 않는다.

**시타델·젝스님트가 중앙에 몰리는 것은 의도된 결과다.** 분기당 22판 남짓으로는 실력을 가릴 표본이 근본적으로 부족하다. 컷 간격을 좁혀서 억지로 벌릴 수는 있지만, 시뮬레이션상 그 대가가 너무 크다.

| 스케일 | 시타델 P(전 구간 분포) | 실력차 0인데 전 구간 분포 |
| --- | --- | --- |
| 500 (채택) | 31% | 9% |
| 650 | 61% | 34% |
| 800 | 81% | 58% |

스케일 800이면 시타델도 화려하게 벌어지지만, 실력이 똑같아도 58% 확률로 챌린저와 브론즈가 나온다 — 티어가 사실상 제비뽑기가 된다. **그래서 종목과 무관하게 동일한 컷(SCALE=500)을 쓰고, 판수가 적은 종목의 티어가 가운데 모이는 것을 정상 동작으로 둔다.** 화면에는 신뢰도(`k`)를 함께 표시해 "왜 안 벌어지는지"를 보여준다.

### 16.7 화면 요구사항

> v2.16에서 이 섹션(및 §16.8 성향 맵)은 `/stats`에서 새 탭 `/records`("통산기록")로 옮겨진다 — §18.8 참조. 아래 내용 자체(카드 구성, 필터 등)는 그대로 유효하다.

- **`/stats`에 "분기 티어" 섹션 신설.** 분기 선택(기본값 = 현재 분기)과 종목 탭(훌라 / 시타델 / 젝스님트 / **통합**)을 두고, 참가자별 티어 카드를 TR 내림차순으로 나열한다. "통합"은 종목을 합쳐 계산해 표본이 가장 크므로(`E_w ≈ 27.6`, `k ≈ 0.78`) 대표 티어로 쓸 만하다.
- 각 카드에는 **티어 뱃지 · TR · 승 지수 · 패 지수 · 참여 판수 · 신뢰도(`k`를 %로) · 직전 분기 대비 승강 화살표**를 표시한다.
- **`/stats` 순위표에서 기존 티어 컬럼은 제거**한다. 순위표는 화면 필터(기간·종목)를 따르는데 티어는 분기 고정이라 두 개념이 섞이면 혼란스럽다. 대신 승률A/승률B 컬럼은 그대로 둔다.
- **대시보드(`/`)에는 이번 분기 통합 티어 요약**을 한 줄 카드로 넣는다(상위 3명 정도 + "전체 보기" 링크).
- 배치 중인 참가자는 "배치 중 (n/9판)"처럼 진행도를 보여준다.
- 티어 계산 방식을 한두 문장으로 설명하는 도움말을 섹션 하단에 둔다 (예: "참석 인원수로 계산한 기대 승·패 대비 성과로 매깁니다. 4인전 기대 승률 25%, 5인전 20%. 분기마다 리셋되며 직전 분기 성적이 35% 반영됩니다.").
- **"성향 맵" 섹션을 별도로 신설**한다(§16.8). 티어 섹션과 달리 분기가 아니라 **최근 90일 롤링**이며, 표본 게이트가 없다. 두 섹션의 기간 개념이 다르다는 점을 각각 부제로 명시한다 — 티어는 "이번 분기", 성향 맵은 "최근 3개월".

### 16.8 성향 맵 — 최근 3개월 2축 산점도 (뱃지 대신)

티어(분기 고정)와 별개로, **"요즘 어떤 플레이를 하고 있나"를 최근 90일 롤링으로 실시간 표시**하는 2축 맵을 둔다. 등급을 매기거나 뱃지를 붙이지 않고, 사람을 좌표평면 위에 점으로 찍는 것이 전부다.

승 지수·패 지수를 45° 돌리면 서로 다른 두 가지를 재는 축이 나온다.

| 축 | 정의 | 중심 | 읽는 법 |
| --- | --- | --- | --- |
| **X — 적극성 지수 `ENG`** | `(WI + LI) / 2` | 1.00 | 높을수록 Win 아니면 Lose로 끝나는 판이 많다(적극·고변동). 낮을수록 무로 지나가는 판이 많다(보수·저변동) |
| **Y — 손익 지수 `PERF`** | `WI − LI` | 0 | 양수면 이득, 음수면 손해. §16.2의 티어 핵심 지표와 같은 값 |

두 축은 **수학적으로 서로 독립**이다. `Cov(W+L, W−L) = Var(W) − Var(L)`인데 승·패의 기대치가 `1/n`으로 동일하므로 이 값이 정확히 0이다. 즉 "얼마나 적극적인가"와 "이득인가 손해인가"는 서로를 오염시키지 않는다.

**시뮬레이션 검증** — 선수마다 실력(θ)과 적극성(α)이라는 잠재 변수를 서로 상관 0이 되게 심어놓고(승자 ∝ softmax(θ+α), 패자 ∝ softmax(−θ+α)), 두 축이 각각을 제대로 복원하는지 확인했다. 90일 표본 2000회 반복:

| | 적극성(α)과의 상관 | 실력(θ)과의 상관 |
| --- | --- | --- |
| **ENG 축** | **+0.88** (훌라) / **+0.91** (통합) | −0.09 (교차, 0에 가까움) |
| **PERF 축** | +0.13 (교차, 0에 가까움) | **+0.87** (훌라) / **+0.90** (통합) |

각 축이 의도한 것만 재고 다른 축의 정보를 거의 섞지 않는다는 뜻이다.

**표본 게이트를 두지 않는다.** 티어와 달리 이 맵은 배치 조건도 축소(shrinkage)도 없이 **원값 그대로** 찍는다 — "요즘 어떠냐"를 즉시 보고 싶은 화면이기 때문이다. 대신 판수가 적으면 점이 크게 튄다는 사실을 **점 크기와 투명도로** 전달한다(판수가 많을수록 크고 진하게). 시뮬레이션으로 잰 실제 변동폭:

| 표본 | ENG (p1 ~ p99) | PERF (p1 ~ p99) |
| --- | --- | --- |
| 90일 전체 | 0.52 ~ 1.56 | −1.42 ~ +1.29 |
| 30일치 | 0.39 ~ 1.69 | −1.77 ~ +1.63 |
| 10일치 | 0.00 ~ 1.94 | −2.76 ~ +2.38 |

따라서 축 도메인은 **X `[0.2, 1.8]`, Y `[−1.6, +1.6]`** 로 고정하고, 벗어나는 점은 가장자리로 클램프해 테두리를 점선 처리한다(자동 스케일을 쓰면 이상치 하나가 나머지를 전부 가운데로 뭉갠다). X 하한은 위 표의 0.4가 아니라 0.2인데, 1.00("기대치")이 Y축의 0("본전")처럼 정중앙에 오게 하려면 상한(1.8, 1.0에서 +0.8)과 대칭인 0.2(1.0에서 −0.8)가 필요하기 때문이다 — 실제 ENG 값이 0.4 밑으로 내려가는 경우는 드물어(90일 p1 = 0.52) 왼쪽에 여백만 늘어날 뿐 데이터가 가려지지는 않는다.

**사분면**은 차트 배경에 옅은 라벨로만 표시한다 — 개인에게 붙는 뱃지가 아니다.

| | 이득 (Y > 0) | 손해 (Y < 0) |
| --- | --- | --- |
| **적극 (X > 1)** | 승부사 | 불나방 |
| **보수 (X < 1)** | 실속파 | 조공러 |

기존 `computeTier` 시절에 구상했던 개별 유형 뱃지(`PlayStyleBadge`)는 만들지 않는다.

### 16.9 티어 계산에 넣지 **않기로** 한 것

사용자가 "다양한 지표를 활용"해 달라고 했으므로 후보들을 실제로 시뮬레이션에 넣고 비교했다. 결론은 **PERF 하나가 이 데이터에서 뽑을 수 있는 신호를 사실상 다 담고 있고, 다른 지표를 더해도 순위 정확도가 오르지 않는다**는 것이다.

| 후보 | 훌라 순위상관 | 판정 |
| --- | --- | --- |
| PERF만 | 0.876 | 채택 |
| PERF + 세션 단위 성적(그날 이득/손해 본 날 비율) | 0.871 | 기각 — 개선 없음 |
| PERF + 세션 + 상대 강도(SOS) | 0.873 | 기각 — 개선 없음 |
| 세션 비중 확대 | 0.873 | 기각 — 개선 없음 |

이유는 명확하다. 세션 단위 성적은 PERF를 거칠게 반올림한 값이라 새 정보가 없고, 상대 강도는 8명 고정 풀에서 라인업이 거의 랜덤이라 사람마다 차이가 거의 안 생긴다. Bradley-Terry 방식의 능력치 추정도 시도했으나 구현이 훨씬 복잡한 데 비해 이득이 확인되지 않아 접었다.

**그래서 "다양한 지표"는 티어 수식을 복잡하게 만드는 방향이 아니라, ① PERF가 이미 흡수하고 있는 요소(인원수·판돈·무·표본)를 화면에서 드러내고 ② 승 지수/패 지수/신뢰도를 티어 옆에 병기하며 ③ 같은 두 축을 45° 돌린 성향 맵(§16.8)을 별도 화면 요소로 두는 방향으로 구현한다.**

### 16.10 성공 기준

- `computeTier(winRate, decisiveGames)`가 코드베이스에서 사라지고, 분기·종목별 티어로 완전히 대체된다.
- 티어 계산이 순수 함수이며 유닛 테스트(또는 검증 스크립트)로 다음이 확인된다: 모두가 정확히 기대치대로면 전원 TR 1000, 판수 부족 시 배치 중, 한 분기 쉬면 TR이 1000 쪽으로 감쇠, 분기 경계(3/31 → 4/1)가 정확히 갈린다.
- 성향 맵이 최근 90일 기준으로 그려지고, 판수가 1판뿐인 참가자도 (작고 흐린 점으로) 빠짐없이 표시된다. 모두가 정확히 기대치대로인 데이터에서는 전원이 (1.00, 0) 부근에 모인다.
- 스키마 변경·환경변수 추가 없음.
- `npm run build`, `npm run lint` 통과.

## 18. v2.16 요구사항 (하루 재정의 + 화면 전면 개편, 구현 완료)

### 18.1 하루의 재정의 (06:00~30:00)

이 모임의 게임 밤은 자정을 넘겨 이어지는 일이 흔한데, 기존에는 "하루"가 달력 날짜(00:00~23:59)였다. v2.16부터 **하루는 06:00부터 다음 날 06:00 직전까지("30:00")를 한 단위로 본다** — 예: 08/15 04:00에 기록된 게임은 08/14의 기록이고, 08/15 07:00에 기록된 게임은 08/15의 기록이다. 이 규칙은 **앱 전체에 공통**이며(게임 `date`, "오늘" 필터·요약, N차전 순서, 분기 경계), §3.3에 정리된 `time.ts`의 `businessDateFromWallClock`/`nowInSeoulBusinessDay`/`minutesSinceBusinessDayStart`가 단일 소스다. `nowInSeoul()`(실제 달력 순간)은 관리자 롤백 커트오프처럼 정말 순간이 필요한 곳을 위해 별도로 남아있다.

모든 시각 표기는 오전/오후가 아니라 **24시간제**로 통일한다 — 실제로 기존 코드베이스는 이미 전부 24시간제였고(`HH:mm`, `<input type="time">`), v2.16은 이 규칙을 명문화하고 관리자 수정 화면의 라벨에 "06:00 기준"/"24시간제"를 명시했다.

마이그레이션은 하지 않는다 — 배포 전 기록은 달력 날짜를 그대로 유지한다(§7).

### 18.2 대시보드: "오늘의 요약" → "최근 경기일 요약"

`computeTodaySummary`를 `computeRecentGameDaysSummary`로 교체했다(`src/lib/stats.ts`). 게임이 있었던 **최근 3개 영업일**(없는 날은 건너뜀)을 최신순으로 보여주며, 날짜별로 판수와 그날의 **득실(순점수) 기준 최다 승자**(공동 1위 전원 표기)·**득실차**를 표시한다. 승 횟수가 아니라 순점수로 순위를 매기는 이유는, 표시되는 "득실차" 숫자와 "누가 1위인지"가 서로 다른 기준이면 공동 1위가 왜 공동인지 설명이 안 되기 때문이다.

### 18.3 대시보드: 티어를 통합 + 종목별로

기존에는 통합 티어 상위 3명만 보여줬다. v2.16부터는 **통합·훌라·시타델·젝스님트 4블록**을 나란히 보여주고, 블록마다 상위 3명 + 분기 표기를 붙인다. "전체 보기" 링크는 `/stats`가 아니라 새 `/records` 탭으로 이동한다(§18.8).

### 18.4 새 게임 기록: 저장 후 게임 기록 탭으로 자동 이동

`/games/new`에서 결과를 확인하고 저장하면, 기존에는 같은 화면에 성공 메시지만 띄우고 남아있었다. v2.16부터는 저장이 끝나면 **`/games`로 자동 이동**해 방금 기록이 실제로 반영됐는지 바로 확인할 수 있다. `createGame` 서버 액션이 이미 `revalidatePath("/games")`를 호출하므로 이동 직후 목록에 최신 데이터가 뜨며, `router.refresh()`를 한 번 더 호출해 이를 보강한다.

### 18.5 통계 화면 재구성

`/stats`의 섹션 순서를 다음과 같이 확정한다: **순위표 → (순위표에서 "상대 전적" 클릭 시) 상대 전적 → 상대 전적 매트릭스 → 천적/밥 → 참가자별 승/패 → 참가자별 누적 순점수 추이 → 기간별 게임 수 추이**. "종목별 성적" 섹션은 삭제한다 — 순위표에 섹션별 종목 필터가 생기면서 같은 정보를 중복으로 보여주게 됐기 때문이다.

**필터 구조가 바뀐다.** 화면 상단 공통 필터는 **기간**만 남는다(여기에 `"오늘"`이 새로 추가된다 — §18.7). 기존에 화면 전체를 지배하던 **종목** 필터는 폐지되고, 위 7개 섹션 각각이 **자신만의 종목 필터**를 갖는다(상대 전적 패널은 순위표 섹션의 종목 필터를 그대로 물려받는다 — 순위표의 드릴다운이므로). **추이 단위**(일/주/월/연도) 필터는 공통 필터에서 빠지고, 실제로 그 단위를 쓰는 두 섹션(참가자별 누적 순점수 추이, 기간별 게임 수 추이)에만 각자 따로 존재한다.

**참가자별 누적 순점수 추이**는 기존에 게임 1건마다 점 하나(±1, 판돈 무시)를 찍었는데, v2.16부터 다른 추이 섹션과 같은 "추이 단위" 버킷 단위로 샘플링하고 판돈(`points`)을 반영한 순점수를 누적한다(`computeCumulativeNetPointsTrend`) — 게임 수가 많아지면 게임당 1점이던 x축이 무의미해지는 문제와, 다른 모든 "순점수" 지표와의 불일치를 함께 해결한다.

### 18.6 정산 화면: 섹션 순서

`/settlements`의 섹션 순서를 다음과 같이 확정한다: **참가자별 순 잔액 → 정리된 채권-채무 관계(거래 카드) → 변제 → 기부 기록 → 기부 랭킹 → 정산 & 조정 이력**. 기존에는 순 잔액이 거의 맨 아래에 있었고, 기부가 변제보다 먼저 나왔다.

### 18.7 통계 화면 기간 필터에 "오늘" 추가 + 필터 컷오프 버그 수정

기간 프리셋(`RangePreset`)에 `"오늘"`을 추가했다(영업일 기준, §18.1). 이 김에 `filterByDatePreset`과 `computeHotColdPlayers`의 최근 N일 컷오프 계산을, `new Date()`/`Date#setDate()` 기반(서버가 실행되는 타임존, 보통 UTC를 그대로 타서 최대 9시간까지 어긋날 수 있었다 — §13.5가 지적한 것과 같은 종류의 버그)에서 다른 곳과 동일한 **Asia/Seoul 문자열 비교**로 다시 작성했다. 부수 효과로 성향 맵의 "최근 90일" 창도 정확히 영업일 90일이 됐다.

### 18.8 새 탭 "통산기록" (`/records`) 신설 + 명예의 전당

`/stats`에 있던 **분기 티어(§16.7)**, **성향 맵(§16.8)**, **기록실**을 별도 탭 `/records`("통산기록")로 옮기고 이 순서로 배치한다. 기록실은 **"명예의 전당"**으로 이름을 바꾸고 다음 부문을 신설했다(`computeRecords`, `src/lib/stats.ts`):

- **최고 순득점 / 최저 순득점** — 참가자별 통산 순점수(`netPoints`) 리더보드의 위/아래.
- **하루 최다 게임수** — 개인 기록이 아니라 **팀 기록**이다. 특정 영업일에 전체 게임이 가장 많이 열린 날을 보여주며, 항목에 참가자 이름 대신 그 날짜가 표시된다.

기존 부문(최장 연승/연패, 하루 최다 승리, 최다 참석)은 그대로 유지된다. 모든 부문은 top 3 + dense ranking(공동 순위) 방식을 그대로 따른다.

### 18.9 성공 기준

- 06:00 이전 기록은 전날로, 06:00 이후 기록은 당일로 분류되며, 자정을 넘겨 이어진 게임 밤 안에서 N차전 순서가 실제 진행 순서와 일치한다(`scripts/verify-business-day.ts`).
- `/games/new`에서 저장하면 `/games`로 이동하고, 방금 기록한 게임이 새로고침 없이 보인다.
- `/settlements`가 참가자별 순 잔액 → 거래 카드 → 변제 → 기부 기록 → 기부 랭킹 → 이력 순으로 렌더링된다.
- `/stats`에 종목별 성적·분기 티어·성향 맵·기록실 섹션이 더 이상 없고, 남은 7개 섹션이 각자 독립된 종목 필터를 갖는다.
- `/records`가 분기 티어 → 성향 맵 → 명예의 전당 순으로 렌더링되고, 명예의 전당에 최고/최저 순득점과 하루 최다 게임수(팀 기록)가 보인다.
- 스키마 변경·환경변수 추가 없음, 기존 기록 마이그레이션 없음.
- `npm run build`, `npm run lint`, `npm run verify:tiers`, `npm run verify:business-day`, `npx tsx scripts/verify-settle.ts` 통과.

## 20. v2.17 요구사항 (영업일 규칙 소급 적용, 구현 완료)

### 20.1 증상과 진단

**증상**: 8/14 저녁부터 8/15 01:00까지 이어진 게임 밤이 화면에서 8/14와 8/15 이틀로 쪼개져 보인다. 자정 이후 기록이 8/14로 묶이지 않는다.

**진단 결과 — v2.16 코드 자체는 정상이다.** 실제 저장소를 확인한 결과:

| 확인 대상 | 상태 |
| --- | --- |
| `time.ts`의 `businessDateFromWallClock` / `nowInSeoulBusinessDay` / `minutesSinceBusinessDayStart` | 정상 |
| `actions.ts`의 `createGame` — `nowInSeoulBusinessDay()` 사용 | 정상 |
| `games.ts`의 `withinDayKey` — 06:00 기준 분 단위 정렬 | 정상 |
| `scripts/verify-business-day.ts` | 정상 (경계·윤년·정렬 모두 검증) |
| **이미 저장된 `games` 행의 `date` 값** | **미적용 — 이것이 원인** |

즉 §7이 "마이그레이션 없이 적용했다"고 명시한 바로 그 결정이 원인이다. v2.16 배포 이전에 자정~06:00 사이에 기록된 게임은 여전히 **달력 날짜**를 들고 있고, 앱은 그 `date` 값을 그대로 신뢰해 화면에 뿌린다. 코드를 아무리 고쳐도 저장된 값이 바뀌지 않으면 증상은 사라지지 않는다.

### 20.2 백필 설계 — `time` 컬럼이 아니라 `created_at`에서 다시 계산한다

가장 먼저 떠오르는 방법("`time < 06:00`인 행의 `date`를 하루 당긴다")은 **쓰면 안 된다.** 멱등하지 않기 때문이다. v2.16 배포 이후에 01:00에 기록된 행은 이미 `date`가 전날로 잡혀 있는데, 이 규칙을 적용하면 하루가 더 당겨져 **이틀 전**이 된다. 스크립트를 두 번 돌리면 또 당겨진다.

대신 **`created_at`에서 매번 다시 계산한다.** `created_at`은 진짜 UTC 순간이고 이 앱의 어떤 기능도 이 값을 고쳐 쓰지 않으므로, 언제 몇 번을 돌려도 같은 답이 나온다.

```
seoul   = seoulWallClockFromIso(row.created_at)      // {date, time} — 진짜 달력 시각
correct = businessDateFromWallClock(seoul.date, seoul.time)
```

**단, 관리자가 손으로 고친 기록은 건드리면 안 된다.** §11의 관리자 수정 기능은 `date`·`time`을 자유롭게 바꿀 수 있고, 그렇게 교정된 값을 `created_at`으로 덮어쓰면 관리자의 수정이 통째로 날아간다. 그래서 다음 가드를 둔다.

| `row.date` 상태 | 판정 | 조치 |
| --- | --- | --- |
| `=== seoul.date` (달력 날짜 그대로) | v2.16 이전에 자동 기록됨, 손댄 적 없음 | **`correct`로 갱신** (같으면 그대로 둠) |
| `=== correct` 이고 `!== seoul.date` | v2.16 이후에 자동 기록됨, 이미 정상 | 건너뜀 (멱등성 확보) |
| 위 어느 쪽도 아님 | 관리자가 손으로 고쳤거나 이상 데이터 | **건드리지 않고 목록으로 보고** |

`time`이 비어 있는 레거시 행(이 필드가 생기기 전 기록)도 `created_at`으로 판정할 수 있다. 다만 이런 행의 `date`를 당기면서 `time`을 비워두면 `withinDayKey`가 그 행을 분 단위 0으로 취급해 **그 영업일의 맨 앞으로 정렬**해버린다 — 실제로는 새벽이라 맨 뒤여야 한다. 따라서 **`date`를 옮기는 행 중 `time`이 비어 있으면 `created_at`에서 뽑은 시각을 함께 채운다.**

`settlements`는 같은 가드가 그대로 적용되지만 잔액 계산에 `date`가 쓰이지 않아 표시상의 문제뿐이므로 **옵션 플래그로 분리**한다. `adjustments`는 관리자가 과거 날짜를 직접 입력하는 테이블이라 위 가드에서 자연히 전부 "건너뜀"으로 걸러지므로 대상에서 제외한다.

### 20.3 재발 방지

- 관리자 수정 화면(`/games`의 인라인 수정 폼)에서 **시간을 06:00 이전으로 두면서 날짜를 그 시각의 달력 날짜로 남겨두면** 영업일 규칙과 어긋난 행이 다시 생긴다. 확인 단계에 경고를 띄우고 "영업일 기준으로 맞추기" 버튼(날짜를 하루 당김)을 제공한다. 관리자가 의도적으로 그렇게 두는 것 자체는 여전히 허용한다 — §11이 보장한 관리자 재량을 좁히지 않는다.
- `scripts/verify-business-day.ts`에 **백필 함수의 멱등성 테스트**(두 번 적용해도 결과가 같은지)와 **가드 테스트**(관리자 수정 흔적이 있는 행을 건너뛰는지)를 추가한다.

### 20.4 성공 기준

- 8/14 저녁 ~ 8/15 05:59에 기록된 과거 게임이 전부 `date = 2026-08-14`가 되고, `/games`·대시보드 최근 경기일 요약·N차전 번호가 하나의 게임 밤으로 보인다.
- 백필 스크립트를 연속 두 번 실행해도 두 번째 실행에서 갱신 건수가 0이다.
- 관리자가 날짜·시각을 직접 고친 기록은 백필 대상에서 제외되고 보고서에 따로 나열된다.
- 스키마 변경·환경변수 추가 없음.
- `npm run build`, `npm run lint`, `npm run verify:business-day`, `npm run verify:tiers`, `npx tsx scripts/verify-settle.ts` 통과.

## 22. v2.18 요구사항 (영업일과 표시 시각의 분리, 이번 작업 범위)

### 22.1 증상

v2.17 백필이 의도대로 동작해서 08/15 01:00에 친 게임이 08/14의 게임 밤으로 잘 묶인다. 그런데 **화면에 그 게임의 시각이 "2026-08-14 01:00"으로 나온다.** 실제로 그 게임을 친 시각은 08/15 01:00이므로, 존재하지 않았던 순간이 표시되고 있는 셈이다.

### 22.2 원인 — 한 값에 두 가지 역할을 시켰다

`games.date`는 v2.16 이후 **영업일**(게임 밤을 묶는 키)이고, `games.time`은 처음부터 **실제 벽시계 시각**이다. 두 필드의 기준이 서로 다른데 화면은 그 둘을 그대로 이어 붙여 출력한다:

```
{g.date} {g.time}  ->  "2026-08-14" + "01:00"  ->  "2026-08-14 01:00"   ← 실제로 없던 시각
```

`date`는 집계·필터·정렬에 쓰이는 **그룹 키**로서는 정확하다. 잘못된 것은 **그 그룹 키를 개별 기록의 타임스탬프로 그대로 재사용한 표시 로직**이다.

### 22.3 해결 — 표시 전용 역함수를 둔다

`businessDateFromWallClock(달력날짜, 시각) → 영업일`의 **정확한 역함수**를 `time.ts`에 추가하고, 개별 기록의 시각을 렌더링하는 모든 곳이 이걸 통과하게 한다.

```
calendarDateFromBusinessDay(영업일, "HH:mm")
  = 시각 >= 06:00 ? 영업일 : 영업일 + 1일
```

**`createdAt`으로 계산하지 않는 이유**: §11의 관리자 수정 기능은 `date`·`time`을 자유롭게 고칠 수 있고, 그렇게 교정된 기록은 `createdAt`(행이 쓰인 순간)과 의도적으로 다르다. 표시 값은 "저장된 게임 시각"을 따라야 하므로, 저장된 두 필드만으로 계산하는 위 순수 함수가 옳다. `createdAt`을 쓰면 관리자가 고쳐둔 시각이 화면에서 무시된다.

**왕복 불변식**: 모든 `(d, t)`에 대해 `businessDateFromWallClock(calendarDateFromBusinessDay(d, t), t) === d`. 검증 스크립트로 확인한다.

### 22.4 영업일을 쓰는 곳 / 벽시계를 쓰는 곳

이 구분이 이번 작업의 전부다. 헷갈리면 **"날(day)을 가리키는 라벨이냐, 기록 하나의 순간이냐"**로 판단한다.

| 화면 요소 | 기준 | 이유 |
| --- | --- | --- |
| `/games` 게임 목록의 각 행 타임스탬프 | **벽시계** | 기록 하나의 순간 |
| 대시보드 "최근 게임" 목록의 각 행 | **벽시계** | 기록 하나의 순간 |
| 관리자 수정 폼의 날짜·시각 입력값 | 영업일 (저장값 그대로) | 저장되는 값을 직접 편집하는 자리 — 다만 실제 벽시계가 무엇인지 보조 문구로 함께 안내 |
| 대시보드 "최근 경기일 요약"의 날짜 헤더 | 영업일 | 게임 밤 하나를 가리키는 라벨 |
| `/records` 명예의 전당 (하루 최다 게임수, 연승 기간 등) | 영업일 | 날 단위 기록 |
| `/games` 연·월·일 필터, `/stats` 기간 필터 | 영업일 | 게임 밤 단위로 묶어 조회 |
| `/settlements` 정산 이력의 날짜 | 영업일 | 시각을 저장하지 않는 날 단위 값 |

### 22.5 그룹과 표시가 어긋나 보일 때의 안내

`/games`에서 필터를 "8/14"로 두면 목록에 "2026-08-15 01:00" 행이 섞여 나온다. 이건 정상 동작이지만 설명 없이는 버그로 보인다. 그 행에 **영업일을 알려주는 작은 표식**(예: `8/14 게임밤` 칩, 또는 시각 앞의 `익일` 라벨)을 달아 두 값이 왜 다른지 한눈에 보이게 한다. 표식은 달력 날짜가 영업일과 다른 행에만 붙인다.

`time`이 없는 레거시 기록은 벽시계를 복원할 수 없으므로 영업일을 그대로 표시하고 표식도 붙이지 않는다.

### 22.6 성공 기준

- 08/15 01:00에 기록된 게임이 `/games`에서 **"2026-08-15 01:00"**으로 표시되고, 동시에 8/14 필터·8/14 경기일 요약·8/14 기준 N차전에 그대로 묶인다.
- 08/14 22:00 게임은 표시도 집계도 08/14로, 표식 없이 나온다.
- 관리자가 §11 수정 기능으로 날짜·시각을 직접 고친 기록은 그 고친 값 기준으로 표시된다(`createdAt`이 아니라).
- 왕복 불변식과 06:00 경계가 `npm run verify:business-day`로 검증된다.
- 스키마 변경·환경변수 추가·데이터 마이그레이션 없음 (표시 로직만 바뀐다).
- `npm run build`, `npm run lint`, `npm run verify:business-day`, `npm run verify:tiers`, `npx tsx scripts/verify-settle.ts` 통과.

## 24. v2.19 요구사항 (UX/UI 전면 정비, 이번 작업 범위)

### 24.1 배경

v2.12~v2.18을 거치며 화면이 8개로 늘고 지표가 티어·성향 맵까지 확장되는 동안, 색·타입·간격 규칙은 v1.0 시절 그대로였다. 그 결과 **정보는 다 있는데 화면이 무엇이 중요한지 말해주지 않는** 상태가 됐다. v2.19는 기능을 추가하지 않고 표현 계층만 정비한다.

기준 코드(v2.18 구현 직후) 실측으로 확인된 대표 증상:

- 화면 텍스트의 상당수가 WCAG AA(4.5:1) 미달이며, 특히 `text-slate-600`(**2.36:1**)이 **삭제 버튼**·**정산 취소 버튼**·**2시간 만료 안내**에 쓰인다. 위험 신호인 빨간색은 `hover:`에만 걸려 있어 폰에서는 영영 표시되지 않는다.
- 장부 앱의 핵심 값인 **점수가 12px `slate-500`** 으로, 같은 행의 타임스탬프보다 작고 흐리다.
- 흰 알약(`bg-slate-100 text-slate-900`)이 주요 액션·활성 필터·금액 뱃지·재활성화 버튼 **5가지 의미**를 겸해, `/stats`에서 화면상 가장 강한 요소가 기간 필터 칩이다.
- `/games`는 **첫 게임 행까지 760~970px**(폰 화면 1.1~1.4개)를 스크롤해야 하며, 그중 ~300px를 달력이 "이 날 게임이 있었다"는 불리언 한 비트에 쓴다.
- 게임 밤에 5~6판을 연속 기록하는데 **연속 입력 경로가 없다.** 한 판마다 `/games`로 이동하고 종목이 `hoola`로 리셋되어, 6판이면 페이지 로드만 12회다.
- 터치 타깃이 **44px에 도달하는 요소가 하나도 없고**(최대 40px), 행 액션(수정/삭제/완전삭제)은 높이 16px 텍스트 링크다.

### 24.2 설계 원칙

1. **토큰 우선.** 색·타입·간격·반경을 컴포넌트에 직접 쓰지 않는다. `globals.css`의 시맨틱 토큰과 `src/components/ui/`의 공용 컴포넌트를 경유한다. 지금 같은 화면별 변형(카드 패딩 `p-3`/`p-4`/`p-5`, 반경 `rounded-xl`/`rounded-2xl`, 섹션 제목 14px/16px)이 재발하지 않게 하는 것이 목적이다.
2. **폰 우선.** 390px에서 성립하지 않는 레이아웃은 데스크톱에서 아무리 예뻐도 실패로 본다.
3. **기능 변경 없음.** 도메인 로직(`settle.ts`·`stats.ts`·`time.ts`·`games.ts`)과 스키마, Server Action의 시그니처는 건드리지 않는다. 예외는 §24.10의 잔액 0 표시 버그뿐이다.
4. **점진 적용.** 세 배치로 나누고 배치마다 `npm run build`/`lint`/검증 스크립트를 통과시킨 뒤 다음으로 넘어간다(§24.12).

### 24.3 시인성 — 색 대비 하한

실측 대비율(배경 `slate-900` `#0f172a` 기준):

| 토큰 | 대비 | AA | 판정 |
| --- | --- | --- | --- |
| `slate-300` `#cbd5e1` | 11.4:1 | ✅ | 인터랙티브 텍스트 |
| `slate-400` `#94a3b8` | 7.06:1 | ✅ | **모든 읽는 텍스트의 새 하한** |
| `slate-500` `#64748b` | 3.74:1 | ❌ | 장식(구분점 `·`, 화살표 `→`) 전용으로 격하 |
| `slate-600` `#475569` | 2.36:1 | ❌❌ | **전면 폐기** |
| `red-500` `#ef4444` | 4.75:1 / slate-800 위 **3.9:1** | △ | `red-400`(6.5:1 / 5.3:1)으로 교체 |
| `emerald-400` `#34d399` | 9.34:1 | ✅ | 유지 |

**규칙: 읽어야 하는 텍스트는 `slate-400` 이상, 누를 수 있는 텍스트는 `slate-300` 이상. `slate-600`은 앱 어디에도 남기지 않는다.**

비활성(soft-deleted) 게임 행은 `bg-slate-800` 위에 `text-slate-600` + `opacity-60`이 겹쳐 최저 **1.9:1**까지 떨어진다. 비활성 표현은 투명도가 아니라 **좌측 세로 스트라이프 + `InactiveBadge`** 로 바꾸고 본문 대비는 유지한다.

색 하한이 적용되어야 하는 개별 지점:

| 위치 | 현재 | 요구 |
| --- | --- | --- |
| 게임 행 삭제/완전삭제 버튼 | `slate-600`, 위험색은 hover에만 | §24.8의 아이콘 액션으로 대체, 위험색 상시 노출 |
| 정산 이력 취소 버튼 | `slate-600` 12px | 동일하게 아이콘 액션 + 확인 |
| 2시간 만료 안내문 | `slate-600` | 문구 삭제(버튼 부재가 곧 신호), 대신 §24.9의 잔여 시간 칩 |
| 달력 비게임일 셀 | `slate-600` | `slate-400` |
| 티어 카드 "− 유지" 델타 | `slate-600` on `slate-800` (1.93:1) | `slate-400` |
| 배치 중 진행바 | 채움 `slate-600` / 트랙 `slate-700` (**1.41:1**) | 채움 `emerald-500`, 옆에 `3.2 / 9판` 수치 병기 |
| 상대전적 히트맵 | 알파 하한 0.12 (**1.07:1**) | 5단계 이산 스케일 + 범례 칩, 최저 단계도 식별 가능하게 |
| 성향 맵 사분면 배경 | `fillOpacity 0.06` | 0.12~0.15, 라벨은 `slate-300` + 배경 칩 |

### 24.4 시인성 — 타입 스케일

현재 실측은 사실상 3단계다: **24px(h1) → 16px(h2, 굵기로만 구분) → 14px(본문) → 12px(나머지 전부)**. `text-xs`(12px)가 필터 라벨·폼 라벨·테이블 헤더·점수 값·메모·참석자 목록·경고 문구·액션 버튼을 모두 담당해 화면이 균일한 회색 벽으로 읽힌다. 게다가 `<h2>`가 `/games`에서는 `text-sm`, `/participants`에서는 16px로 화면마다 다르다.

4단계로 고정한다:

| 역할 | 클래스 | 크기/굵기 | 용도 |
| --- | --- | --- | --- |
| display | `text-2xl font-bold` | 24px | 페이지 `<h1>` |
| section | `text-lg font-semibold` | 18px | 섹션 `<h2>` — **신설** |
| body | `text-sm` | 14px | 본문·테이블·입력·버튼 |
| caption | `text-xs` | 12px | 라벨·헬퍼 (색은 `slate-400` 이상) |

섹션 제목은 예외 없이 공용 `<SectionTitle>`을 쓰고, 화면마다 다른 크기를 쓰지 않는다.

### 24.5 시인성 — 정보 위계 재배치

**게임 행(`/games`, 대시보드 최근 게임)** — 현재는 가장 왼쪽·가장 긴 요소가 타임스탬프(14px)이고, 핵심 값인 점수가 12px `slate-500`이다. 참석자 8명 이름 나열이 행 높이의 1/4을 차지하는데 정보 밀도는 가장 낮다.

목표 구조:

```
[훌라] [8/14 게임밤]                        3차전 · 23:30
민수  →  지훈                                       2점
참석 5명 ⌄                                          [⋯]
```

- **점수를 우측 정렬 + `text-base font-semibold`** 로 승격한다.
- 날짜는 **날짜별 그룹 헤더**로 올리고 행에는 시각만 남긴다. (필터·달력과 중복이며, 게임 밤 하나를 보는 게 기본 사용 패턴이다.) 게임 밤 표식(§22.5)은 그룹 헤더가 아니라 **행**에 유지한다 — 어긋나는 건 개별 행이므로.
- 참석자는 **인원수만** 기본 노출하고 이름은 탭하면 펼친다.
- 메모는 점수와 같은 스타일이라 구분이 안 되므로 별도 줄 + `slate-400`.

**정산 잔액** — §24.10 참조.

### 24.6 심미성 — 토큰과 공용 컴포넌트

`globals.css`가 정의하는 변수는 현재 `--background`/`--foreground` 둘뿐이고 나머지는 전부 하드코딩이다. 시맨틱 토큰을 도입한다:

```
--color-surface          카드 배경
--color-surface-raised   강조 블록 (제한적으로만)
--color-border           slate-700/60 — 실제로 보이는 보더
--color-text             본문
--color-text-muted       보조 하한 (slate-400)
--color-win / --color-lose
--color-accent           indigo-400/500 — 신설
```

서피스는 **3단(950/900/800)에서 2단(페이지/카드)으로 축소**한다. 현재 내부 카드가 `bg-slate-800`에 `border-slate-800`을 써서 보더가 무효이고, 인접 단계 간 명도 차가 1.4:1이라 구분되지 않는다. 내부 그룹은 배경색 대신 **구분선 + 여백**으로 나눈다.

공용 컴포넌트를 `src/components/ui/`에 신설한다: **`Card` / `SectionTitle` / `Button` / `FilterChip` / `EmptyState`**. 기존 `badges.tsx`가 유일한 공용 컴포넌트였고, 그래서 위 변형들이 생겼다.

### 24.7 심미성 — 버튼 토큰 분리와 액센트

흰 알약이 5가지 의미를 겸하는 문제를 토큰 분리로 해소한다:

| 역할 | 스타일 | 규칙 |
| --- | --- | --- |
| Primary | `bg-accent text-white` | **화면당 1개** 원칙 |
| Selected | `bg-slate-700 text-slate-100 ring-1 ring-slate-500` | 필터 칩·탭 선택 상태 |
| Neutral | `bg-slate-800 text-slate-300` | 보조 액션 |
| Danger | `bg-red-500/15 text-red-300 border-red-800/60` | 파괴적 동작 |
| Data | `bg-slate-700 text-slate-100` | 금액 뱃지 등 표시용 |

부수 효과로 앱에 처음으로 **액센트 색**이 생긴다. 지금은 slate + 의미색(초록/빨강)뿐이라 정체성이 없다. 훌라 뱃지가 이미 indigo를 쓰고 있으므로 indigo를 브랜드 색으로 승격한다.

### 24.8 심미성 — 폰트, 숫자, 인터랙션 피드백

- **Pretendard Variable** 적용(`next/font/local`). 현재 시스템 스택 + `Malgun Gothic` 폴백은 윈도우/안드로이드에서 한글이 투박하게 렌더된다. 이 문서에서 비용 대비 인상 변화가 가장 큰 항목이다.
- **`tabular-nums`** 를 순위표·점수 테이블·TR 값 등 세로로 정렬되는 모든 숫자에 적용한다. 지금은 자릿수마다 글리프 폭이 달라 미세하게 흔들린다.
- **`active:` / `focus-visible:` 상태 신설.** 현재 `focus-visible:`는 앱 전체에서 2곳뿐이고 `active:`는 0곳이며, 나머지는 전부 `hover:`다. 폰에는 hover가 없으므로 **버튼을 눌러도 화면상 아무 반응이 없다.** 서버 왕복이 끝날 때까지 무반응이라 사용자가 한 번 더 누르게 되는데, 참가자 추가·이름 저장·비활성화 3개 폼에는 중복 제출 방어조차 없다. 공용 `Button`에 `active:scale-[0.97]` + `focus-visible:ring-2 ring-accent`를 기본 탑재하고, 모든 폼 제출에 `useFormStatus`/`useTransition` 기반 pending·disabled를 건다.
- **아이콘 체계 도입**(`lucide-react`). 현재 앱 전체 아이콘은 직접 그린 `CheckIcon` 1개와 경고 배너의 리터럴 `⚠` 글자뿐이라, 행 액션이 "수정 삭제 완전삭제" 12px 글자 나열이 되어 시인성·터치 문제로 직결된다.

### 24.9 편의성 — 연속 기록 모드 (최우선)

게임 밤에 훌라 5~6판 + 시타델 1~2판을 친다. 그런데 한 판을 기록하면 `router.push("/games")`로 폼이 언마운트되어 **종목이 `hoola`로, 점수가 1로 리셋**되고, 다음 판을 넣으려면 `/games`에서 `+ 새 게임 기록`을 다시 눌러 페이지 전환을 한 번 더 거쳐야 한다. 한 판당 실제 비용은 **탭 3~5회 + 페이지 로드 2회**다.

요구사항:

- 기록 화면 하단 버튼을 둘로 나눈다. **`기록하고 계속 입력`(Primary)** — 종목·참가자를 유지한 채 승패 선택만 초기화하고 폼에 머문다. 상단에 `N판 기록됨` 누적 표시와 방금 기록한 결과 한 줄을 띄운다. **`기록하고 목록으로`(Neutral)** — 현재 동작.
- **종목 기본값을 "직전 게임의 종목"** 으로 바꾼다(현재 `hoola` 하드코딩). 시타델 2연판을 칠 때 매번 다시 고르는 낭비가 사라진다.
- **칩 리플로우 제거.** 결과 선택 시 칩 안에 `Lose 선택됨 · Win을 탭하세요` 부제가 추가되면서 칩 높이가 커져 그리드 전체가 재배치되고, 아직 누르지 않은 Win 대상이 손가락 밑에서 움직인다. 부제는 칩 밖 고정 위치(섹션 헤더)로 옮기고 칩 높이는 고정한다.
- 종목 버튼 그리드(`grid-cols-3`)는 390px에서 셀 폭 ~100px인데 "젝스님트" + 체크 아이콘 + 패딩이 ~106px라 줄바꿈된다. 선택 시에만 아이콘이 붙어 폭이 변하는 것도 레이아웃 시프트의 원인이다. **아이콘 자리를 항상 확보**하고 셀 폭을 맞춘다.

### 24.10 편의성 — `/games` 상단 압축과 필터

첫 게임 행까지의 세로 비용(390×844):

| 구간 | 높이 |
| --- | --- |
| 스티키 헤더 + 페이지 헤더 | ~136px |
| 연·월·일 필터 카드 | ~130px |
| **달력** | **~275-310px** |
| 구간 인별 점수 테이블 | ~150-356px |
| **첫 게임 행까지** | **≈ 760 ~ 970px** |

- **달력은 기본 접힘.** `날짜로 찾기` 토글 한 줄로 대체한다. 연·월·일 드롭다운이 이미 같은 일을 하므로 달력은 보조 수단이다. 펼쳤을 때는 날짜 셀 높이를 **44px**로 올리고, 게임이 있었던 날은 불리언이 아니라 **게임 수를 농도/점 개수로** 인코딩한다.
- **`/games`에 종목 필터를 추가**한다. 모든 행에 종목 뱃지를 달아놓고 그것으로 거를 수 없는 상태다.
- **일 드롭다운이 1~31 고정**이라 2월 30일을 선택할 수 있다. 선택된 연·월의 실제 일수로 제한한다.
- **월 드롭다운을 바꿔도 달력 그리드는 이전 달에 남는다**(`cursorMonth` 미동기화). 두 컨트롤이 같은 상태를 가리키므로 동기화한다.
- 구간 인별 점수 테이블은 기본 접힘 또는 요약 1줄로 축소한다.

### 24.11 편의성 — 터치 타깃과 파괴적 동작

**44px에 도달하는 요소가 앱 전체에 하나도 없다**(최대 40px). 특히 문제인 것:

| 현재 크기 | 요소 |
| --- | --- |
| **16px** | 수정 / 삭제 / 완전삭제 / 하드삭제 확인·취소 / 정산 취소 / 참가자 이름 저장 / 이전·다음 게임일 / 로그아웃 |
| 28px | 달력 월 이동 화살표, 참가자 비활성화 |
| 32px | 점수 스테퍼 −/+ |
| 44×28px | 달력 날짜 셀 (세로 부족, 간격 4px) |

또한 **위험도와 마찰의 순서가 뒤집혀 있다.** 소프트 삭제는 확인 없이 한 번 탭이면 되는데 완전삭제에는 인라인 확인이 있다. 정산 쪽은 더 극단적이어서, **기록**에는 2단계 확인 + 5점 이상 경고 배너가 있는데 **취소(삭제)** 는 2.35:1 회색 12px 글자 한 번 탭이면 끝난다.

요구사항:

- 행 액션을 **`⋯` 오버플로 버튼(44×44)** 으로 통합하고, 시트/드롭다운에서 `수정` / `삭제`(Danger 스타일)를 고르게 한다.
- 소프트 삭제는 확인 대화상자 대신 **"삭제됨 · 되돌리기" 스낵바**로 처리한다. 이미 정산 쪽에 2시간 되돌리기 배너 패턴이 있으므로 일관되고, 확인 단계 없이도 안전하며 더 빠르다.
- 정산 이력의 취소도 동일한 아이콘 액션 + 되돌리기 패턴으로.
- 점수 스테퍼 −/+, 달력 월 이동, 달력 날짜 셀, 참가자 화면의 모든 액션을 44×44 히트 영역으로. (시각적 크기는 작아도 되며, `::before` 확장이나 패딩으로 히트 영역만 키워도 된다.)
- **되돌리기 배너가 슬롯 1개**라 연속 2건을 기록하면 첫 번째 취소 버튼이 사라진다. 스택으로 바꾸거나, 최소한 이력 목록의 취소 접근성을 위 아이콘 액션으로 확보한다.

### 24.12 편의성 — 잔여 시간, 필터 상태, 빈 상태

**2시간 창의 잔여 시간이 안 보인다.** `isWithinEditWindow`는 렌더 시점 1회 평가이고 타이머가 없어서, 몇 분 남았는지 알 수 없고, 화면을 열어둔 채 2시간이 지나도 버튼이 남아 있다가 누르면 서버에서 거절된다. 그리고 만료된 행에는 22자짜리 2.36:1 안내문이 들어와 **오래된 행일수록 더 시끄러워진다**.
→ 수정 가능한 행에만 작은 칩 `수정 가능 1시간 23분`(1분 간격 갱신), 만료된 행은 안내문 제거(버튼 부재가 곧 신호).

**필터 상태가 어디에도 저장되지 않는다.**

| 화면 | 필터 개수 | 저장 |
| --- | --- | --- |
| `/games` | 연·월·일 3 + 달력 커서 | 없음 (기본값 = 오늘) |
| `/stats` | **9개** (기간 1 + 종목 6 + 추이단위 2) | 없음 |
| `/records` | 3개 (분기 + 종목 2) | 없음 |

전부 `useState`라 게임 하나 수정하러 들어갔다 나오면 오늘로 리셋되고, 뒤로가기도 무의미하며 링크 공유도 안 된다. **URL 검색 파라미터로 동기화**한다(`/stats?h2h=`가 이미 같은 패턴을 쓴다).

**`/stats`의 종목 필터 6개** 는 그 자체로 문제다. "훌라만 보기"를 하려면 동일한 컨트롤을 6번 눌러야 한다. **페이지 레벨 1개**로 줄이고, 굳이 개별 오버라이드가 필요한 섹션만 예외로 둔다.

**빈 상태에 출구가 없다.** 빈 상태 문구는 총 15곳이며 전부 `text-sm text-slate-500` 한 줄로, 아이콘·액션·원인 설명이 없다. 특히 `/games`는 기본 필터가 **오늘**이라 게임 밤이 아닌 날 앱을 켜면 **300px 달력 아래에 빈 상태 2개가 연달아 쌓인다.**
→ 공용 `EmptyState`에 액션을 붙인다: `필터 초기화` / `최근 게임일로 이동` / `+ 새 게임 기록`. 그리고 `/games` 기본 필터를 **"가장 최근 게임일"** 로 바꾼다(대시보드 최근 게임 섹션이 이미 같은 로직을 쓴다).

### 24.13 표시 버그와 차트 정비

**잔액 0 표시 버그(기능 수정).** `settlements/page.tsx`가 `b.amount > 0 ? emerald : red`라 **잔액이 정확히 0인 사람이 빨간 `0`으로 표시된다.** 정산이 끝난 사람이 채무자처럼 보인다. 양수/0/음수 3분기로 나누고, 0은 중립색 + `정산 완료` 표기로.

**차트:**

- `PALETTE[0] = #e2e8f0`은 본문 텍스트 색과 거의 같아 첫 참가자의 선이 UI 텍스트처럼 보인다. 팔레트에서 제외한다.
- 색이 **위치 기반 배정**(`PALETTE[i % len]`)이라 필터를 바꾸면 같은 사람의 선 색이 바뀐다. **참가자 ID 기반 고정 매핑**으로 교체하고, 이 매핑을 통계·성향 맵·기록실이 공유한다.
- 누적 추이 라인차트가 버킷 20개 초과 시 **X축을 통째로 숨긴다**(`hide={len > 20}`). "일별 + 최근 90일"을 고르면 시간축 없는 그래프가 나온다. `interval="preserveStartEnd"`로 솎아낸다.
- `dot={false}` + 툴팁 전용이라 터치에서 값을 읽을 수단이 없다. 최소한 마지막 점에 값 라벨을 붙이거나 활성 점을 키운다.
- 차트 높이 280/320/**420**px 고정. 성향 맵 420px는 폰 화면의 절반이다. 뷰포트 기반으로 낮춘다.
- 성향 맵 이름 라벨 8개가 10px로 겹친다(도메인 고정이라 대부분 중앙에 뭉치는데 충돌 회피가 없다). 라벨 크기를 올리고 충돌 시 숨기거나 오프셋을 준다.

**기타:**

- `/records` 카드가 폰에서 1열이다(`sm:grid-cols-2`는 640px부터). 티어 8장 + 명예의 전당 8장 = **16장이 세로로 쌓이고** 그 사이에 420px 차트가 있다. 티어 카드는 390px에서도 2열이 충분하다.
- 정산 이력이 **무한 렌더**다(상한·페이지네이션 없음). 필터 탭도 클라이언트 상태가 아니라 서버 왕복 네비게이션이라 매번 전체 리로드된다.
- **"변제"가 이력에서 "실제 정산"으로 표시된다.** 둘 다 `type: "payment"`로 저장되는데 입력 화면에는 별도 폼이 있어 명칭이 어긋난다. 라벨을 맞추거나 구분 근거를 만든다.
- **정산 폼 확인 단계에서 입력값이 사라진다.** early return으로 폼 전체가 언마운트되어, 확인 화면에서 오타를 발견해도 뒤로 가야 한다. 입력값을 읽기 전용으로 함께 보여준다.
- **관리자 모드 칩이 `/participants`에서 2번 뜬다**(헤더 + 페이지 상단).
- **관리자 네비게이션 8탭이 폰에서 2개만 보인다.** 가로 스크롤인데 스크롤 가능하다는 시각 신호가 없다. 가장자리 페이드 또는 2행 배치로.

### 24.14 작업 분할

세 배치로 나눈다. 배치마다 빌드·린트·검증 스크립트를 통과시킨 뒤 다음으로 넘어간다.

| 배치 | 범위 | 대응 프롬프트 |
| --- | --- | --- |
| **A. 기반** | §24.3 색 하한, §24.4 타입 스케일, §24.6 토큰·공용 컴포넌트, §24.7 버튼 토큰, §24.8 폰트·숫자·인터랙션 | `CLAUDE_CODE_PROMPT_V2_19A_FOUNDATION.md` |
| **B. 편의성** | §24.9 연속 기록, §24.10 `/games` 압축·필터, §24.11 터치 타깃·파괴적 동작, §24.12 잔여 시간·URL 동기화·빈 상태 | `CLAUDE_CODE_PROMPT_V2_19B_CONVENIENCE.md` |
| **C. 표시·차트** | §24.5 정보 위계, §24.13 잔액 버그·차트·기타 | `CLAUDE_CODE_PROMPT_V2_19C_POLISH.md` |

A를 먼저 끝내야 B·C가 공용 컴포넌트 위에서 작업할 수 있다. B와 C는 순서를 바꿔도 되지만 겹치는 파일(`GamesListClient.tsx`)이 있으므로 동시에 진행하지 않는다.

### 24.15 성공 기준

- **대비**: 앱 전체 검색에서 `text-slate-600`이 0건, `text-red-500`이 0건. 모든 본문·라벨 텍스트가 배경 대비 4.5:1 이상.
- **터치**: 주요 액션·파괴적 액션·행 액션·스테퍼·달력 셀의 히트 영역이 44×44px 이상. 밀집 필터 칩은 최소 36px + 8px 이상 간격. 390px 뷰포트에서 확인.
- **타입**: 모든 섹션 `<h2>`가 `SectionTitle`을 경유해 18px로 통일. 화면별 변형 없음.
- **토큰**: 카드가 전부 공용 `Card`를 경유해 패딩·반경이 동일. `bg-slate-100 text-slate-900` 직접 사용이 Primary 버튼 외에는 0건.
- **연속 기록**: 훌라 6판 연속 기록에 페이지 전환 0회, 판당 탭 2~3회.
- **`/games`**: 390px에서 첫 게임 행이 **첫 화면(≤700px) 안에** 들어온다.
- **필터**: `/games`·`/stats`·`/records`의 필터가 URL에 반영되고, 새로고침·뒤로가기 후에도 유지된다.
- **잔액 0**: 잔액이 정확히 0인 참가자가 빨간색으로 표시되지 않는다.
- **차트**: 같은 참가자의 선/점 색이 필터를 바꿔도 동일하다.
- 기능·스키마·환경변수 변경 없음(§24.13 잔액 표시 조건 수정 제외).
- `npm run build`, `npm run lint`, `npm run verify:business-day`, `npm run verify:tiers`, `npx tsx scripts/verify-settle.ts` 통과.

## 26. v2.20 요구사항 (라이브 게임밤 보드, 이번 작업 범위)

### 26.1 배경

이 앱은 지표가 이미 충분하다 — 순위표, 승률A/B, 상대 전적 매트릭스, 천적/밥, 핫/콜드, 최근 폼, 스트릭, 분기 티어, 성향 맵, 명예의 전당까지 있다. **비어 있는 건 지표가 아니라 "지금 이 순간"이다.** 모든 화면이 사후 조회용이고, 게임 밤 *중에* 테이블 위에 올려두고 다 같이 볼 화면이 없다.

라이브 게임밤 보드는 이 빈칸 하나만 채운다. **새로운 데이터 수집도, 새로운 지표도 없다** — 이미 있는 집계 함수를 "오늘 영업일" 범위로 돌려서 큰 글씨로 보여주는 게 전부다. 그런데 이것만으로 앱의 성격이 "다음 날 확인하는 장부"에서 "게임 중에 켜놓는 물건"으로 바뀐다.

### 26.2 표시 조건과 위치

- **표시 조건**: `todayInSeoul()`(영업일, §18.1)과 `date`가 같은 **활성 게임이 1판 이상** 있을 때만.
- 조건을 만족하지 않으면 **대시보드에서 통째로 사라진다.** 게임 밤이 아닌 날의 대시보드를 어지럽히지 않는 게 이 기능의 전제다.
- 위치는 **대시보드 최상단** — 페이지 헤더 바로 아래, "최근 경기일 요약"보다 위.
- 게임 밤이 06:00을 넘겨 이어지면 영업일이 바뀌면서 보드가 초기화되고 새 밤이 시작된다. 이는 §18.1 규칙의 자연스러운 귀결이며 **의도된 동작**이다(06:05에 친 게임은 새 날의 1차전이 맞다).

### 26.3 보드 내용

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  오늘의 게임밤 · 8/14 (목)              [전체화면 ⤢]
  훌라 4 · 시타델 1  ·  총 5판
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  1  민수     3승 0패      +3      🔥 3연승
  2  지훈     1승 1패       0
  3  창민     1승 2패      -1
  4  성호     0승 1패      -1
  5  태윤     0승 0패       0      아직 무승부
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  방금 5차전 · 23:30 · 민수 → 창민 1점
```

| 요소 | 내용 |
| --- | --- |
| 헤더 | 영업일 날짜 + 요일, 종목별 판수, 총 판수 |
| 행 | 순위 · 이름 · `N승 M패` · 순점수(부호+색) · 연승/연패 뱃지 |
| 행 대상 | **오늘 참석한 사람만.** 오늘 안 온 사람은 나오지 않는다 |
| 무승부 참가자 | 오늘 참석했지만 승·패가 없는 사람도 `0승 0패`로 표시한다 (기존 `computeParticipantPointTotals`는 승·패가 있는 사람만 반환하므로 그대로 쓸 수 없다) |
| 푸터 | 가장 최근 판의 N차전·시각·승자→패자·점수 |

정렬은 **순점수 내림차순 → 승수 내림차순 → 이름(한국어 정렬)**.

연승/연패는 **오늘 밤 기준**이다(통산 스트릭이 아니라). 오늘 게임만 넘겨서 계산한다.

### 26.4 데이터 계산

`src/lib/stats.ts`에 `computeGameNightBoard(participants, games, businessDate)`를 추가한다. 기존 함수들을 재사용하되, 오늘 참석자 전원(승·패가 없는 사람 포함)을 포함해야 하므로 얇은 래퍼가 아니라 별도 함수로 둔다.

반환 형태:

```ts
export interface GameNightRow {
  id: string;
  name: string;
  appearances: number;   // 오늘 참석 판수
  wins: number;
  losses: number;
  netPoints: number;
  streakType: "W" | "L" | null;   // 오늘 밤 기준
  streakLength: number;
}

export interface GameNightBoard {
  date: string;                    // 영업일
  totalGames: number;
  countsByGameType: { gameType: GameType | undefined; count: number }[];
  rows: GameNightRow[];
  latestGame: GameResult | null;
  latestSequence: number | null;   // N차전
}
```

- 게임 순서·N차전은 기존 `withinDayKey`(`src/lib/games.ts`)와 `computeDailySequenceNumbers`를 그대로 쓴다. 자정을 넘긴 판이 앞으로 오면 안 된다.
- `points`가 없는 레거시 기록은 1점으로 취급한다.
- 소프트 삭제된 게임은 `activeGames`로 제외한다.

### 26.5 전체화면 모드 (`/tonight`)

이 기능의 실제 사용처는 **폰을 테이블 가운데 두고 다 같이 보는 것**이다. 대시보드 카드 안에서는 글씨가 작다.

- `/tonight` 라우트를 신설하고, 보드 카드의 `전체화면 ⤢` 링크로 연결한다.
- 같은 데이터를 **훨씬 큰 타이포**로, 헤더 네비게이션 없이 전체 화면에 렌더한다.
- 게임 밤이 아닐 때 접근하면 "오늘은 게임 밤이 아닙니다" + 대시보드로 돌아가는 링크.
- **화면 꺼짐 방지**(`navigator.wakeLock`)를 걸어둔다. 지원하지 않는 브라우저에서는 조용히 무시한다.
- 이 화면은 조회 전용이다 — 기록·수정 진입점을 두지 않는다(테이블 위에서 누가 실수로 누르는 걸 막는다). 단 `+ 새 게임 기록` 버튼 하나는 예외로 크게 둔다.

### 26.6 자동 갱신

새 게임을 기록하면 보드가 저절로 갱신되어야 한다.

- 클라이언트 컴포넌트에서 **30초 간격 `router.refresh()`**. 대시보드는 이미 `force-dynamic`이라 서버에서 새로 계산된다.
- **탭이 보이지 않을 때는 폴링을 멈춘다**(`document.visibilityState`). 다시 보이면 즉시 한 번 갱신하고 재개한다.
- 폴링은 **보드가 표시되는 동안에만** 돈다. 게임 밤이 아니면 타이머 자체가 없다.

### 26.7 이번 범위에서 하지 않는 것

- 다음 판 승률 예측 — 별도 제안 항목이며 여기 섞지 않는다
- 업셋 표식, 게임밤 요약 공유 이미지, 시상식, 동석 효과 — 전부 별도
- 웹소켓·SSE 실시간 — 8명이 30초 폴링이면 충분하고, 인프라를 늘릴 이유가 없다
- 새 지표·새 랭킹 — 이 기능은 **기존 집계를 오늘 범위로 다시 보여주는 것**이지 새 지표가 아니다

### 26.8 성공 기준

- 게임 밤에 한 판을 기록하면 **30초 안에** 보드에 반영된다.
- 게임 밤이 아닌 날 대시보드에는 보드가 **전혀 나타나지 않는다.**
- 오늘 참석했지만 승·패가 없는 참가자도 `0승 0패`로 보드에 나온다.
- 자정을 넘겨 친 판(예: 01:00)이 같은 게임 밤에 묶이고, N차전 번호가 진행 순서대로다(§18.1·§22).
- `/tonight`을 열어두고 방치해도 화면이 꺼지지 않으며(지원 브라우저), 탭을 가리면 폴링이 멈춘다.
- 스키마 변경·새 환경변수·새 의존성 없음.
- `npm run build`, `npm run lint`, `npm run verify:business-day`, `npm run verify:tiers`, `npx tsx scripts/verify-settle.ts` 통과.

> **구현 완료 — 2026-08-16.**
> - **`computeGameNightBoard`** (`src/lib/stats.ts`): 활성 게임이 없는 영업일은 `null`. 오늘 참석자(`attendeeIds` 합집합)만 `computeParticipantStats`에 넘겨 0승0패 참석자도 포함시켰고(전체 참가자 풀을 넘기지 않는 게 핵심), `computeCurrentStreaks`도 오늘 게임·오늘 참석자로 좁혀서 "오늘 밤 기준" 스트릭을 얻었다. `latestGame`/`latestSequence`는 `withinDayKey` 정렬 + `computeDailySequenceNumbers`(전체 게임 목록 기준)로 계산.
> - **컴포넌트 공유 구조**: `src/components/GameNightBoard.tsx` 하나에 `GameNightBoardCard`/`GameNightBoardFull`을 두고, 행 렌더링(`BoardRow`/`BoardFooter`)은 완전히 공유하며 타이포 클래스만 `size: "card" | "full"` 룩업 테이블로 갈아끼운다 — 헤더·부제는 카드 쪽만 `SectionTitle`(18px 고정)을 쓰고, full 쪽은 그 스케일로는 목적(1~2미터 밖에서 읽기)을 못 이루므로 자체 `text-3xl~4xl` 마크업을 쓴다.
> - **`/tonight` 헤더 숨김**: `(app)/` 밖에 최상위 라우트로 뺐다(`/login`·`/admin-login`과 같은 층위). `(app)/layout.tsx`가 무조건 헤더 내비를 렌더하므로, 그 안에 조건부 분기를 넣기보다 애초에 그 레이아웃을 안 타게 하는 쪽이 더 단순했다. 인증은 `src/proxy.ts`의 매처가 `_next/static`·`_next/image`·`favicon.ico`를 뺀 전 경로를 잡으므로 라우트 그룹과 무관하게 그대로 적용된다 — 이 라우트만을 위한 별도 인증 코드는 필요 없었다.
> - **화살표 방향**: PRD 예시(`민수 → 창민`, 승자→패자)는 v2.19 후속 수정에서 앱 전체를 패자→채권자(정산 화면과 동일한 "빚이 흐르는 방향") 관례로 통일하기 전에 쓰인 것이라, 그 관례를 따라 `창민 → 민수`(패자→승자) 순서로 구현했다.
> - **`verify-business-day.ts`에 8케이스 추가** (게임 밤 아님/자정 교차/무승부 참석자/미참석자 제외/소프트 삭제/레거시 점수/정렬/오늘 밤 스트릭) — `computeGameNightBoard`가 영업일 규칙에 가장 의존적인 함수라 이 파일에 둔다는 PRD 지시를 그대로 따름.

## 28. v2.21 요구사항 (대시보드 재구성 + 차트·기록 정비, 이번 작업 범위)

### 28.1 배경

v2.20의 게임밤 보드는 **"게임 밤이 진행 중일 때만" 나타나는 실시간 화면**으로 설계됐다. 실제로 써 보니 두 가지가 어긋났다.

1. **평소에는 아무것도 안 보인다.** 주 2회 모이는 그룹이라 대부분의 날은 게임 밤이 아니고, 그럴 때 대시보드 최상단이 비어 있다. 정작 알고 싶은 건 "그래서 지난번엔 어떻게 끝났나"인데 그건 화면 한참 아래 "최근 게임" 카드에 있었다.
2. **같은 하루를 두 카드가 나눠 갖고 있었다.** 게임밤 보드는 그날의 *집계*(승패·순점수·연승)를, "최근 게임" 카드는 같은 날의 *개별 기록*을 보여줬다. 둘이 항상 같은 날짜를 가리키는데 카드가 분리돼 있어 스크롤을 오가야 했다.

v2.21은 이 둘을 **"최근 경기일" 카드 하나로 합치고**, 실시간성을 낮추는(자동 갱신 → 수동 갱신) 대신 항상 무언가를 보여주는 쪽으로 성격을 바꾼다. `/tonight` 전체화면 모드는 폐기한다.

### 28.2 "최근 경기일" 카드 (v2.20 게임밤 보드 개편)

**대상 영업일**은 "오늘"이 아니라 **활성 게임이 있는 가장 최근 영업일**이다. 그래서 게임이 한 판이라도 기록돼 있으면 카드는 항상 나타난다.

| 상태 | 조건 | 표기 |
| --- | --- | --- |
| 진행 중 | 대상 영업일 == `todayInSeoul()` | `진행 중` 칩 — 그 경기일의 **경과** |
| 종료 | 대상 영업일 < `todayInSeoul()` | `종료` 칩 — 가장 최근 **종료된 경기일의 결과** |

- 제목: `최근 경기일 · 8/14 (목)` + 상태 칩
- 부제: 종목별 판수 + 총 판수 (기존 유지)
- 순위 행: 기존 게임밤 보드의 행(순위·이름·N승 M패·순점수·연승/연패)을 그대로 유지
- **"방금 N차전" 푸터는 폐기**한다. 그 자리를 아래 상세 목록이 대신한다.
- **"최근 게임" 카드를 흡수한다.** 그 경기일의 개별 게임 기록(종목 뱃지 · N차전 · 시각 · 패자→승자 · 점수 · 참가 인원)을 **접이식**으로 이 카드 안에 넣는다. 기본은 접힘, 펼치면 목록이 나온다.
- 접기/펼치기는 `<details>/<summary>`로 구현한다 — 이 카드는 서버 컴포넌트이므로 JS 없이 동작하고 키보드 접근성도 기본 제공된다. `<summary>`는 44px 이상 높이.
- 개별 기록의 시각은 **벽시계**(`gameWallClock`), 카드 제목의 날짜는 **영업일**이다(§22.4). 자정을 넘긴 판에는 기존 `GameNightBadge`를 유지한다.

기존 "최근 게임" 카드는 대시보드에서 **제거**한다(내용이 이 카드로 이동).

### 28.3 갱신 방식: 자동 30초 → 수동

v2.20의 `GameNightRefresher`(30초 폴링 + 탭 가시성 감지)를 **수동 갱신 버튼**으로 대체한다.

- 카드 헤더 우측(기존 `전체화면` 링크 자리)에 **`갱신` 버튼**
- 클릭 시 `router.refresh()`, `useTransition`으로 pending 상태 표시
- 버튼 옆에 마지막 갱신 시각을 조용히 표기 (`23:41 기준`). **서버에서 이미 포맷된 문자열을 prop으로 내려보낸다** — 클라이언트에서 `new Date()`로 만들면 hydration 불일치가 난다
- 자동 폴링 코드는 전부 삭제

### 28.4 `/tonight` 폐기

- `src/app/tonight/` 라우트 삭제
- `src/components/WakeLockKeeper.tsx` 삭제
- `GameNightBoardFull` 컴포넌트 삭제
- 카드의 `전체화면` 링크 삭제

### 28.5 대시보드 카드 순서

위에서 아래로 이 순서로 고정한다.

| # | 카드 | 변경 |
| --- | --- | --- |
| 1 | **최근 경기일** | §28.2 (신규 통합) |
| 2 | **순위 (누적 점수)** | 설명 문구 변경 (§28.6) |
| 3 | **정리된 채권-채무 관계** | 그대로 |
| 4 | **최근 경기일 요약** | 최근 3개 경기일 → **최근 7일 이내** (§28.7) |
| 5 | **최근 폼 & 스트릭** | 5게임 → **10게임**, "경기" → "게임" (§28.8) |
| 6 | **핫 / 콜드 플레이어** | 문구의 "경기" → "게임" |
| 7 | **이번 분기 티어** | 그대로 (최상단 → 최하단으로 이동) |

2·3번과 6번의 좌우 2단 그리드(`md:grid-cols-2`)는 유지해도 된다 — 데스크톱에서 좌→우로 읽는 순서가 위 순서와 같다.

### 28.6 순위 카드 설명 문구

```
현재: 게임 승/무/패 기준 — 과거 누적기록(조정)은 반영되지 않습니다.
변경: 게임 승/무/패 기준 — 앱 사용 이전 기록은 반영되지 않습니다.
```

"과거 누적기록(조정)"은 내부 기능 이름이라 처음 보는 사람에게 뜻이 통하지 않는다.

### 28.7 "최근 경기일 요약" — 최근 7일 이내

현재는 `RECENT_GAME_DAYS_COUNT = 3`으로 **최근 3개 경기일**을 보여준다. 이를 **오늘(영업일) 포함 최근 7일 이내에 게임이 있었던 날 전부**로 바꾼다.

- 기준일(`today`)은 함수 안에서 `todayInSeoul()`을 호출하지 말고 **인자로 받는다** — 테스트 가능해야 하고 영업일 규칙을 호출부와 공유해야 한다.
- 범위 판정은 `date >= addDaysToIsoDate(today, -6)` (오늘 포함 7일). ISO 날짜 문자열 비교로 충분하며, **`new Date(date)` 파싱을 쓰지 않는다**(§13.5).
- 개수 상한 없음. 최근 7일에 경기일이 없으면 빈 상태.
- 이 블록은 현재 `<Card>`가 아니라 별도 스타일의 `div`인데, 이제 다른 카드들 사이에 끼므로 **`<Card>` + `<SectionTitle>`로 통일**한다.

> **명칭 주의**: 1번 카드("최근 경기일")와 4번 카드("최근 경기일 요약")가 이름이 비슷하다. 4번에는 `최근 7일 이내에 게임이 있었던 날`이라는 설명을 붙여 역할 차이를 드러낸다. (1번은 경기일 **하나**의 상세, 4번은 **여러** 경기일의 한 줄 요약.)

### 28.8 "경기"와 "게임"의 구분

이 앱에는 두 개념이 있는데 지금 화면에서 섞여 쓰인다.

| 용어 | 뜻 |
| --- | --- |
| **경기일** | 영업일 하루 = 한 번의 모임(게임 밤) |
| **게임** | 개별 판 (1차전, 2차전 …) |

**"경기"를 단독으로 개별 판의 뜻으로 쓰지 않는다.** 전부 "게임"으로 바꾼다. `경기일`이라는 합성어는 그대로 둔다.

바꿔야 할 지점:

- 최근 폼 각주: `... 가장 최근 경기입니다 (최근 5경기).` → `... 가장 최근 게임입니다 (최근 10게임).`
- 핫/콜드 설명: `최근 14일간 3경기 이상 치른 참가자 ...` → `... 3게임 이상 ...`
- 저장소 전체에서 같은 용법이 남아 있는지 확인

그리고 최근 폼의 표본을 **5게임 → 10게임**으로 늘린다(`computeRecentForm(..., 10)`).

### 28.9 통계 — 참가자별 누적 순점수 추이

#### 28.9.1 추이 단위에 "게임별"·"분기별" 추가, 기본값은 "일별"

현재 이 차트의 추이 단위는 `일별 / 주별 / 월별 / 연도별`이고 기본값이 **주별**이다. 다음으로 바꾼다.

```
게임별 · 일별(기본) · 주별 · 월별 · 분기별 · 연도별
```

- **"게임별"** = 판마다 한 점. 누적 순점수는 판 단위로 움직이므로 이게 가장 원본에 가까운 뷰다.
- **"분기별"** = 티어 시스템의 분기와 같은 경계. **반드시 `quarterKeyOf()`(문자열 슬라이싱)를 쓴다** — `new Date(date)`로 파싱해 분기를 구하면 UTC 자정 이슈로 하루가 밀려 분기가 바뀔 수 있다(§13.5·§16의 기존 결론).
- **"게임별"은 이 차트에만 적용된다.** "기간별 게임 수 추이" 차트에서는 모든 버킷이 1이 되어 무의미하므로 옵션에서 제외한다. 두 차트의 옵션 목록과 기본값을 분리한다(누적 추이 기본 `일별`, 게임 수 추이 기본 `주별` 유지).

정렬은 **영업일 오름차순 → `withinDayKey` 오름차순**이다. 현재는 같은 날 안에서 `createdAt`으로 정렬하는데, 자정을 넘긴 판과 관리자가 시각을 교정한 판에서 앱의 다른 화면과 순서가 어긋날 수 있다.

#### 28.9.2 범례 폐기 → 선 끝 이름 라벨

하단 범례는 선이 8개일 때 어느 이름이 어느 선인지 매칭이 안 된다. 범례를 없애고 **각 선의 오른쪽 끝에 참가자 이름을 직접 붙인다.**

- **이름 색 = 선 색** (`buildParticipantColorMap`, v2.19 배치 C에서 이미 도입됨)
- 차트 오른쪽에 라벨용 여백을 확보한다 (`margin.right` ≈ 76px)
- **겹침 방지**: 두 참가자의 마지막 값이 가까우면 라벨이 겹친다. y좌표를 정렬한 뒤 최소 간격(≈14px)을 확보하도록 **위아래로 밀어서 배분**한다. 밀린 라벨과 실제 선 끝 사이에는 짧은 연결선을 그어도 좋다.
- 라벨 y좌표는 눈대중이 아니라 **차트의 실제 y 스케일에서 계산**해야 한다. recharts의 `<Customized>` 컴포넌트가 `yAxisMap`/`offset`을 넘겨주므로 `scale(value)`로 정확한 픽셀 위치를 얻을 수 있다.
- 이 변경은 **누적 순점수 추이 차트에만** 적용한다. 승/패 막대 차트의 범례(항목 2개)는 그대로 둔다.

### 28.10 통산기록 — 명예의 전당

#### 28.10.1 "최고/최저 순득점" → "하루 최고/최저 득실차"

현재 `highestNetPoints`/`lowestNetPoints`는 **통산 누적 순점수**의 최상·최하다. 이건 이미 대시보드 순위 카드가 보여주는 값과 같아서 명예의 전당에 둘 이유가 없고, "기록"이라기보다 "현재 순위"다.

**하루 단위 득실차**로 바꾼다 — 이쪽이 진짜 기록이다.

- 산식: 각 (참가자, 영업일) 쌍에 대해 `그날 딴 점수 − 그날 잃은 점수` (points 가중)
- 최댓값 → **하루 최고 득실차**, 최솟값 → **하루 최저 득실차**
- 그날 승·패가 하나도 없는 참가자(단순 참관)는 제외한다
- 기존 `mostWinsInOneDay`/`mostLossesInOneDay`와 같은 구조이므로 그 계산 옆에 나란히 둔다
- `startDate`/`endDate`에 해당 날짜를 넣어 기존 날짜 괄호 표기를 그대로 쓴다
- 부호 표기(`signed`)는 유지 — `+5점` / `-4점`
- 필드명도 함께 바꾼다: `highestNetPoints` → `bestDailyMargin`, `lowestNetPoints` → `worstDailyMargin`

#### 28.10.2 카테고리 순서

`최다 참석 (개근왕)`을 `하루 최다 게임수 (팀 기록)` 다음으로 옮긴다. 최종 순서:

1. 최장 연승
2. 최장 연패
3. 하루 최다 승리
4. 하루 최다 패배
5. **하루 최고 득실차**
6. **하루 최저 득실차**
7. 하루 최다 게임수 (팀 기록)
8. **최다 참석 (개근왕)** ← 이동
9. 역대 최고 채권 보유

### 28.11 성공 기준

- 게임이 하나라도 기록돼 있으면 대시보드 최상단에 **"최근 경기일" 카드가 항상 보인다.** 오늘 게임이 있으면 `진행 중`, 없으면 마지막 경기일 + `종료`.
- 그 카드의 접이식 상세를 펼치면 예전 "최근 게임" 카드와 같은 내용이 나오고, 대시보드에 "최근 게임" 카드는 더 이상 없다.
- 자동 갱신이 없다 — 개발자도구 Network 탭에서 방치했을 때 요청이 발생하지 않고, `갱신` 버튼으로만 최신화된다.
- `/tonight`이 404이고 `WakeLockKeeper`가 저장소에 없다.
- 카드 순서가 §28.5 표와 같다.
- "최근 경기일 요약"이 최근 7일 이내 경기일을 전부 보여준다(3개 상한 없음).
- 최근 폼이 10게임을 보여주고, 390px에서 뱃지 10개가 레이아웃을 깨뜨리지 않는다.
- 저장소 전체에서 개별 판을 뜻하는 "경기" 표기가 없다(`경기일`은 유지).
- 누적 순점수 추이의 기본값이 `일별`이고, `게임별`·`분기별`이 동작한다. 분기 경계가 티어 화면의 분기와 일치한다.
- 그 차트에 범례가 없고, 선 오른쪽 끝에 선과 같은 색의 이름이 겹치지 않게 표시된다.
- 명예의 전당에 `하루 최고 득실차`/`하루 최저 득실차`가 있고 `최고/최저 순득점`이 없으며, 카테고리 순서가 §28.10.2와 같다.
- 스키마 변경·새 환경변수·새 의존성 없음.
- `npm run build`, `npm run lint`, `npm run verify:design`, `npm run verify:business-day`, `npm run verify:tiers`, `npx tsx scripts/verify-settle.ts` 통과.

> **구현 완료 — 2026-08-16.**
> - **`computeGameDayBoard`**(`src/lib/stats.ts`): `businessDate` 인자를 없애고 함수 내부에서 활성 게임의 `date` 최댓값으로 대상 영업일을 스스로 찾는다. `latestGame`/`latestSequence`를 `games: {game, sequence}[]`(withinDayKey 내림차순)로 교체 — 접이식 상세 목록이 이 배열을 그대로 순회한다.
> - **recharts 3.x — `<Customized>`는 이 버전에서 이미 폐기(deprecated)됨.** `node_modules/recharts/types/component/Customized.d.ts`에 "Starting from Recharts 3.x, all charts are able to render arbitrary elements anywhere, and Customized is no longer needed" 라고 적혀 있다. 대신 `useYAxisScale()`/`usePlotArea()`(recharts 3.8+) 훅을 쓰는 컴포넌트를 `<LineChart>`의 자식으로 직접 렌더했다 — PRD가 제안한 `<Customized component={...}>` + `yAxisMap`/`offset` prop 방식은 이 버전엔 없다.
> - **상태 칩**: `GameDayStatusChip`을 `badges.tsx`에 추가(재사용 가치가 있다고 판단). "진행 중"은 `bg-win/15 text-win border-win/30`, "종료"는 중립(`bg-surface-raised text-content-muted`) — 기존 `StreakBadge` 등의 톤과 통일.
> - **`<summary>` 라벨**: "게임 상세 N판 보기" / "게임 상세 접기"(N은 `board.totalGames`). `list-style:none` + `[&::-webkit-details-marker]:hidden`으로 기본 마커를 지우고 `ChevronDown`을 직접 그려 `group-open:rotate-180`으로 회전시켰다.
> - **이름 라벨**: 6자 초과 시 `…`로 자름, 겹침 회피로 실제 위치에서 2px 넘게 밀렸을 때만 같은 색 연결선을 그린다(늘 그리면 안 밀린 라벨까지 지저분해진다).
> - **최근 폼 뱃지**: 390px에서 뱃지 10개 + 이름 + StreakBadge가 한 줄에 빠듯해, `ResultBadge` 크기를 줄이는 대신 뱃지 줄을 이름 아래 자기 줄로 내렸다(§28.8) — 크기를 줄이면 20px→16px 정도라 여전히 빠듯하고 터치 대상도 작아지는데, 줄바꿈은 폭 제약 자체를 없앤다.
> - **명예의 전당 카테고리 순서**: `RecordsSummary`의 필드 선언 순서도 §28.10.2 표시 순서에 맞춰 정리했다(선언 순서가 렌더 순서를 강제하진 않지만, 다음에 읽는 사람이 헷갈리지 않게).

## 30. v2.22 요구사항 (통계 차트 여백 정리 + 누적 추이 시작점, 이번 작업 범위)

### 30.1 문제 1 — 그래프 좌우 여백이 과하다

`/stats`의 세 차트(**참가자별 승/패**, **참가자별 누적 순점수 추이**, **기간별 게임 수 추이**)가 카드 폭을 제대로 쓰지 못한다. 390px에서 카드 안쪽 폭이 318px 남짓인데, 실제 플롯 영역은 그중 60% 남짓이다.

실측된 원인은 하나가 아니라 여러 개가 겹친 것이다.

| 원인 | 크기 | 설명 |
| --- | --- | --- |
| **`<YAxis>`의 기본 `width={60}`** | **60px** | 가장 큰 원인. 이 앱의 Y축 값은 `0`·`3`·`-5` 같은 한두 자리인데 60px를 통째로 차지한다 |
| `<LineChart>`의 `margin.right={76}` | 76px | v2.21 §28.9.2가 선 끝 이름 라벨용으로 확보한 여백. 한글 이름이 2~3자라 과하다 |
| recharts 기본 `margin` | 좌우 5px씩 | 명시하지 않아 기본값이 적용 중 |
| 카테고리 축의 양 끝 반(半)밴드 | 밴드 폭의 절반씩 | 라인 차트에서 첫·마지막 점이 플롯 가장자리에 닿지 않고 안쪽으로 들어와 있다 |
| `<Card>`의 `p-4 sm:p-5` | 좌우 16~20px씩 | 카드 자체의 패딩 |

요구사항:

1. **세 차트 모두 `<YAxis width>`를 실제 필요한 만큼으로 줄인다.** recharts 3.x는 `width="auto"`를 지원하므로 우선 그걸 쓰고, 설치된 버전이 지원하지 않으면 고정값(양수만 나오는 차트는 `28`, 음수가 나올 수 있는 누적 추이는 `36` 정도)으로 둔다.
2. **세 차트 모두 `margin`을 명시**해 좌우를 최소화한다 (`left: 0`, `right`는 아래 3번에 따름).
3. **누적 추이의 이름 라벨 여백을 데이터에 맞춰 계산**한다. 고정 76px 대신 실제로 표시될 라벨의 최대 길이에서 산출하고(대략 `8 + 글자수 × 11`, 상한 64px), 라벨 글자 수 제한도 함께 줄인다.
4. **누적 추이 라인 차트는 첫·마지막 점이 플롯 좌우 끝에 닿게** 한다 (`scale="point"` + `padding={{ left: 0, right: 0 }}`). 막대 차트는 밴드 스케일이 필요하므로 이 방식을 쓰지 말고, 대신 `barCategoryGap`을 조금 줄이는 선에서 정리한다.
5. **차트 컨테이너를 카드 패딩 바깥으로 약간 확장**(`-mx-*` 형태의 부분 블리드)해 카드 좌우 패딩분을 회수하는 것을 검토한다. 단 `<Card>`의 공용 패딩 토큰(§24.2)은 바꾸지 않는다 — 차트 래퍼에서만 상쇄한다.

**목표**: 390px에서 플롯 영역이 카드 안쪽 폭의 **85% 이상**을 차지한다.

### 30.2 문제 2 — 누적 순점수 추이가 "첫 승/패"부터 그려진다

`computeCumulativeNetPointsTrend`의 누적 맵(`running`)은 **`winnerId`/`loserId`로만** 키가 생긴다.

```ts
running.set(g.winnerId, (running.get(g.winnerId) ?? 0) + points);
running.set(g.loserId,  (running.get(g.loserId)  ?? 0) - points);
```

그래서 게임에 **참석은 했지만 아직 이기지도 지지도 않은 참가자**는 맵에 키 자체가 없고, 그 결과:

- 그 사람의 선은 **첫 승 또는 첫 패가 나오는 시점부터** 그려진다
- 조회 기간 내내 무승부만 한 참가자는 **아예 선이 없다**

이는 의도가 아니다. **참가자는 자신의 첫 참석(무승부라도) 이후부터 순점수 0에서 출발해 선이 그려져야 한다.**

수정 방향: 각 게임을 처리할 때 승패를 반영하기 **전에** `g.attendeeIds`를 순회하며 아직 맵에 없는 참가자를 **0으로 초기화**한다. 이러면

- 첫 참석 시점부터 선이 0에서 시작한다
- 무승부만 한 참가자는 0에 붙은 수평선으로 표시된다
- 참석하기 전 구간에는 여전히 키가 없으므로 선이 그려지지 않는다 (의도대로)

`grouping === "game"`과 버킷 기반 경로 **양쪽 모두**에 적용해야 한다.

부수 효과:

- 선 개수가 늘어 §28.9.2의 선 끝 이름 라벨이 더 자주 겹친다. 겹침 배분 로직은 이미 있으므로 동작만 확인하면 된다. 0 근처에 여러 명이 몰릴 수 있다는 점만 유의.
- 참가자 명단에 없는 id(하드 삭제된 참가자)가 `attendeeIds`에 남아 있으면 이제 선으로 나타날 수 있다. 이런 id는 색도 없고 의미도 없으므로 **차트에서 제외**한다.

### 30.3 적용 범위 밖

- 상대 전적 히트맵, 성향 맵(`/records`), 대시보드의 어떤 것도 건드리지 않는다.
- 승/패 막대 차트의 범례(항목 2개)는 그대로 둔다 — 읽기 문제가 없다.
- 집계 산식은 바뀌지 않는다. 순점수 값 자체는 이전과 동일하고, **언제부터 그려지는지**만 달라진다.

### 30.4 성공 기준

- 390px에서 세 차트 모두 플롯 영역이 카드 안쪽 폭의 85% 이상을 차지한다.
- 누적 추이 차트에서 첫 점과 마지막 점이 플롯 좌우 끝에 닿는다.
- 조회 기간 내에 참석만 하고 승패가 없는 참가자가 **0에 붙은 수평선**으로 표시된다.
- 어떤 참가자든 자신의 **첫 참석 게임**부터 선이 시작한다(첫 승/패가 아니라).
- 참가자 명단에 없는 id는 차트에 나타나지 않는다.
- 순점수 값 자체는 이전과 동일하다.
- 스키마 변경·새 환경변수·새 의존성 없음.
- `npm run build`, `npm run lint`, `npm run verify:design`, `npm run verify:business-day`, `npm run verify:tiers`, `npx tsx scripts/verify-settle.ts` 통과.

> **구현 완료 — 2026-08-16.**
> - **`width="auto"`**: `node_modules/recharts/types/state/cartesianAxisSlice.d.ts`의 `YAxisWidth = number | 'auto'`로 3.10.1이 지원함을 확인하고 세 차트 모두 적용 — 고정값 분기(양수 28 / 음수 36)는 쓰지 않았다.
> - **390px 실측 방법**: 이 세션의 브라우저 자동화 도구가 실제 뷰포트 리사이즈를 지원하지 않아(`resize_window` 호출은 성공을 반환하지만 스크린샷 해상도가 안 바뀜), `width:390px`짜리 `<iframe>`을 페이지에 주입해 그 안에서 `/stats`를 새로 로드하는 방식으로 진짜 390px CSS 뷰포트를 만들어 측정했다(iframe은 자신의 미디어 쿼리 컨텍스트를 가지므로 Tailwind `sm:` 브레이크포인트가 정확히 안 걸린다). `.recharts-cartesian-grid`와 카드(`.rounded-2xl`)의 `getBoundingClientRect()` 폭을 직접 재서 비율을 계산했다.
> - **부분 블리드로는 부족했다**: `-mx-2 sm:-mx-3`로 처음 측정했을 때 승/패 87.6%, 게임 수 85.5%는 넘겼지만 누적 순점수 추이는 79.1%에 그쳤다(이름 라벨 여백 때문에 셋 중 오른쪽 여백이 가장 크다). `<Card>`의 `p-4 sm:p-5`와 정확히 상쇄되는 전체 블리드(`-mx-4 sm:-mx-5`)로 세 차트를 통일하고, `cumulativeLabelMargin`의 하한도 32→28px, 글자당 폭도 11→10px로 같이 낮춰 최종 92.3% / 85.0% / 90.3%를 확인했다. 누적 추이가 정확히 경계값(85.0%)이라 여유가 크진 않지만, 이름 길이에 따라 자동으로 늘어나는 여백 계산 자체는 그대로이므로 더 긴 이름이 오면 여백도 같이 늘어난다.
> - **라벨 폰트 10px**: 원래 11px에서 줄였다. iframe 스크린샷으로 실제 렌더를 확인한 결과 참가자 색(팔레트가 전부 대비 3:1 이상, `verify-design-tokens.ts` 체크 9)로 칠해진 10px 텍스트가 읽는 데 지장이 없어 그대로 채택했다 — 안 읽혔다면 11px로 되돌리고 여백만 줄이라는 지시를 따랐을 것이다.
> - **`computeCumulativeNetPointsTrend`**: `grouping === "game"`과 버킷 경로 둘 다에 쓰는 `seedAttendees(g)` 헬퍼 하나로 attendeeIds 초기화를 공유해, 한쪽만 고치는 사고를 원천적으로 막았다.

## 32. v2.23 요구사항 (탄소배출권 리브랜딩 + 운영원칙 + UX 마이너, 이번 작업 범위)

### 32.1 배경과 원칙

이 앱은 게임 결과로 오가는 점수를 **채권-채무**로 표현하고, 그걸 **정산**하며, **금액**을 다루고, 점수를 **상품으로 교환**한다고 설명한다. 기능적으로는 정확하지만 읽는 사람에 따라 **금전 도박의 장부**처럼 읽힌다.

**기능은 하나도 바꾸지 않는다.** 데이터 모델, 잔액 계산, 정산 알고리즘, 티어 산식 전부 그대로다. 바뀌는 것은 **그 위에 씌우는 세계관과 용어**뿐이다.

**새 세계관**: 점수는 **탄소배출권**이다.

- **1점 = 10kg CO₂e 배출권**
- 게임에서 진 사람은 이긴 사람에게 **배출권을 넘겨야 한다** (의무 발생)
- 보유량이 마이너스면 **실제 감축 행동을 인증**해 배출권을 확보해야 한다
- 감축 행동 목록과 인증 규칙은 새 **운영원칙** 화면(§32.4)에 둔다

이 세계관은 기존 데이터 모델과 **정확히 대응한다**. `payment`(실제 정산)는 "배출권을 실제로 넘긴 기록", `donation`(기부)은 "자발적 양도", `adjustment`(과거 누적기록)는 "앱 도입 이전 이월분"이다. 억지로 끼워 맞추는 게 아니라 이름만 갈아입는다.

### 32.2 용어 대응표

**이 표가 이번 작업의 사양이다.** 왼쪽이 화면에 남아 있으면 실패다.

| 현재 | 변경 | 비고 |
| --- | --- | --- |
| 채권-채무 관계 | **배출권 이전 관계** | |
| 정리된 채권-채무 관계 | **정리된 이전 계획** | |
| 채권자 (받을 사람) | **받을 사람 (배출권 잉여)** | |
| 채무자 (빚진 사람) | **넘길 사람 (배출권 부족)** | |
| 빚 / 빚졌는지 | **부족분 / 얼마를 넘겨야 하는지** | |
| 정산 (탭 이름) | **배출권** | 탭은 짧아야 함 |
| 정산 (동작·화면 제목) | **배출권 이전** | |
| 실제 정산 | **배출권 이전** | `payment` 라벨 |
| 정산 완료 | **이전 완료** | |
| 정산할 내역이 없습니다 | **넘길 배출권이 없습니다** | |
| 정산 & 조정 이력 | **이전 & 이월 이력** | |
| 금액 | **수량** | 입력 라벨·플레이스홀더·에러 문구 전부 |
| 잔액 / 순 잔액 | **보유량 / 순 보유량** | |
| 거래 / 최소 거래 수 | **이전 / 최소 이전 횟수** | |
| 전액 | **전량** | |
| 갚은 사람 / 받은 사람 | **넘긴 사람 / 받은 사람** | |
| 대신 현금으로 지불 | **대신 넘겨줌** | 메모 플레이스홀더 |
| 점수를 상품으로 교환했다면 | **감축 행동을 인증했다면** | §32.4로 링크 |
| 딴 점수 / 잃은 점수 | **받은 배출권 / 넘긴 배출권** | 통계·게임 목록 표 헤더 |
| 순점수 | **순증감** | |
| 판돈(점수) | **점수 규모** | 티어 각주 |
| 본전 | **기준선** | 성향 맵 |
| 불나방 | **모험가** | 성향 맵 사분면 |
| 조공러 | **기여자** | 성향 맵 사분면 |
| 역대 최고 채권 보유 | **역대 최대 잉여 보유** | 명예의 전당 |
| 과거 누적기록 | **이월 기록** | 화면 제목·탭 |
| 앱 title `게임 장부` | **배출권 장부** | metadata + 로그인 화면 |
| 앱 description | **게임 결과에 따른 탄소배출권 이전을 기록·관리하는 앱** | |

**유지하는 것**: `점`(단위 — 1점 = 10kg으로 §32.4에서 환산 설명), `기부`(자발적 양도라는 뜻이 그대로 통하고 금전 연상이 없음), `승부사`·`실속파`(스포츠 용어), 티어 이름(브론즈~챌린저), `천적`/`밥`, `득실차`.

> `점` 단위를 유지하는 이유: 모든 계산·저장·검증 스크립트가 정수 점수를 전제로 하고, kg로 바꾸면 화면마다 ×10 환산이 끼어들어 버그 표면이 넓어진다. 환산은 §32.4에서 한 번만 설명하고, 주요 지점(보유량 카드)에서만 `12점 (120kg)` 형태로 병기한다.

### 32.3 코드 주석과 내부 식별자

- **사용자에게 보이는 문자열은 전부** 위 표대로 바꾼다.
- **내부 식별자**(`Settlement`, `settlements`, `payment`, `donation`, `adjustment`, 라우트 `/settlements`, DB 컬럼)는 **바꾸지 않는다.** 스키마·URL 변경은 이번 범위 밖이고, 리스크 대비 실익이 없다.
- **코드 주석**은 억지로 바꾸지 않는다. 다만 `settle.ts`처럼 debt/creditor/debtor 용어로 도메인을 설명하는 doc 주석에는 **"화면 표기는 배출권 이전 용어를 쓴다(§32.2)"** 한 줄만 덧붙여 다음 사람이 헷갈리지 않게 한다.

### 32.4 새 화면 — 운영원칙 (`/principles`)

**통산기록 다음** 탭으로 신설한다. 정적 콘텐츠 화면이며 데이터 접근이 없다.

#### 32.4.1 구성

1. **환산 규칙** — `1점 = 10kg CO₂e 배출권`
2. **의무 발생** — 게임에서 진 사람은 이긴 사람에게 해당 수량의 배출권을 넘긴다
3. **부족 시 확보 방법** — 보유량이 마이너스면 아래 감축 행동을 실제로 수행하고 인증해 배출권을 확보한다
4. **감축 행동 목록** (§32.4.2)
5. **인증 규칙** — 사진·기록·영수증 등으로 인증하면 해당 수량을 확보한 것으로 보고, 배출권 화면에서 이전 처리한다
6. **면책 문구** — 계수는 국내 평균 기준 **근사치**이며 공식 산정값이 아니다

#### 32.4.2 감축 행동 목록 (1점 = 10kg 기준)

난이도 3단계로 묶고, 각 항목에 산출 근거를 작게 병기한다.

**이동**

| 행동 | 근거 |
| --- | --- |
| 자동차 대신 걷거나 자전거로 **62km** 이동 | 승용차 1km ≈ 0.16kg |
| 왕복 20km 자전거 출퇴근 **3일 연속** | 위와 동일 |
| 국내선 비행기 대신 **KTX·버스로 1회 이동** | 김포–제주 편도 ≈ 100kg → **10점** |
| 엘리베이터 대신 계단으로 **누적 500층** | 승강기 운행 전력 기준 |

**냉난방·전기**

| 행동 | 근거 |
| --- | --- |
| 한여름 에어컨 **15시간** 끄고 버티기 | 1.5kW × 0.459kg/kWh ≈ 0.69kg/시간 |
| 겨울 난방 설정온도 **2℃ 낮추고 2주** | |
| 전기건조기 대신 **자연건조 7회** (널고 걷기 포함) | 1회 3kWh ≈ 1.38kg |
| 대기전력 전부 뽑고 **40일 유지** | 가구 평균 대기전력 기준 |
| 전기 **22kWh 절약** | 한전 배출계수 0.459kg/kWh |

**물·온수**

| 행동 | 근거 |
| --- | --- |
| 온수 샤워 대신 **냉수 샤워 6회** | 10분 온수 샤워 ≈ 1.8kg |
| 세탁을 **찬물로 17회** | 1회당 ≈ 0.6kg 절감 |

**식사·소비**

| 행동 | 근거 |
| --- | --- |
| 소고기 **1인분(200g)** 을 채식으로 대체 | 소고기 1kg ≈ 60kg → **1.2점** |
| 배달 음식 **20회** 참고 직접 조리·설거지 | 1회 포장·배송 ≈ 0.5kg |
| 새 티셔츠 **2벌** 안 사기 | 1벌 생산 ≈ 6kg |
| 새 청바지 안 사고 **수선해서 입기** | 1벌 생산 ≈ 29kg → **약 3점** |

**흡수 (장기)**

| 행동 | 근거 |
| --- | --- |
| 나무 **1그루 심고 1년 6개월 관리** | 30년생 소나무 연간 흡수 ≈ 6.6kg |

문구 톤은 "고생스럽다"는 점이 드러나게 쓴다 — 예: *"62km를 걸어야 1점입니다. 하루에 안 됩니다."*

### 32.5 UX — 헤더 정리와 관리자·로그아웃 이동

탭이 6개(대시보드·게임·배출권·통계·통산기록·운영원칙)가 되면서 390px 헤더에서 가로 스크롤 없이는 다 안 보인다. 반면 `관리자`·`로그아웃`은 거의 쓰지 않는데 헤더 우측을 고정으로 점유하고 있다(`shrink-0`).

- **헤더에는 일반 탭 6개만 남긴다.** 우측 버튼 묶음(Admin 링크/관리자 종료/로그아웃)을 전부 제거한다.
- 6개가 한 줄에 안 들어가면 **가로 스크롤 대신 `flex-wrap`으로 2줄 배치**한다. 스크롤은 "더 있다"는 신호가 약해서 탭을 못 찾게 만든다.
- **푸터를 신설**하고 거기에 `관리자 모드` 진입/종료, `로그아웃`, 그리고 **관리자 전용 링크(참가자·이월 기록·롤백)** 를 모은다. 관리자 탭 3개도 헤더에서 내려간다.
- 푸터는 `<main>` 아래, 페이지 맨 끝. 터치 타깃 44px 규칙(§24.11)은 그대로 지킨다.

### 32.6 UX — 새 게임 기록 버튼 단일화

`기록하고 계속 입력`(v2.19 §24.9)은 실사용 빈도가 낮았다.

- **`기록하고 계속 입력` 버튼을 화면에서 제거**한다.
- 남는 **`기록하고 목록으로`를 `variant="primary"`** 로 바꾼다(현재 `neutral`이라 잘 안 보임). 지금 `기록하고 계속 입력`이 쓰던 색이다.
- 그 버튼에 `pending`/`pendingText="기록 중..."` 을 옮겨 붙인다 — 현재 `neutral` 쪽에는 pending 표시가 없어서 저장 중 아무 반응이 없다.
- 연속 입력 경로가 사라지므로 딸려 있던 **세션 배너(`이번에 N판 기록됨`)와 `UndoStack`, `sessionCount` 상태도 함께 제거**한다. 목록으로 이동하면 어차피 보이지 않는다.
- `취소` 버튼은 그대로 둔다.

### 32.7 통계 — 승률 각주

`순위표` 카드에는 설명이 없어서 승률A/B의 차이를 알 수 없다. 표(데스크톱)와 카드(모바일) **양쪽 아래**에 각주를 단다.

```
승률A = 승 ÷ (승 + 패)   ·   승률B = 승 ÷ (승 + 무 + 패)
```

### 32.8 통계 — 천적 / 밥을 마진 기준으로

**현재는 모순이 생긴다.** `computeNemesisAndVictim`이 천적을 `pointsLost` 최대(총량), 밥을 `pointsWon` 최대(총량)로 뽑는데, 둘 다 **절대량**이라 **가장 많이 붙은 상대가 천적이자 밥이 될 수 있다.**

**마진(순증감) 기준으로 바꾼다.**

- 상대별 `margin = pointsWon − pointsLost`
- **천적** = margin이 가장 작은(가장 음수) 상대. `margin < 0`인 상대가 없으면 `null`
- **밥** = margin이 가장 큰(가장 양수) 상대. `margin > 0`인 상대가 없으면 `null`
- margin은 상대마다 하나뿐이므로 **같은 사람이 천적이자 밥이 되는 일이 구조적으로 불가능**해진다
- 동률 시: 총 유통량(`pointsWon + pointsLost`)이 큰 쪽 우선(표본이 두꺼운 쪽), 그것도 같으면 이름순
- 표시값도 마진으로: `민수 (-5)` / `지훈 (+7)`
- 설명 문구: `천적 = 마진이 가장 나쁜 상대, 밥 = 마진이 가장 좋은 상대 (마진 = 받은 배출권 − 넘긴 배출권)`
- 인터페이스 필드명도 `pointsLost`/`pointsWon` → **`margin`** 으로 통일

### 32.9 통산기록 — 티어 지표 설명

분기 티어 카드에 TR·승 지수·패 지수가 숫자로만 있어 뜻이 전달되지 않는다. 기존 각주(3줄)가 이미 길므로, **접이식(`<details>`)** 으로 설명을 추가한다 — v2.21에서 쓴 패턴과 같다.

담을 내용:

- **TR 1000 = 기대치대로 한 상태.** 참석 인원수로 계산한 기대 성과와 정확히 일치하면 1000이다.
- **TR ±100 ≈ 기대 대비 성과가 약 20%p 차이.** 티어 컷 간격이 약 90점이므로 **TR 100은 대략 티어 한 계단**이다.
- **승 지수** = 이긴 판에서 얻은 점수 ÷ 기대 점수. `1.00`이면 기대만큼, `1.30`이면 기대보다 30% 더 이겼다는 뜻.
- **패 지수** = 진 판에서 잃은 점수 ÷ 기대 점수. `1.00`이면 기대만큼이고 **낮을수록 좋다.**
- **기대 점수**는 참석 인원수로 정해진다 — 4인전이면 이길 기대도 질 기대도 25%, 5인전이면 20%.
- 표본이 적으면 신뢰도가 낮아 TR이 1000 쪽으로 당겨진다.

기존 각주의 `판돈(점수)` 은 §32.2대로 `점수 규모`로 바꾼다.

### 32.10 성공 기준

- `rg "채권|채무|정산|금액|잔액|판돈|불나방|조공러|상품으로 교환" src/` 결과에 **사용자에게 보이는 문자열이 0건**(코드 주석·내부 식별자는 예외).
- 앱 제목·설명·로그인 화면에 금전/도박 연상 표현이 없다.
- `/principles` 탭이 통산기록 다음에 있고, 1점 = 10kg 규칙과 16개 감축 행동, 인증 규칙, 면책 문구가 있다.
- 390px에서 일반 탭 6개가 **가로 스크롤 없이** 보인다(한 줄 또는 두 줄).
- 헤더에 관리자·로그아웃 버튼이 없고, 푸터에서 관리자 진입/종료·로그아웃·관리자 전용 링크에 접근할 수 있다.
- 새 게임 기록 확인 단계에 버튼이 **`기록하고 목록으로`(primary)와 `취소` 둘뿐**이고, 저장 중 `기록 중...`이 뜬다.
- 순위표 아래에 승률A/B 산식 각주가 데스크톱·모바일 모두에 있다.
- **어떤 참가자에게도 같은 사람이 천적이자 밥으로 나오지 않는다.**
- 분기 티어에서 TR·승 지수·패 지수의 뜻을 화면에서 읽을 수 있다.
- 기능·산식·스키마·라우트·환경변수 변경 없음. 잔액·티어·통계 **숫자가 이전과 완전히 동일**하다(천적/밥 제외).
- `npm run build`, `npm run lint`, `npm run verify:design`, `npm run verify:business-day`, `npm run verify:tiers`, `npx tsx scripts/verify-settle.ts` 통과.


## 33. 다음 단계

- `CLAUDE_CODE_PROMPT_V2_23_CARBON_REBRAND.md` — 탄소배출권 리브랜딩 + 운영원칙 + UX 마이너 (§32)
- `RELATIONSHIP_ANALYSIS_FRAMEWORK.md` — 기부 × 상대 전적 관계 분석 프레임워크 (제안 단계, 미채택)
- `UX_REVIEW_2026-08.md` — v2.18 코드 실측 기반 UX/UI 진단 원본 (§24의 근거)
- `FUN_FEATURES_PROPOSAL.md` — 재미 요소 제안 모음 (§26은 이 중 A-1만 채택한 것)

### 완료된 작업 프롬프트

- `CLAUDE_CODE_PROMPT_V2_22_CHART_MARGINS.md` — 차트 여백 정리 + 누적 추이 시작점 (§30)
- `CLAUDE_CODE_PROMPT_V2_21_DASHBOARD_REWORK.md` — 대시보드 재구성 + 차트·기록 정비 (§28)
- `CLAUDE_CODE_PROMPT_V2_20_GAME_NIGHT_BOARD.md` — 라이브 게임밤 보드 (§26)
- `CLAUDE_CODE_PROMPT_V2_19A_FOUNDATION.md` — 디자인 기반 (§24.3·24.4·24.6·24.7·24.8)
- `CLAUDE_CODE_PROMPT_V2_19B_CONVENIENCE.md` — 편의성 (§24.9~24.12)
- `CLAUDE_CODE_PROMPT_V2_19C_POLISH.md` — 표시·차트 정비 (§24.5·24.13)
- `CLAUDE_CODE_PROMPT_V2_18_DISPLAY_WALLCLOCK.md` — 영업일과 표시 시각의 분리 (§22)
- `CLAUDE_CODE_PROMPT_V2_17_BUSINESS_DAY_BACKFILL.md` — 영업일 규칙 소급 적용 (§20)
- `CLAUDE_CODE_PROMPT_V2_15_TIER.md` — 티어 시스템 재설계 (§16)
- `CLAUDE_CODE_PROMPT_V2_14.md` — 2시간 수정·삭제 유예 (§15)
