# 게임 장부 (hoola_ledge) — PRD v2.18

- 최초 작성: 2026-08-13 / 최종 개정: 2026-08-15 (v2.18)
- 저장소: https://github.com/EDH1026/hoola_ledge
- 배포: Vercel 프로젝트 `hoola` (도메인: `hoola-three.vercel.app`), 팀 `HYSNESTR`
- 문서의 목적: 현재 구현된 기능/구조를 정리하고 다음 릴리스의 범위를 확정하여 Claude Code 작업의 기준 문서로 사용

### 버전 이력

| 버전 | 내용 |
| --- | --- |
| v1.0 | 최초 구현 — 참가자 풀, 게임 기록(드래그 앤 드롭), 최소 거래 정산, 통계, 공유 비밀번호 인증, Vercel 배포 |
| v2.0 | 관리자 모드, 과거 누적기록, 게임 종목/시간 자동기록/점수/N차전, 게임 소프트 삭제, Win-Lose 용어 통일, 정산 카드 UI + 기부, 상대 전적, 기부 랭킹, 관리자 롤백 (구현 완료) |
| v2.10 | 버그 수정(모바일 드래그, 기부 방향, 관리자 탭 노출, 정산 안전장치) + 게임 구간별 인별 점수 합계 + 이스포츠 스타일 통계/대시보드 확장 (구현 완료) |
| v2.11 | 저장소를 Vercel Blob(JSON 파일)에서 Supabase(Postgres)로 전환 (구현 완료) |
| v2.12 | 게임 기록을 관리자 전용으로 수정(edit)할 수 있는 기능 추가 — §11 (구현 완료) |
| v2.13 | 승무패/승률A·B 표기, 기록실 top3·공동순위 개편, 통계 커스텀 기간, 게임 목록 기본 필터를 오늘로, 관리자 완전삭제, 변제(자유 정산) — §13 (구현 완료, 스키마 변경 없음) |
| v2.14 | 게임 기록 수정·삭제를 일반 이용자에게도 개방(날짜·시간 제외), 기록 후 2시간이 지나면 수정·삭제·정산취소 불가 — §15 (관리자는 시간 제한 예외) |
| v2.15 | 티어 시스템 전면 재설계 — 승률A 컷 방식을 폐기하고 기대 대비 성과(승 지수·패 지수) 기반 분기별·종목별 티어로 교체, 개인별 성향은 뱃지 대신 최근 90일 롤링 2축 산점도(성향 맵)로 표현 — §16 (구현 완료, 스키마 변경 없음) |
| v2.16 | **하루의 재정의(06:00~30:00, 24시간 표기 통일)** + 대시보드·새 게임 기록·정산·통계 화면 전면 개편 + **통산기록 탭 신설** — §18 (구현 완료, 스키마 변경 없음) |
| v2.17 | **v2.16 영업일 규칙의 기존 데이터 소급 적용(백필)** — 앞으로 기록될 게임에만 적용되던 06:00 경계를 이미 저장된 기록에도 적용해, 자정을 넘겨 기록된 과거 게임이 실제 게임 밤으로 되돌아가게 한다 + 관리자 수정 화면의 영업일 정합성 경고 — §20 (구현 완료, 스키마 변경 없음) |
| v2.18 | **영업일과 표시 시각의 분리** — 그룹핑·필터는 영업일(06:00 경계)을 쓰되, 개별 기록의 타임스탬프는 실제 달력 벽시계로 표시한다. 8/15 01:00 게임은 8/14로 묶이되 화면에는 "8/15 01:00"으로 나온다 — §22 (구현 완료, 스키마 변경 없음) |

## 1. 제품 개요

매번 참석자가 달라지는 소규모 모임 게임(보드게임 등)에서, 각 게임의 결과를 기록하고 그로부터 발생하는 채권-채무 관계를 자동으로 정리해주는 웹 앱이다. 참가자 풀은 고정되어 있지만 게임마다 실제 참석자는 그중 일부로 달라진다. 접근은 팀 전체가 공유하는 비밀번호로 제한되며, 장부 정합성에 영향을 주는 기능은 별도의 관리자 비밀번호로 한 번 더 보호된다.

### 핵심 규칙

- 매 게임마다 전체 등수가 아니라 **Win(1위)과 Lose(최하위)만** 기록한다.
- **Lose가 Win에게 해당 게임의 점수를 지급**한다. 점수는 기본 1점이며 게임별로 다르게(예: 2점) 지정할 수 있다. 점수는 소정의 상품으로 교환 가능하다.
- 게임 결과 입력 UI는 Lose 참가자 카드를 Win 참가자 카드 위로 **드래그 앤 드롭**(또는 탭-투-탭)하는 방식이며, 확인 후 결과가 기록되고 채권-채무 장부가 갱신된다.
- 새 게임을 등록할 때 참석자 선택의 기본값은 **직전 게임의 참석자와 동일**하며, 필요 시 체크박스로 변경할 수 있다.
- 게임 날짜·시각은 사용자가 입력하지 않고 **기록하는 순간의 서버 시각(Asia/Seoul)이 그대로 저장**된다. 이 값은 관리자만 예외적으로 고칠 수 있다(§11).
- **하루는 06:00~30:00(다음 날 06:00)을 한 단위로 본다(v2.16, §18.1).** 게임 밤이 자정을 넘겨 이어지는 걸 반영한 것으로, 08/15 04:00에 기록된 게임은 08/14의 기록이고 08/15 07:00에 기록된 게임은 08/15의 기록이다. 이 정의는 게임 날짜, "오늘" 필터·요약, N차전 순서, 분기 경계 등 **앱 전체에 공통으로 적용**된다. 모든 시각 표기는 오전/오후가 아니라 **24시간제**를 쓴다.
- **단, 영업일은 묶는 기준일 뿐 표시하는 값이 아니다(v2.18, §22).** 개별 기록의 타임스탬프는 실제로 벽시계가 가리켰던 시각 그대로 보여준다 — 08/15 01:00에 친 게임은 08/14의 게임 밤으로 집계되지만, 화면에는 "08/15 01:00"으로 표시된다. 날(day) 단위 라벨(경기일 요약, 명예의 전당, 기간 필터)은 영업일을 쓰고, 특정 기록 하나의 시각은 달력 벽시계를 쓴다.
- 게임이 쌓이면 참가자 간에 다대다 채권-채무 관계가 형성되는데, 이를 **최소 거래 수**로 자동 간소화한다.
- 정산에는 실제 상품 교환인 **"실제 정산"**과, 임의의 두 사람 사이에서 자유롭게 점수를 넘겨주는 **"기부"** 두 종류가 있다. 기부하는 사람의 잔액은 감소하고 받는 사람의 잔액은 증가한다.
- 기간별(일/주/월/년)·종목별 승/패 횟수와 순위, 상대 전적, 티어, 명예의 전당 등을 여러 각도로 볼 수 있는 통계(`/stats`)·통산기록(`/records`, v2.16 신규)·대시보드를 제공한다.
- **티어는 분기(3개월)마다 리셋되며, 종목별로 따로 매겨진다.** 단순 승률이 아니라 참석 인원수로부터 계산한 기대 승·패 대비 성과를 기준으로 한다(§16).
- 참가자 풀 관리, 과거 누적기록 입력, 데이터 롤백은 관리자 모드에서만 접근 가능하며 일반 화면에는 노출되지 않는다.
- 게임 기록은 **누구나 기록 후 2시간 이내라면** 날짜·시간을 제외한 내용을 수정하거나 삭제할 수 있다. 2시간이 지나면 관리자만 손댈 수 있다(§15). 이 앱은 개인별 로그인이 없고 팀 전체가 공유 비밀번호 하나로 접속하므로, "기록한 본인만" 같은 개인 단위 권한 분리는 두지 않는다.

## 2. 데이터 모델 (애플리케이션 타입)

파일: `src/lib/types.ts`

```ts
interface Participant {
  id: string;
  name: string;
  active: boolean;   // 비활성 참가자는 새 게임 선택 목록에서 제외되나 과거 기록은 유지
  createdAt: string;
}

type GameType = "hoola" | "citadels" | "6nimmt"; // 훌라 / 시타델 / 젝스님트

interface GameResult {
  id: string;
  date: string;           // yyyy-MM-dd — v2.16부터 "영업일"(§18.1, 06:00~30:00) 기준. 기록 시점에 서버가 설정 (일반 이용자 수정 불가, 관리자만 예외)
  time?: string;          // "HH:mm", Asia/Seoul. 이 필드 이전 레코드에는 없음
  gameType?: GameType;    // 이 필드 이전 레코드에는 없음
  points?: number;        // Win이 Lose에게서 가져가는 점수. 없으면 1로 간주
  active?: boolean;       // false = 소프트 삭제(잔액/통계/목록에서 제외). 없으면 true로 간주
  attendeeIds: string[];  // 티어 계산의 기대치(1/n)를 만드는 핵심 필드 — §16
  winnerId: string;       // Win (내부 필드명 유지)
  loserId: string;        // Lose (내부 필드명 유지)
  note?: string;
  createdAt: string;      // ISO datetime, 정렬·관리자 롤백·수정 유예시간(2시간) 판단 기준
}

type SettlementType = "payment" | "donation" | "waiver";  // waiver는 레거시 판독용

interface Settlement {
  id: string;
  type?: SettlementType; // 없으면 "payment"로 간주
  fromId: string;        // 주는 사람 (기부는 잔액 -amount, 실제 정산은 +amount)
  toId: string;          // 받는 사람 (기부는 잔액 +amount, 실제 정산은 -amount)
  amount: number;
  date: string;
  note?: string;
  createdAt: string;     // 취소 유예시간(2시간) 판단 기준
}

interface LedgerAdjustment {   // 과거 누적기록 — 승패 없이 채무자→채권자, 금액만 존재
  id: string;
  fromId: string;  // 채무자
  toId: string;    // 채권자
  amount: number;
  note?: string;
  date: string;
  createdAt: string;
}

interface DB {
  participants: Participant[];
  games: GameResult[];
  settlements: Settlement[];
  adjustments: LedgerAdjustment[];
}
```

옵셔널 필드들은 마이그레이션 없이 기존 레코드를 읽을 수 있도록 설계되어 있다 — 없으면 위 주석의 기본값으로 처리된다. **v2.15의 티어 재설계도 스키마 변경 없이 기존 필드만으로 계산된다**(§16).

## 3. 도메인 로직

### 3.1 잔액 계산 및 최소 거래 간소화 (`src/lib/settle.ts`)

`computeNetBalances(games, settlements, adjustments)`는 참가자별 순잔액을 계산한다. 양수 = 받을 것이 있음(채권자), 음수 = 줄 것이 있음(채무자). 소프트 삭제된 게임(`active === false`)은 내부에서 자동으로 제외된다.

부호 규칙:

| 이벤트 | fromId(주는 쪽) | toId(받는 쪽) | 비고 |
| --- | --- | --- | --- |
| 게임 | Lose `−points` | Win `+points` | 점수 없으면 1로 간주 |
| 실제 정산 (`payment`) | `+amount` | `−amount` | 빚을 갚아 양쪽 잔액이 0에 수렴 |
| 기부 (`donation`, 레거시 `waiver` 포함) | `−amount` | `+amount` | 점수를 그냥 넘겨주는 것 — 게임과 같은 방향 |
| 과거 누적기록 | 채무자 `−amount` | 채권자 `+amount` | 게임과 같은 방향 |

`simplifyDebts(balances)`는 0이 아닌 잔액이 12명(`EXACT_SOLVE_THRESHOLD`) 이하면 완전탐색(branch-and-bound, LeetCode 870 "Optimal Account Balancing"과 동일한 문제)으로 **진짜 최소** 거래 수를 찾는다. 각 거래는 항상 두 사람의 남은 잔액 중 작은 쪽으로 "깔끔하게" 정산되도록 제한해, 자기 잔액보다 더 많이 지불하는 것처럼 보이는 수학적으로만 유효한 해는 배제한다. 12명 초과 시 그리디로 대체된다. `scripts/verify-settle.ts`로 검증되어 있다.

### 3.2 통계 (`src/lib/stats.ts`)

- `computeParticipantStats`: 참가자별 승/패/참석 횟수, 승률A/B, 순포인트.
- `computeHeadToHead` / `computeHeadToHeadMatrix` / `computeNemesisAndVictim`: 상대 전적, 천적/밥.
- `groupGamesByPeriod`, `filterByDatePreset`, `filterGamesByType`: 기간·종목 필터링/그룹핑. v2.16에서 `RangePreset`에 `"today"`가 추가되고, 문자열 기반 영업일 컷오프로 다시 작성되었다(§18.1).
- `computeHotColdPlayers`, `computeRecentForm`, `computeCurrentStreaks`, `computeRecords`.
- **`computeRecentGameDaysSummary` (v2.16 신규, §18.2)**: 기존 `computeTodaySummary`/`TodaySummary`를 대체 — 게임이 있었던 최근 3개 영업일(없는 날은 건너뜀)의 판수와 그날의 득실(순점수) 기준 최다 승자(공동 1위 모두 포함)·득실차.
- **`computeCumulativeNetPointsTrend` (v2.16 신규, §18.5)**: 참가자별 누적 순점수 추이를 `PeriodGrouping` 단위(일/주/월/연도)로 버킷팅. 기존에는 게임 1건당 1점으로 취급했으나(±1), 판돈(`points`)을 반영하도록 함께 수정했다 — 앱의 다른 모든 "순점수"와 일관되게.
- **`computeQuarterlyTiers` (v2.15 신규)**: 분기별·종목별 티어. §16 참조. 기존 `computeTier(winRate, decisiveGames)`를 대체한다.
- v2.16에서 `computeGameTypeStats`/`GameTypeParticipantStat`(종목별 성적)이 제거되었다 — 순위표에 섹션별 종목 필터가 생기며 중복이 되었다(§18.5).

### 3.3 시간/회차 (`src/lib/time.ts`, `src/lib/games.ts`)

- 게임 기록 시각은 Asia/Seoul 기준 벽시계로 서버에서 생성한다.
- "N차전"은 저장하지 않고 조회 시 계산한다.
- `activeGames(games)`가 소프트 삭제 필터링을 담당한다.
- v2.14에서 `EDIT_WINDOW_MS`(2시간)와 `isWithinEditWindow(createdAt)`가 추가된다.
- v2.15에서 `quarterKeyOf(date)` / `currentQuarterKey()` / `formatQuarterKey()`가 추가된다 (§16.5).
- **v2.18 — 표시 시각 분리(§22)**: `businessDateFromWallClock`의 정확한 역함수 `calendarDateFromBusinessDay(date, time)`가 바로 아래에 추가된다. 여기에 `gameWallClock(date, time)`을 더해 개별 기록 한 건의 화면 표시용 값(`{date, time, crossedMidnight, businessDate}`)을 한 번에 뽑는다 — `date`는 그 `time`이 실제로 찍힌 달력 날짜, `crossedMidnight`는 그게 영업일(그룹 키)과 다른지. **`createdAt`이 아니라 저장된 `date`/`time`만으로 계산**한다는 게 핵심이다 — §11 관리자 수정으로 고쳐진 값을 존중해야 하기 때문. `time`이 없는 레거시 기록은 벽시계를 복원할 수 없어 영업일을 그대로 반환한다.
- **v2.17 — 소급 적용(§20)**: `seoulWallClockFromIso(iso)`(UTC ISO 순간 → Asia/Seoul `{date, time}`)와 `businessDateFromIso(iso)`가 `time.ts`에 추가된다. `formatInSeoul`/`nowInSeoul`은 이 함수를 재사용하도록 정리된다(동작은 그대로). 판정 로직 자체는 `src/lib/backfill.ts`의 순수 함수 `planBusinessDayBackfill(row)`에 있다 — `time.ts`가 아니라 별도 모듈로 둔 이유는, 이게 일반적인 시간 유틸이 아니라 "저장된 행을 어떻게 소급 보정할지"라는 이 마이그레이션 전용 도메인 로직이기 때문이다. `scripts/backfill-business-day.ts`(`npm run backfill:business-day` = 드라이런, `--apply`는 직접 실행)가 이 함수와 Supabase I/O를 연결한다.
- **v2.16 — 하루 재정의(§18.1)**: `BUSINESS_DAY_START_HOUR`(6), `addDaysToIsoDate(date, days)`(문자열 기반 날짜 산술), `businessDateFromWallClock(date, time)`, `nowInSeoulBusinessDay()`(영업일 기준 "지금"), `minutesSinceBusinessDayStart(time)`(자정을 넘는 같은 영업일 내 시간 정렬용)가 `time.ts`에 추가된다. `todayInSeoul()`은 이름은 그대로지만 이제 **영업일** 기준 오늘을 반환하도록 의미가 바뀌었다 — `nowInSeoul()` 자체는 관리자 롤백 화면의 커트오프처럼 실제 달력 순간이 필요한 곳을 위해 그대로 남아있다. `games.ts`의 `withinDayKey`는 `time` 문자열을 그대로 비교하는 대신 `minutesSinceBusinessDayStart`로 변환해 비교하도록 바뀌어, 자정을 넘긴 게임이 같은 영업일의 더 이른 게임보다 항상 뒤에 정렬된다. `scripts/verify-business-day.ts`(`npm run verify:business-day`)로 검증되어 있다.

## 4. 화면/기능 명세

| 라우트 | 접근 | 설명 |
| --- | --- | --- |
| `/login` | 공개 | 공유 비밀번호 로그인 |
| `/admin-login` | 로그인 | 관리자 비밀번호 입력 → 관리자 세션 발급 |
| `/` (대시보드) | 로그인 | 리더보드, 정산 미리보기, 최근 게임, 핫/콜드 플레이어, 최근 폼, 스트릭, **최근 경기일 요약(§18.2)**, **이번 분기 티어 — 통합+종목별 4블록(§18.3)** |
| `/games/new` | 로그인 | 종목 선택 → 참석자 선택 → 드래그(또는 탭-투-탭)로 Win/Lose 지정 → 점수·메모 확인 후 기록 → **저장 즉시 `/games`로 자동 이동해 기록을 바로 확인(§18.4)** |
| `/games` | 로그인 | 게임 목록, 기간 필터(기본값 오늘 = 영업일), 기간 내 전체·인별 점수 합계, 수정·삭제(2시간 이내, §15), 관리자는 무제한 + 완전삭제 |
| `/settlements` | 로그인 | **참가자별 순 잔액 → 정리된 거래 카드(2단계 확인) → 변제 → 기부 기록 → 기부 랭킹 → 정산·조정 이력 순(§18.6)**, 2시간 이내 취소(§15) |
| `/stats` | 로그인 | 공통 기간 필터(+**오늘**, §18.7) + 섹션별 독립 종목 필터. 순서: 순위표 → (클릭 시) 상대 전적 → 상대 전적 매트릭스 → 천적/밥 → 참가자별 승/패 → 참가자별 누적 순점수 추이(섹션별 추이 단위) → 기간별 게임 수 추이(섹션별 추이 단위) (§18.7) |
| `/records` (통산기록, v2.16 신규) | 로그인 | **분기 티어(§16.7) → 성향 맵(§16.8) → 명예의 전당(구 기록실 + 최고/최저 순득점 + 하루 최다 게임수, §18.8)** — `/stats`에서 이관 |
| `/participants` | **관리자** | 참가자 추가/이름 변경/활성-비활성 |
| `/adjustments` | **관리자** | 과거 누적기록 입력/수정/삭제 |
| `/rollback` | **관리자** | 특정 시각 이후 기록 하드 삭제 |

관리자 전용 라우트는 비관리자 상태에서 상단 네비게이션에 노출되지 않는다. 인증: 모든 `(app)` 라우트는 `src/proxy.ts`에서 세션 쿠키(`gl_session`)를 검사하고, 관리자 전용 라우트는 관리자 쿠키를 추가로 검사한다.

## 5. 아키텍처

### 5.1 스택

- Next.js 16.3.0 (App Router, Turbopack), React 19.2.8
- `proxy.ts` (Next 16의 middleware 대체 파일 컨벤션, Node.js 런타임 기본)
- React Server Components + Server Actions
- `@dnd-kit/core` — 드래그 앤 드롭 / `recharts` — 차트 / `date-fns` / `uuid` / `zod`
- Tailwind CSS v4
- **저장소: Supabase(Postgres)** — v2.11에서 Vercel Blob(JSON 파일)으로부터 전환 완료 (`@supabase/supabase-js`)

### 5.2 저장소 계층 (Supabase/Postgres)

로컬 개발과 프로덕션 모두 동일한 Supabase 프로젝트(`BackRoom`)를 사용한다. 로컬 전용 `data/db.json` 파일 경로는 제거되었다.

**테이블 구조** (`supabase/schema.sql`): `participants`, `games`, `settlements`, `adjustments` — §2의 애플리케이션 타입과 1:1로 대응한다(camelCase↔snake_case). `games.game_type`/`games.time`은 레거시 레코드를 위해 nullable, `games.points`/`games.active`는 NOT NULL. `settlements.type`은 Postgres enum.

**접근 제어**: 모든 테이블에 RLS를 켜두고 정책은 만들지 않는다(=사실상 전체 잠금). 앱은 서버 코드(`src/lib/supabase.ts`)에서만 `SUPABASE_SERVICE_ROLE_KEY`로 접근하며, 이 키는 RLS를 완전히 우회한다 — 브라우저에는 노출되지 않는다. 접근 제어는 앱 레이어의 `SITE_PASSWORD`/`ADMIN_PASSWORD`가 담당한다.

**데이터 접근 계층**: `src/lib/storage.ts`가 테이블별 타겟팅된 쿼리 함수를 제공한다 (`listParticipants`/`insertParticipant`/`updateParticipantName`/`updateParticipantActive`, `listGames`/`insertGame`/`softDeleteGame`/`updateGameRow`/`deleteGameRow`, `listSettlements`/`insertSettlement`/`deleteSettlementRow`, `listAdjustments`/`insertAdjustment`/`updateAdjustmentRow`/`deleteAdjustmentRow`, `getFullDB()`). 각 함수는 Postgres 행을 애플리케이션 타입으로 매핑하며 `timestamptz`/`time` 포맷 차이를 정규화한다.

관리자 롤백만 예외적으로 `rollback_after(cutoff)` RPC로 묶여 하나의 트랜잭션으로 실행된다.

### 5.3 페이지 렌더링

DB를 읽는 모든 `(app)/` 페이지에 `export const dynamic = "force-dynamic"`을 명시해 정적 프리렌더링을 막고 있다.

## 6. 배포 환경 및 트러블슈팅 기록

### 6.1 필수 환경변수

| 변수 | 설명 |
| --- | --- |
| `SITE_PASSWORD` | 앱 접속용 공유 비밀번호 |
| `ADMIN_PASSWORD` | 관리자 모드 비밀번호 |
| `SUPABASE_URL` | Supabase 프로젝트 URL |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase 서비스 롤 키(비밀값). 절대 커밋 금지 |

`BLOB_READ_WRITE_TOKEN`은 더 이상 앱 코드에서 쓰이지 않는다. 환경변수를 추가/변경한 뒤에는 반드시 **Redeploy**해야 반영된다. v2.14·v2.15는 새 환경변수가 필요 없다.

### 6.2 겪었던 문제와 해결

- **Private Blob 스토어 읽기 실패**: `access: "public"` + raw `fetch()`가 Private 모드에서 실패 → `get(pathname, { access: "private" })`로 교체.
- **사이트 전체 404 (`NOT_FOUND`, Runtime Logs에 요청 자체가 안 찍힘)**: Vercel 프로젝트 내부 라우팅 매니페스트 손상 추정. **같은 GitHub 저장소를 가리키는 새 Vercel 프로젝트를 만들어 import**해서 해결.
- **재배포 누락으로 인한 ENOENT**: 환경변수가 실행 중 배포에 반영되지 않아 로컬 파일 쓰기를 시도하다 실패. Redeploy로 해결.

## 7. 알려진 제약사항

- **모든 페이지가 `force-dynamic`이라 캐싱이 없다.** 대시보드/통계처럼 자주 안 바뀌는 화면도 매번 새로 렌더링 + 쿼리한다.
- **통계·정산·대시보드 화면은 필요한 테이블 전체를 가져와 애플리케이션 메모리에서 집계**한다. 데이터가 커지면 SQL 집계(뷰·RPC)로 옮길 필요가 있을 수 있다. v2.15의 티어 계산도 "전체 게임을 처음부터 분기 단위로 접는" 방식이라 이 항목에 해당한다(§16.5). **v2.16부터 대시보드는 이 fold를 통합+종목별 4번(§18.3) 반복한다** — 게임 수가 늘어나면 이 부분이 먼저 무거워질 후보다.
- **드래그 컴포넌트가 마운트 후에만 DndContext를 렌더링**한다 — 초기 로드 시 약간의 레이아웃 시프트.
- **개인별 로그인이 없다.** 팀 전체가 `SITE_PASSWORD` 하나를 공유하므로 "누가 수정했는지" 같은 개인 단위 이력은 구현 불가능하다.
- ~~**v2.16 하루 재정의는 마이그레이션 없이 적용됐다.**~~ → **v2.17에서 해소.** v2.16은 새로 기록되는 게임에만 영업일 규칙을 적용했고, 이미 저장된 기록의 `date`는 달력 날짜 그대로 남겨뒀다. 그 결과 "8/14 저녁에 시작해 8/15 01:00까지 이어진 게임 밤"이 화면에서 이틀로 쪼개져 보이는 문제가 실제로 발생했다 — §20이 이를 백필로 바로잡는다.

## 8. v2.0 요구사항 (구현 완료)

관리자 모드(`ADMIN_PASSWORD` + 별도 쿠키), 과거 누적기록 입력, 게임 종목·자동 시각 기록·점수·N차전, 게임 소프트 삭제, "1등/꼴찌"→"Win/Lose" 용어 통일, 정산 카드 UI + 탕감→기부 확장, 게임 목록 기간 필터, 참가자별 상대 전적, 기부 랭킹, 관리자 데이터 롤백.

## 9. v2.10 요구사항 (구현 완료)

**버그 수정 4건**: ① 모바일 드래그 앤 드롭 미작동(`touch-action`, `DragOverlay`, 탭-투-탭 추가). ② 기부의 잔액 반영 방향이 반대였던 것. ③ 관리자 전용 탭이 비관리자에게 노출되던 것. ④ 정산이 한 번의 클릭으로 크게 실행될 수 있던 것(2단계 확인, 큰 금액 경고, 즉시 취소 동선 — v2.14에서 2시간 유예로 확장).

**기능 추가**: 게임 목록 구간 필터 내 인별 점수 합계, 대시보드 핫/콜드 플레이어·최근 폼·스트릭·오늘의 요약, 통계 화면 티어 뱃지·상대 전적 매트릭스·천적/밥·종목별 성적·기록실.

## 10. v2.11 요구사항 (구현 완료)

저장소를 Vercel Blob에서 **Supabase(Postgres)**로 전환했다. 4개 테이블 스키마(`supabase/schema.sql`), RLS + 서비스 롤 키 접근, `mutateDB` 폐기 후 타겟팅된 쿼리로 재작성, 관리자 롤백을 위한 `rollback_after` RPC 트랜잭션, 기존 데이터 마이그레이션(`scripts/migrate-to-supabase.ts`)까지 완료.

## 11. v2.12 요구사항 (게임 기록 관리자 수정, 구현 완료)

게임 목록(`/games`)에서 관리자가 기존 게임의 종목·참석자·Win/Lose·점수·메모·**날짜·시각**·활성 상태를 고칠 수 있다. 삭제 후 재입력하면 기록 시각이 재입력 시점으로 다시 자동 저장되는 문제를 피하기 위한 기능이다. `updateGame` 액션이 내부에서 관리자 세션을 직접 검증하고, 저장 전 변경 전/후 diff 확인 단계를 거친다. 소프트 삭제된 게임도 관리자에게는 목록에 노출되어 열람·수정·재활성화가 가능하다.

> v2.14에서 이 기능은 "관리자는 무제한, 일반 이용자는 날짜·시간 제외 + 2시간 이내"로 확장된다(§15).

## 13. v2.13 요구사항 (마이너 업데이트 모음, 구현 완료)

### 13.1 승/무/패 표기
3명 이상 게임에서는 Win도 Lose도 아닌 참석("무")이 존재한다. 무는 저장하지 않고 `참여 − 승 − 패`로 계산한다.

### 13.2 승률A / 승률B
승률A = 승/(승+패), 승률B = 승/참여. 두 값을 나란히 표기한다.

### 13.3 종목별 성적: 승률A·B 기준 정렬, "최강" 표기 제거

### 13.4 기록실 개편
단일 게임 최고 점수 삭제, 최장 연승/연패에 기간 표기 추가, top 3 + 공동 순위(dense ranking) 지원.

### 13.5 통계 조회 기간 직접 입력
`RangePreset`에 `"custom"` 추가. `yyyy-MM-dd` **문자열 직접 비교**로 구현했다 — `new Date()` 파싱을 거치면 UTC 자정으로 해석되어 하루가 밀리기 때문. **v2.15의 분기 판정도 같은 이유로 문자열 슬라이스만 쓴다.**

### 13.6 게임 목록 기본 필터 = 오늘, 관리자 완전삭제
`/games` 필터 기본값을 오늘(Asia/Seoul)로. 관리자는 소프트 삭제 외에 완전삭제(하드 삭제)를 인라인 2단계 확인 후 쓸 수 있다.

### 13.7 변제 (자유 정산)
당초 `proxy_payment`(대리 변제) 타입을 만들려 했으나, 대리인을 `fromId`로 잡으면 기존 "실제 정산"과 부호 규칙이 완전히 동일해 원 채무자는 잔액 계산에 관여하지 않는다는 것을 확인했다. 그래서 별도 타입 없이 "실제 정산"을 자유롭게 기록하는 폼(`RepaymentForm`)으로 단순화했고, **스키마 변경이 불필요해졌다**. `normalizeSettlementType()`이 레거시 `proxy_payment`를 `payment`로 정규화한다.

---

## 15. v2.14 요구사항 (게임 기록 일반 사용자 수정/삭제 + 2시간 유예시간)

### 15.1 배경

게임 기록 수정(§11)이 관리자 전용이라 사소한 오기록(참석자 빠짐, 점수 오류, 종목 오선택)을 고치려고 매번 관리자에게 요청해야 한다. 날짜·시간은 서버가 기록한 실제 시각을 신뢰해야 하므로 일반 이용자에게는 자동기록·수정불가를 유지한다. "오기록 정정" 목적을 벗어나 오래된 기록을 고치는 걸 막기 위해 기록 후 2시간 유예시간을 둔다. 정산/기부의 취소도 같은 취지로 제한한다.

### 15.2 요구사항

- **게임 기록**: 일반 이용자도 종목·참석자·Win/Lose·점수·메모를 수정하거나 (소프트) 삭제할 수 있다.
  - **날짜·시간은 일반 이용자에게 수정 불가**(관리자만 예외, §11 유지).
  - **기록 후 2시간이 지나면 일반 이용자는 수정도 삭제도 불가.** 기준은 `createdAt`. 서버 액션이 직접 강제해야 한다 — 버튼 숨김은 UX일 뿐.
  - **관리자는 시간 제한과 무관하게 무제한**(날짜·시간 포함 수정, 완전삭제).
  - 개인별 로그인이 없으므로 "본인이 참여한 게임만" 같은 분리는 불가능하다. 2시간 이내면 로그인한 누구나 수정·삭제할 수 있다.
- **정산/기부**: "방금 기록함 · 취소" 되돌리기의 유효 기간을 2시간으로 명시하고, `deleteSettlement`에도 `createdAt` 기준 2시간 제한을 강제한다(현재는 아무 제한이 없다). 관리자는 예외.
- 시간 제한에 걸린 경우 버튼을 조용히 숨기지 말고 짧게 이유를 안내한다.

### 15.3 구현 방향

- `src/lib/admin.ts`에 `isAdminSession(): Promise<boolean>`(throw하지 않는 버전) 추가.
- `src/lib/time.ts`에 `EDIT_WINDOW_MS`(2시간)와 `isWithinEditWindow(createdAt)` 추가 — `"server-only"` 없는 순수 함수라 클라이언트에서도 재사용.
- `src/lib/storage.ts`에 `getGameById`/`getSettlementById` 추가(유예시간 판단용 `createdAt` 조회).
- `updateGame`은 비관리자 호출을 허용하되, **비관리자 입력의 날짜·시간·활성 상태는 신뢰하지 않고 항상 기존 값을 유지**한다.
- `recordSettlement` 반환값에 `createdAt` 추가.

---

## 16. v2.15 요구사항 (티어 시스템 재설계, 이번 작업 범위)

### 16.1 현행 방식과 그 문제점

현재는 `src/lib/stats.ts`의 `computeTier(winRate, decisiveGames)`가 **승률A(승/(승+패))** 하나만 보고 절대 컷으로 티어를 매긴다 — 브론즈 <35%, 실버 <45%, 골드 <55%, 플래티넘 <65%, 다이아 <75%, 챌린저 ≥75%, 3경기 미만은 랭크 미배정. 이 방식에는 구조적인 문제가 다섯 가지 있다.

1. **참석 인원수를 무시한다.** 4인전에서 Win할 확률은 25%, 5인전은 20%다. 같은 1승인데 난이도가 다른데도 승률A는 똑같이 취급한다. 사용자가 지적한 "보통 4~5명에서 하므로 기대 승률은 20~25%"가 바로 이 지점이다.
2. **표본의 대부분을 버린다.** 5인전 평균 실력자의 실제 분포는 승 20% · 패 20% · **무 60%**다. 승률A는 이 60%를 통째로 버리고 남은 40%만 본다. 즉 기록된 게임의 60%가 티어 계산에 아무 영향을 못 준다.
3. **판돈(`points`)을 무시한다.** 2점짜리 판을 이긴 것과 1점짜리를 이긴 것이 동일하게 1승으로 계산된다.
4. **표본 크기 보정이 없다.** 3경기에서 2승 1패면 승률A 67% → 플래티넘. 한 번의 운으로 최상위 티어가 나온다.
5. **컷이 기대치와 무관한 절대값이다.** 5인전에서 승률A 50%는 정확히 "기대치대로"인데 골드 구간 한복판이고, 승률A 75%(챌린저)는 사실상 도달 불가능한 수치다.

부수적으로, 이 티어는 통계 화면 순위표의 컬럼 하나로만 쓰이는데 화면의 기간·종목 필터에 따라 값이 바뀐다. 즉 "티어"라기보다 그때그때 필터의 승률 표시에 가깝고, 분기·시즌 개념이 없다.

### 16.2 새 티어의 기본 지표 — 승 지수와 패 지수

한 게임 g에 대해 참석 인원 `n_g`, 판돈 `p_g`라 하고, **기대치 `e_g = 1/n_g`** 를 정의한다. 실력이 모두 같다면 Win할 확률도 `1/n_g`, Lose할 확률도 `1/n_g`이다 — 이것이 사용자가 말한 "기대 승률 20~25%, 기대 패배율 20~25%"를 게임마다 정확하게 계산한 값이다(4인전이면 0.25, 5인전이면 0.20, 6인전이면 0.167).

분기 안에서 그 참가자가 뛴 해당 종목 게임 전체에 대해 다음을 누적한다.

| 기호 | 정의 | 의미 |
| --- | --- | --- |
| `E_w` | `Σ e_g` | **기대 승수 = 기대 패수.** 유효 표본 크기 역할도 한다 |
| `E_p` | `Σ p_g · e_g` | 기대 획득 점수 = 기대 상실 점수 |
| `W_p` | 이긴 게임의 `p_g` 합 | 실제 획득 점수 |
| `L_p` | 진 게임의 `p_g` 합 | 실제 상실 점수 |

여기서 두 축이 나온다.

- **승 지수 `WI = W_p / E_p`** — 1.00이면 기대만큼 이겼다는 뜻. 1.50이면 기대보다 50% 더 이겼다.
- **패 지수 `LI = L_p / E_p`** — 1.00이면 기대만큼 졌다. **낮을수록 좋다.**

그리고 티어의 핵심 수치는 두 축의 차이다.

```
PERF = WI − LI = (W_p − L_p) / E_p        (0 = 정확히 기대치)
```

`W_p − L_p`는 이 앱이 이미 계산하는 **순점수**와 같은 값이다. 즉 PERF는 "내 순점수를, 참석 인원수와 판돈으로부터 계산된 기대 변동폭으로 나눈 값"이다. 이 하나의 수식이 앞의 문제 1·2·3을 동시에 해결한다 — 인원수는 `e_g`로, 무는 "이기지도 지지도 않은 게임이 기대치만 쌓고 실적은 안 쌓는" 형태로, 판돈은 `p_g` 가중으로 자동 반영된다.

현실적인 범위 감각: 5인전에서 승 32% · 패 12%인 최상위권은 `WI=1.6, LI=0.6` → **PERF ≈ +1.0**. 승 12% · 패 32%인 최하위권은 **PERF ≈ −1.0**. 평균은 0.

### 16.3 신뢰도 축소 (표본이 적으면 함부로 티어를 주지 않는다)

PERF의 표준오차는 대략 `√(2 / E_w)`이다. 표본이 적으면 PERF가 크게 흔들리므로, 베이지안 축소로 0(평균)쪽으로 당긴다.

```
k = E_w / (E_w + E0),   E0 = 8
```

`E0 = 8`은 "실제 실력의 사전 표준편차를 PERF 기준 0.5로 본다"는 가정과 정확히 같다(`E0 = 2/τ²`, `τ = 0.5`). 실제 예상 표본:

| 종목 | 분기당 본인 참여 게임 | `E_w` | `k` |
| --- | --- | --- | --- |
| 훌라 (회당 5~6판) | 약 80판 | 17.9 | 0.69 |
| 시타델 (회당 1~2판) | 약 22판 | 4.9 | 0.38 |
| 젝스님트 (회당 1~2판) | 약 22판 | 4.9 | 0.38 |

*주 2회 × 13주 = 26세션, 출석률 75%, 판당 4~5인 가정.*

### 16.4 분기 리셋 — 단, 소프트 리셋

티어는 분기(1~3월 Q1, 4~6월 Q2, 7~9월 Q3, 10~12월 Q4)마다 새로 시작한다. 다만 **완전 리셋이 아니라 직전 분기의 성적 일부를 가중치 `γ = 0.35`로 이월**한다(롤 시즌 초 소프트 MMR 리셋과 같은 개념).

```
w_total   = E_w + γ · w_prev
PERF_mix  = (PERF · E_w + PERF_prev · γ · w_prev) / w_total
k         = w_total / (w_total + E0)
```

이월을 넣는 이유는 시뮬레이션 결과 때문이다(§16.6). 순수 리셋(γ=0)에서 티어와 실제 실력의 순위 상관계수는 훌라 0.876 / 시타델 0.578 / 젝스님트 0.396이었는데, γ=0.35에서 **0.929 / 0.712 / 0.533**으로 크게 올랐다. 판수가 적은 시타델·젝스님트에서 특히 효과가 크다. 그러면서도 새 분기가 시작되면 이월 가중치가 35%뿐이라 "다시 올라가야 한다"는 시즌 감각은 유지된다.

부수 효과로, **한 분기를 통째로 쉰 사람은 `E_w = 0`이라 `w_total = γ · w_prev`가 되어 `k`가 줄고 티어가 1000(중앙) 쪽으로 자연스럽게 감쇠**한다. 별도의 비활동 강등 규칙이 필요 없다.

> γ를 0으로 두면 사용자가 처음 말한 "완전 리셋"이 된다. 상수 하나만 바꾸면 되므로 실제로 한 분기 돌려보고 결정해도 된다.

### 16.5 TR(티어 레이팅)과 티어 컷

```
TR = 1000 + 500 × PERF_mix × k
```

| 티어 | TR 구간 |
| --- | --- |
| 챌린저 | ≥ 1270 |
| 마스터 | 1180 ~ 1269 |
| 다이아몬드 | 1090 ~ 1179 |
| 플래티넘 | 1000 ~ 1089 |
| 골드 | 910 ~ 999 |
| 실버 | 820 ~ 909 |
| 브론즈 | < 820 |
| 배치 중 | `w_total < 2.0` (기대 승수 2회분 미만 ≈ 9판 미만) |

**기존 6단계에 "마스터"를 새로 추가해 7단계**로 만든다. 8명이 참여하는 그룹에서 티어가 7단계면 대체로 한 칸에 1명씩 들어가 서열이 눈에 보인다.

계산은 스키마 변경 없이 **전체 게임을 분기 순서대로 접어(fold) 나가는 순수 함수**로 구현한다.

```
computeQuarterlyTiers(participants, games, gameType | "all")
  → Map<quarterKey, TierRow[]>

// quarterKey: "2026-Q3" — g.date.slice(0,4), slice(5,7) 문자열 슬라이스로만 판정.
//   §13.5에서 확인했듯 new Date() 파싱은 UTC 자정 해석으로 하루가 밀릴 수 있다.
// 첫 분기부터 순서대로 돌면서 각 분기의 (PERF_mix, w_total)을 다음 분기 prior로 넘긴다.
// 그 분기에 한 판도 안 뛴 참가자도 반드시 fold에 포함시켜야 감쇠가 동작한다.
```

### 16.6 시뮬레이션 검증

이 설계는 몬테카를로로 컷을 맞췄다. 8명, 주 2회 × 13주, 훌라 5~6판/시타델·젝스님트 각 1~2판/세션, 출석률 75%, 판당 4~5인, 20%는 2점짜리. 실력은 Plackett-Luce(θ를 −0.5~+0.5로 8등분, 최상위 승 32%·패 12% 수준)로 부여했다. 각 조건 700~3000분기 반복.

**훌라 — 티어별 분포 (실력차가 실제로 있는 세계)**

| 실력 순위 | TR 평균 | 브론즈 | 실버 | 골드 | 플래 | 다이아 | 마스터 | 챌린저 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 8위(최약) | 636 | 98% | 2% | · | · | · | · | · |
| 7위 | 739 | 79% | 17% | 4% | · | · | · | · |
| 6위 | 846 | 40% | 33% | 22% | 4% | 1% | · | · |
| 5위 | 952 | 9% | 23% | 37% | 23% | 7% | 1% | · |
| 4위 | 1064 | 1% | 5% | 19% | 36% | 28% | 10% | 2% |
| 3위 | 1156 | · | 1% | 6% | 18% | 37% | 26% | 13% |
| 2위 | 1253 | · | · | · | 4% | 18% | 33% | 45% |
| 1위(최강) | 1359 | · | · | · | · | 2% | 15% | 82% |

**핵심 지표**

| 종목 | 실력↔티어 순위상관 | P(8명이 브론즈~챌린저 전 구간에 분포) | P(실력이 완전히 같은데도 전 구간 분포) |
| --- | --- | --- | --- |
| 훌라 | 0.93 | **93.7%** | 1.5% |
| 시타델 | 0.71 | 31.0% | 5.2% |
| 젝스님트 | 0.53 | 15.0% | 5.0% |

사용자 요구("8명이 챌린저부터 브론즈까지 분포될 가능성이 현실적으로 존재")는 **주력 종목인 훌라에서 93.7%로 충족**된다. 동시에 실력차가 전혀 없는 세계에서 전 구간이 벌어질 확률은 1.5%뿐이라, 티어가 순전히 운으로 갈리지 않는다.

**시타델·젝스님트가 중앙에 몰리는 것은 의도된 결과다.** 분기당 22판 남짓으로는 실력을 가릴 표본이 근본적으로 부족하다. 컷 간격을 좁혀서 억지로 벌릴 수는 있지만, 시뮬레이션상 그 대가가 너무 크다.

| 스케일 | 시타델 P(전 구간 분포) | 실력차 0인데 전 구간 분포 |
| --- | --- | --- |
| 500 (채택) | 31% | 9% |
| 650 | 61% | 34% |
| 800 | 81% | 58% |

스케일 800이면 시타델도 화려하게 벌어지지만, 실력이 똑같아도 58% 확률로 챌린저와 브론즈가 나온다 — 티어가 사실상 제비뽑기가 된다. **그래서 종목과 무관하게 동일한 컷(SCALE=500)을 쓰고, 판수가 적은 종목의 티어가 가운데 모이는 것을 정상 동작으로 둔다.** 화면에는 신뢰도(`k`)를 함께 표시해 "왜 안 벌어지는지"를 보여준다.

### 16.7 화면 요구사항

> v2.16에서 이 섹션(및 §16.8 성향 맵)은 `/stats`에서 새 탭 `/records`("통산기록")로 옮겨진다 — §18.8 참조. 아래 내용 자체(카드 구성, 필터 등)는 그대로 유효하다.

- **`/stats`에 "분기 티어" 섹션 신설.** 분기 선택(기본값 = 현재 분기)과 종목 탭(훌라 / 시타델 / 젝스님트 / **통합**)을 두고, 참가자별 티어 카드를 TR 내림차순으로 나열한다. "통합"은 종목을 합쳐 계산해 표본이 가장 크므로(`E_w ≈ 27.6`, `k ≈ 0.78`) 대표 티어로 쓸 만하다.
- 각 카드에는 **티어 뱃지 · TR · 승 지수 · 패 지수 · 참여 판수 · 신뢰도(`k`를 %로) · 직전 분기 대비 승강 화살표**를 표시한다.
- **`/stats` 순위표에서 기존 티어 컬럼은 제거**한다. 순위표는 화면 필터(기간·종목)를 따르는데 티어는 분기 고정이라 두 개념이 섞이면 혼란스럽다. 대신 승률A/승률B 컬럼은 그대로 둔다.
- **대시보드(`/`)에는 이번 분기 통합 티어 요약**을 한 줄 카드로 넣는다(상위 3명 정도 + "전체 보기" 링크).
- 배치 중인 참가자는 "배치 중 (n/9판)"처럼 진행도를 보여준다.
- 티어 계산 방식을 한두 문장으로 설명하는 도움말을 섹션 하단에 둔다 (예: "참석 인원수로 계산한 기대 승·패 대비 성과로 매깁니다. 4인전 기대 승률 25%, 5인전 20%. 분기마다 리셋되며 직전 분기 성적이 35% 반영됩니다.").
- **"성향 맵" 섹션을 별도로 신설**한다(§16.8). 티어 섹션과 달리 분기가 아니라 **최근 90일 롤링**이며, 표본 게이트가 없다. 두 섹션의 기간 개념이 다르다는 점을 각각 부제로 명시한다 — 티어는 "이번 분기", 성향 맵은 "최근 3개월".

### 16.8 성향 맵 — 최근 3개월 2축 산점도 (뱃지 대신)

티어(분기 고정)와 별개로, **"요즘 어떤 플레이를 하고 있나"를 최근 90일 롤링으로 실시간 표시**하는 2축 맵을 둔다. 등급을 매기거나 뱃지를 붙이지 않고, 사람을 좌표평면 위에 점으로 찍는 것이 전부다.

승 지수·패 지수를 45° 돌리면 서로 다른 두 가지를 재는 축이 나온다.

| 축 | 정의 | 중심 | 읽는 법 |
| --- | --- | --- | --- |
| **X — 적극성 지수 `ENG`** | `(WI + LI) / 2` | 1.00 | 높을수록 Win 아니면 Lose로 끝나는 판이 많다(적극·고변동). 낮을수록 무로 지나가는 판이 많다(보수·저변동) |
| **Y — 손익 지수 `PERF`** | `WI − LI` | 0 | 양수면 이득, 음수면 손해. §16.2의 티어 핵심 지표와 같은 값 |

두 축은 **수학적으로 서로 독립**이다. `Cov(W+L, W−L) = Var(W) − Var(L)`인데 승·패의 기대치가 `1/n`으로 동일하므로 이 값이 정확히 0이다. 즉 "얼마나 적극적인가"와 "이득인가 손해인가"는 서로를 오염시키지 않는다.

**시뮬레이션 검증** — 선수마다 실력(θ)과 적극성(α)이라는 잠재 변수를 서로 상관 0이 되게 심어놓고(승자 ∝ softmax(θ+α), 패자 ∝ softmax(−θ+α)), 두 축이 각각을 제대로 복원하는지 확인했다. 90일 표본 2000회 반복:

| | 적극성(α)과의 상관 | 실력(θ)과의 상관 |
| --- | --- | --- |
| **ENG 축** | **+0.88** (훌라) / **+0.91** (통합) | −0.09 (교차, 0에 가까움) |
| **PERF 축** | +0.13 (교차, 0에 가까움) | **+0.87** (훌라) / **+0.90** (통합) |

각 축이 의도한 것만 재고 다른 축의 정보를 거의 섞지 않는다는 뜻이다.

**표본 게이트를 두지 않는다.** 티어와 달리 이 맵은 배치 조건도 축소(shrinkage)도 없이 **원값 그대로** 찍는다 — "요즘 어떠냐"를 즉시 보고 싶은 화면이기 때문이다. 대신 판수가 적으면 점이 크게 튄다는 사실을 **점 크기와 투명도로** 전달한다(판수가 많을수록 크고 진하게). 시뮬레이션으로 잰 실제 변동폭:

| 표본 | ENG (p1 ~ p99) | PERF (p1 ~ p99) |
| --- | --- | --- |
| 90일 전체 | 0.52 ~ 1.56 | −1.42 ~ +1.29 |
| 30일치 | 0.39 ~ 1.69 | −1.77 ~ +1.63 |
| 10일치 | 0.00 ~ 1.94 | −2.76 ~ +2.38 |

따라서 축 도메인은 **X `[0.2, 1.8]`, Y `[−1.6, +1.6]`** 로 고정하고, 벗어나는 점은 가장자리로 클램프해 테두리를 점선 처리한다(자동 스케일을 쓰면 이상치 하나가 나머지를 전부 가운데로 뭉갠다). X 하한은 위 표의 0.4가 아니라 0.2인데, 1.00("기대치")이 Y축의 0("본전")처럼 정중앙에 오게 하려면 상한(1.8, 1.0에서 +0.8)과 대칭인 0.2(1.0에서 −0.8)가 필요하기 때문이다 — 실제 ENG 값이 0.4 밑으로 내려가는 경우는 드물어(90일 p1 = 0.52) 왼쪽에 여백만 늘어날 뿐 데이터가 가려지지는 않는다.

**사분면**은 차트 배경에 옅은 라벨로만 표시한다 — 개인에게 붙는 뱃지가 아니다.

| | 이득 (Y > 0) | 손해 (Y < 0) |
| --- | --- | --- |
| **적극 (X > 1)** | 승부사 | 불나방 |
| **보수 (X < 1)** | 실속파 | 조공러 |

기존 `computeTier` 시절에 구상했던 개별 유형 뱃지(`PlayStyleBadge`)는 만들지 않는다.

### 16.9 티어 계산에 넣지 **않기로** 한 것

사용자가 "다양한 지표를 활용"해 달라고 했으므로 후보들을 실제로 시뮬레이션에 넣고 비교했다. 결론은 **PERF 하나가 이 데이터에서 뽑을 수 있는 신호를 사실상 다 담고 있고, 다른 지표를 더해도 순위 정확도가 오르지 않는다**는 것이다.

| 후보 | 훌라 순위상관 | 판정 |
| --- | --- | --- |
| PERF만 | 0.876 | 채택 |
| PERF + 세션 단위 성적(그날 이득/손해 본 날 비율) | 0.871 | 기각 — 개선 없음 |
| PERF + 세션 + 상대 강도(SOS) | 0.873 | 기각 — 개선 없음 |
| 세션 비중 확대 | 0.873 | 기각 — 개선 없음 |

이유는 명확하다. 세션 단위 성적은 PERF를 거칠게 반올림한 값이라 새 정보가 없고, 상대 강도는 8명 고정 풀에서 라인업이 거의 랜덤이라 사람마다 차이가 거의 안 생긴다. Bradley-Terry 방식의 능력치 추정도 시도했으나 구현이 훨씬 복잡한 데 비해 이득이 확인되지 않아 접었다.

**그래서 "다양한 지표"는 티어 수식을 복잡하게 만드는 방향이 아니라, ① PERF가 이미 흡수하고 있는 요소(인원수·판돈·무·표본)를 화면에서 드러내고 ② 승 지수/패 지수/신뢰도를 티어 옆에 병기하며 ③ 같은 두 축을 45° 돌린 성향 맵(§16.8)을 별도 화면 요소로 두는 방향으로 구현한다.**

### 16.10 성공 기준

- `computeTier(winRate, decisiveGames)`가 코드베이스에서 사라지고, 분기·종목별 티어로 완전히 대체된다.
- 티어 계산이 순수 함수이며 유닛 테스트(또는 검증 스크립트)로 다음이 확인된다: 모두가 정확히 기대치대로면 전원 TR 1000, 판수 부족 시 배치 중, 한 분기 쉬면 TR이 1000 쪽으로 감쇠, 분기 경계(3/31 → 4/1)가 정확히 갈린다.
- 성향 맵이 최근 90일 기준으로 그려지고, 판수가 1판뿐인 참가자도 (작고 흐린 점으로) 빠짐없이 표시된다. 모두가 정확히 기대치대로인 데이터에서는 전원이 (1.00, 0) 부근에 모인다.
- 스키마 변경·환경변수 추가 없음.
- `npm run build`, `npm run lint` 통과.

## 18. v2.16 요구사항 (하루 재정의 + 화면 전면 개편, 구현 완료)

### 18.1 하루의 재정의 (06:00~30:00)

이 모임의 게임 밤은 자정을 넘겨 이어지는 일이 흔한데, 기존에는 "하루"가 달력 날짜(00:00~23:59)였다. v2.16부터 **하루는 06:00부터 다음 날 06:00 직전까지("30:00")를 한 단위로 본다** — 예: 08/15 04:00에 기록된 게임은 08/14의 기록이고, 08/15 07:00에 기록된 게임은 08/15의 기록이다. 이 규칙은 **앱 전체에 공통**이며(게임 `date`, "오늘" 필터·요약, N차전 순서, 분기 경계), §3.3에 정리된 `time.ts`의 `businessDateFromWallClock`/`nowInSeoulBusinessDay`/`minutesSinceBusinessDayStart`가 단일 소스다. `nowInSeoul()`(실제 달력 순간)은 관리자 롤백 커트오프처럼 정말 순간이 필요한 곳을 위해 별도로 남아있다.

모든 시각 표기는 오전/오후가 아니라 **24시간제**로 통일한다 — 실제로 기존 코드베이스는 이미 전부 24시간제였고(`HH:mm`, `<input type="time">`), v2.16은 이 규칙을 명문화하고 관리자 수정 화면의 라벨에 "06:00 기준"/"24시간제"를 명시했다.

마이그레이션은 하지 않는다 — 배포 전 기록은 달력 날짜를 그대로 유지한다(§7).

### 18.2 대시보드: "오늘의 요약" → "최근 경기일 요약"

`computeTodaySummary`를 `computeRecentGameDaysSummary`로 교체했다(`src/lib/stats.ts`). 게임이 있었던 **최근 3개 영업일**(없는 날은 건너뜀)을 최신순으로 보여주며, 날짜별로 판수와 그날의 **득실(순점수) 기준 최다 승자**(공동 1위 전원 표기)·**득실차**를 표시한다. 승 횟수가 아니라 순점수로 순위를 매기는 이유는, 표시되는 "득실차" 숫자와 "누가 1위인지"가 서로 다른 기준이면 공동 1위가 왜 공동인지 설명이 안 되기 때문이다.

### 18.3 대시보드: 티어를 통합 + 종목별로

기존에는 통합 티어 상위 3명만 보여줬다. v2.16부터는 **통합·훌라·시타델·젝스님트 4블록**을 나란히 보여주고, 블록마다 상위 3명 + 분기 표기를 붙인다. "전체 보기" 링크는 `/stats`가 아니라 새 `/records` 탭으로 이동한다(§18.8).

### 18.4 새 게임 기록: 저장 후 게임 기록 탭으로 자동 이동

`/games/new`에서 결과를 확인하고 저장하면, 기존에는 같은 화면에 성공 메시지만 띄우고 남아있었다. v2.16부터는 저장이 끝나면 **`/games`로 자동 이동**해 방금 기록이 실제로 반영됐는지 바로 확인할 수 있다. `createGame` 서버 액션이 이미 `revalidatePath("/games")`를 호출하므로 이동 직후 목록에 최신 데이터가 뜨며, `router.refresh()`를 한 번 더 호출해 이를 보강한다.

### 18.5 통계 화면 재구성

`/stats`의 섹션 순서를 다음과 같이 확정한다: **순위표 → (순위표에서 "상대 전적" 클릭 시) 상대 전적 → 상대 전적 매트릭스 → 천적/밥 → 참가자별 승/패 → 참가자별 누적 순점수 추이 → 기간별 게임 수 추이**. "종목별 성적" 섹션은 삭제한다 — 순위표에 섹션별 종목 필터가 생기면서 같은 정보를 중복으로 보여주게 됐기 때문이다.

**필터 구조가 바뀐다.** 화면 상단 공통 필터는 **기간**만 남는다(여기에 `"오늘"`이 새로 추가된다 — §18.7). 기존에 화면 전체를 지배하던 **종목** 필터는 폐지되고, 위 7개 섹션 각각이 **자신만의 종목 필터**를 갖는다(상대 전적 패널은 순위표 섹션의 종목 필터를 그대로 물려받는다 — 순위표의 드릴다운이므로). **추이 단위**(일/주/월/연도) 필터는 공통 필터에서 빠지고, 실제로 그 단위를 쓰는 두 섹션(참가자별 누적 순점수 추이, 기간별 게임 수 추이)에만 각자 따로 존재한다.

**참가자별 누적 순점수 추이**는 기존에 게임 1건마다 점 하나(±1, 판돈 무시)를 찍었는데, v2.16부터 다른 추이 섹션과 같은 "추이 단위" 버킷 단위로 샘플링하고 판돈(`points`)을 반영한 순점수를 누적한다(`computeCumulativeNetPointsTrend`) — 게임 수가 많아지면 게임당 1점이던 x축이 무의미해지는 문제와, 다른 모든 "순점수" 지표와의 불일치를 함께 해결한다.

### 18.6 정산 화면: 섹션 순서

`/settlements`의 섹션 순서를 다음과 같이 확정한다: **참가자별 순 잔액 → 정리된 채권-채무 관계(거래 카드) → 변제 → 기부 기록 → 기부 랭킹 → 정산 & 조정 이력**. 기존에는 순 잔액이 거의 맨 아래에 있었고, 기부가 변제보다 먼저 나왔다.

### 18.7 통계 화면 기간 필터에 "오늘" 추가 + 필터 컷오프 버그 수정

기간 프리셋(`RangePreset`)에 `"오늘"`을 추가했다(영업일 기준, §18.1). 이 김에 `filterByDatePreset`과 `computeHotColdPlayers`의 최근 N일 컷오프 계산을, `new Date()`/`Date#setDate()` 기반(서버가 실행되는 타임존, 보통 UTC를 그대로 타서 최대 9시간까지 어긋날 수 있었다 — §13.5가 지적한 것과 같은 종류의 버그)에서 다른 곳과 동일한 **Asia/Seoul 문자열 비교**로 다시 작성했다. 부수 효과로 성향 맵의 "최근 90일" 창도 정확히 영업일 90일이 됐다.

### 18.8 새 탭 "통산기록" (`/records`) 신설 + 명예의 전당

`/stats`에 있던 **분기 티어(§16.7)**, **성향 맵(§16.8)**, **기록실**을 별도 탭 `/records`("통산기록")로 옮기고 이 순서로 배치한다. 기록실은 **"명예의 전당"**으로 이름을 바꾸고 다음 부문을 신설했다(`computeRecords`, `src/lib/stats.ts`):

- **최고 순득점 / 최저 순득점** — 참가자별 통산 순점수(`netPoints`) 리더보드의 위/아래.
- **하루 최다 게임수** — 개인 기록이 아니라 **팀 기록**이다. 특정 영업일에 전체 게임이 가장 많이 열린 날을 보여주며, 항목에 참가자 이름 대신 그 날짜가 표시된다.

기존 부문(최장 연승/연패, 하루 최다 승리, 최다 참석)은 그대로 유지된다. 모든 부문은 top 3 + dense ranking(공동 순위) 방식을 그대로 따른다.

### 18.9 성공 기준

- 06:00 이전 기록은 전날로, 06:00 이후 기록은 당일로 분류되며, 자정을 넘겨 이어진 게임 밤 안에서 N차전 순서가 실제 진행 순서와 일치한다(`scripts/verify-business-day.ts`).
- `/games/new`에서 저장하면 `/games`로 이동하고, 방금 기록한 게임이 새로고침 없이 보인다.
- `/settlements`가 참가자별 순 잔액 → 거래 카드 → 변제 → 기부 기록 → 기부 랭킹 → 이력 순으로 렌더링된다.
- `/stats`에 종목별 성적·분기 티어·성향 맵·기록실 섹션이 더 이상 없고, 남은 7개 섹션이 각자 독립된 종목 필터를 갖는다.
- `/records`가 분기 티어 → 성향 맵 → 명예의 전당 순으로 렌더링되고, 명예의 전당에 최고/최저 순득점과 하루 최다 게임수(팀 기록)가 보인다.
- 스키마 변경·환경변수 추가 없음, 기존 기록 마이그레이션 없음.
- `npm run build`, `npm run lint`, `npm run verify:tiers`, `npm run verify:business-day`, `npx tsx scripts/verify-settle.ts` 통과.

## 20. v2.17 요구사항 (영업일 규칙 소급 적용, 구현 완료)

### 20.1 증상과 진단

**증상**: 8/14 저녁부터 8/15 01:00까지 이어진 게임 밤이 화면에서 8/14와 8/15 이틀로 쪼개져 보인다. 자정 이후 기록이 8/14로 묶이지 않는다.

**진단 결과 — v2.16 코드 자체는 정상이다.** 실제 저장소를 확인한 결과:

| 확인 대상 | 상태 |
| --- | --- |
| `time.ts`의 `businessDateFromWallClock` / `nowInSeoulBusinessDay` / `minutesSinceBusinessDayStart` | 정상 |
| `actions.ts`의 `createGame` — `nowInSeoulBusinessDay()` 사용 | 정상 |
| `games.ts`의 `withinDayKey` — 06:00 기준 분 단위 정렬 | 정상 |
| `scripts/verify-business-day.ts` | 정상 (경계·윤년·정렬 모두 검증) |
| **이미 저장된 `games` 행의 `date` 값** | **미적용 — 이것이 원인** |

즉 §7이 "마이그레이션 없이 적용했다"고 명시한 바로 그 결정이 원인이다. v2.16 배포 이전에 자정~06:00 사이에 기록된 게임은 여전히 **달력 날짜**를 들고 있고, 앱은 그 `date` 값을 그대로 신뢰해 화면에 뿌린다. 코드를 아무리 고쳐도 저장된 값이 바뀌지 않으면 증상은 사라지지 않는다.

### 20.2 백필 설계 — `time` 컬럼이 아니라 `created_at`에서 다시 계산한다

가장 먼저 떠오르는 방법("`time < 06:00`인 행의 `date`를 하루 당긴다")은 **쓰면 안 된다.** 멱등하지 않기 때문이다. v2.16 배포 이후에 01:00에 기록된 행은 이미 `date`가 전날로 잡혀 있는데, 이 규칙을 적용하면 하루가 더 당겨져 **이틀 전**이 된다. 스크립트를 두 번 돌리면 또 당겨진다.

대신 **`created_at`에서 매번 다시 계산한다.** `created_at`은 진짜 UTC 순간이고 이 앱의 어떤 기능도 이 값을 고쳐 쓰지 않으므로, 언제 몇 번을 돌려도 같은 답이 나온다.

```
seoul   = seoulWallClockFromIso(row.created_at)      // {date, time} — 진짜 달력 시각
correct = businessDateFromWallClock(seoul.date, seoul.time)
```

**단, 관리자가 손으로 고친 기록은 건드리면 안 된다.** §11의 관리자 수정 기능은 `date`·`time`을 자유롭게 바꿀 수 있고, 그렇게 교정된 값을 `created_at`으로 덮어쓰면 관리자의 수정이 통째로 날아간다. 그래서 다음 가드를 둔다.

| `row.date` 상태 | 판정 | 조치 |
| --- | --- | --- |
| `=== seoul.date` (달력 날짜 그대로) | v2.16 이전에 자동 기록됨, 손댄 적 없음 | **`correct`로 갱신** (같으면 그대로 둠) |
| `=== correct` 이고 `!== seoul.date` | v2.16 이후에 자동 기록됨, 이미 정상 | 건너뜀 (멱등성 확보) |
| 위 어느 쪽도 아님 | 관리자가 손으로 고쳤거나 이상 데이터 | **건드리지 않고 목록으로 보고** |

`time`이 비어 있는 레거시 행(이 필드가 생기기 전 기록)도 `created_at`으로 판정할 수 있다. 다만 이런 행의 `date`를 당기면서 `time`을 비워두면 `withinDayKey`가 그 행을 분 단위 0으로 취급해 **그 영업일의 맨 앞으로 정렬**해버린다 — 실제로는 새벽이라 맨 뒤여야 한다. 따라서 **`date`를 옮기는 행 중 `time`이 비어 있으면 `created_at`에서 뽑은 시각을 함께 채운다.**

`settlements`는 같은 가드가 그대로 적용되지만 잔액 계산에 `date`가 쓰이지 않아 표시상의 문제뿐이므로 **옵션 플래그로 분리**한다. `adjustments`는 관리자가 과거 날짜를 직접 입력하는 테이블이라 위 가드에서 자연히 전부 "건너뜀"으로 걸러지므로 대상에서 제외한다.

### 20.3 재발 방지

- 관리자 수정 화면(`/games`의 인라인 수정 폼)에서 **시간을 06:00 이전으로 두면서 날짜를 그 시각의 달력 날짜로 남겨두면** 영업일 규칙과 어긋난 행이 다시 생긴다. 확인 단계에 경고를 띄우고 "영업일 기준으로 맞추기" 버튼(날짜를 하루 당김)을 제공한다. 관리자가 의도적으로 그렇게 두는 것 자체는 여전히 허용한다 — §11이 보장한 관리자 재량을 좁히지 않는다.
- `scripts/verify-business-day.ts`에 **백필 함수의 멱등성 테스트**(두 번 적용해도 결과가 같은지)와 **가드 테스트**(관리자 수정 흔적이 있는 행을 건너뛰는지)를 추가한다.

### 20.4 성공 기준

- 8/14 저녁 ~ 8/15 05:59에 기록된 과거 게임이 전부 `date = 2026-08-14`가 되고, `/games`·대시보드 최근 경기일 요약·N차전 번호가 하나의 게임 밤으로 보인다.
- 백필 스크립트를 연속 두 번 실행해도 두 번째 실행에서 갱신 건수가 0이다.
- 관리자가 날짜·시각을 직접 고친 기록은 백필 대상에서 제외되고 보고서에 따로 나열된다.
- 스키마 변경·환경변수 추가 없음.
- `npm run build`, `npm run lint`, `npm run verify:business-day`, `npm run verify:tiers`, `npx tsx scripts/verify-settle.ts` 통과.

## 22. v2.18 요구사항 (영업일과 표시 시각의 분리, 구현 완료)

### 22.1 증상

v2.17 백필이 의도대로 동작해서 08/15 01:00에 친 게임이 08/14의 게임 밤으로 잘 묶인다. 그런데 **화면에 그 게임의 시각이 "2026-08-14 01:00"으로 나온다.** 실제로 그 게임을 친 시각은 08/15 01:00이므로, 존재하지 않았던 순간이 표시되고 있는 셈이다.

### 22.2 원인 — 한 값에 두 가지 역할을 시켰다

`games.date`는 v2.16 이후 **영업일**(게임 밤을 묶는 키)이고, `games.time`은 처음부터 **실제 벽시계 시각**이다. 두 필드의 기준이 서로 다른데 화면은 그 둘을 그대로 이어 붙여 출력한다:

```
{g.date} {g.time}  ->  "2026-08-14" + "01:00"  ->  "2026-08-14 01:00"   ← 실제로 없던 시각
```

`date`는 집계·필터·정렬에 쓰이는 **그룹 키**로서는 정확하다. 잘못된 것은 **그 그룹 키를 개별 기록의 타임스탬프로 그대로 재사용한 표시 로직**이다.

### 22.3 해결 — 표시 전용 역함수를 둔다

`businessDateFromWallClock(달력날짜, 시각) → 영업일`의 **정확한 역함수**를 `time.ts`에 추가하고, 개별 기록의 시각을 렌더링하는 모든 곳이 이걸 통과하게 한다.

```
calendarDateFromBusinessDay(영업일, "HH:mm")
  = 시각 >= 06:00 ? 영업일 : 영업일 + 1일
```

**`createdAt`으로 계산하지 않는 이유**: §11의 관리자 수정 기능은 `date`·`time`을 자유롭게 고칠 수 있고, 그렇게 교정된 기록은 `createdAt`(행이 쓰인 순간)과 의도적으로 다르다. 표시 값은 "저장된 게임 시각"을 따라야 하므로, 저장된 두 필드만으로 계산하는 위 순수 함수가 옳다. `createdAt`을 쓰면 관리자가 고쳐둔 시각이 화면에서 무시된다.

**왕복 불변식**: 모든 `(d, t)`에 대해 `businessDateFromWallClock(calendarDateFromBusinessDay(d, t), t) === d`. 검증 스크립트로 확인한다.

### 22.4 영업일을 쓰는 곳 / 벽시계를 쓰는 곳

이 구분이 이번 작업의 전부다. 헷갈리면 **"날(day)을 가리키는 라벨이냐, 기록 하나의 순간이냐"**로 판단한다.

| 화면 요소 | 기준 | 이유 |
| --- | --- | --- |
| `/games` 게임 목록의 각 행 타임스탬프 | **벽시계** | 기록 하나의 순간 |
| 대시보드 "최근 게임" 목록의 각 행 | **벽시계** | 기록 하나의 순간 |
| 관리자 수정 폼의 날짜·시각 입력값 | 영업일 (저장값 그대로) | 저장되는 값을 직접 편집하는 자리 — 다만 실제 벽시계가 무엇인지 보조 문구로 함께 안내 |
| 대시보드 "최근 경기일 요약"의 날짜 헤더 | 영업일 | 게임 밤 하나를 가리키는 라벨 |
| `/records` 명예의 전당 (하루 최다 게임수, 연승 기간 등) | 영업일 | 날 단위 기록 |
| `/games` 연·월·일 필터, `/stats` 기간 필터 | 영업일 | 게임 밤 단위로 묶어 조회 |
| `/settlements` 정산 이력의 날짜 | 영업일 | 시각을 저장하지 않는 날 단위 값 |

### 22.5 그룹과 표시가 어긋나 보일 때의 안내

`/games`에서 필터를 "8/14"로 두면 목록에 "2026-08-15 01:00" 행이 섞여 나온다. 이건 정상 동작이지만 설명 없이는 버그로 보인다. 그 행에 **영업일을 알려주는 작은 표식**(예: `8/14 게임밤` 칩, 또는 시각 앞의 `익일` 라벨)을 달아 두 값이 왜 다른지 한눈에 보이게 한다. 표식은 달력 날짜가 영업일과 다른 행에만 붙인다.

`time`이 없는 레거시 기록은 벽시계를 복원할 수 없으므로 영업일을 그대로 표시하고 표식도 붙이지 않는다.

### 22.6 성공 기준

- 08/15 01:00에 기록된 게임이 `/games`에서 **"2026-08-15 01:00"**으로 표시되고, 동시에 8/14 필터·8/14 경기일 요약·8/14 기준 N차전에 그대로 묶인다.
- 08/14 22:00 게임은 표시도 집계도 08/14로, 표식 없이 나온다.
- 관리자가 §11 수정 기능으로 날짜·시각을 직접 고친 기록은 그 고친 값 기준으로 표시된다(`createdAt`이 아니라).
- 왕복 불변식과 06:00 경계가 `npm run verify:business-day`로 검증된다.
- 스키마 변경·환경변수 추가·데이터 마이그레이션 없음 (표시 로직만 바뀐다).
- `npm run build`, `npm run lint`, `npm run verify:business-day`, `npm run verify:tiers`, `npx tsx scripts/verify-settle.ts` 통과.

## 23. 다음 단계

현재 확정된 백로그 항목은 없다. 다음 릴리스 범위가 정해지면 이 절에 `CLAUDE_CODE_PROMPT_*.md` 형태로 기록한다.
