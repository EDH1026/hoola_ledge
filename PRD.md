# 게임 장부 (hoola_ledge) — PRD

- 작성일: 2026-08-13
- 저장소: https://github.com/EDH1026/hoola_ledge
- 배포: Vercel 프로젝트 `hoola` (도메인: `hoola-three.vercel.app`), 팀 `HYSNESTR`
- 이 문서의 목적: 지금까지 구현된 기능/구조를 정리하여, 이후 성능 개선 작업을 Claude Code에서 이어갈 때 참고할 기준 문서로 사용

## 1. 제품 개요

매번 참석자가 달라지는 소규모 모임 게임(보드게임 등)에서, 각 게임의 등수를 기록하고 그로부터 발생하는 채권-채무 관계를 자동으로 정리해주는 웹 앱이다. 참가자 풀은 고정되어 있지만 게임마다 실제 참석자는 그중 일부로 달라진다. 접근은 팀 전체가 공유하는 비밀번호로 제한된다.

### 핵심 규칙

- 매 게임마다 등수 전체가 아니라 **1등(승자)과 꼴찌(패자)만** 기록한다.
- **꼴찌가 1등에게 1점을 지급**한다. 1점은 소정의 상품으로 교환 가능하다.
- 게임 결과 입력 UI는 꼴찌 참가자 카드를 1등 참가자 카드 위로 **드래그 앤 드롭**하는 방식이며, 드롭 즉시 결과가 기록되고 채권-채무 장부가 갱신된다.
- 새 게임을 등록할 때 참석자 선택의 기본값은 **직전 게임의 참석자와 동일**하며, 필요 시 체크박스로 변경할 수 있다.
- 게임이 쌓이면 참가자 간에 다대다 채권-채무 관계가 형성되는데, 이를 **최소 거래 수**로 자동 간소화한다. (예: A가 B에게 2를 줄 것이 있고 B가 C에게 1을 줄 것이 있으면, A→B 1, A→C 1로 정리)
- 부분/전체 정산(상품 교환) 기록을 남길 수 있고, 정산 완료 여부가 장부에 반영된다.
- 기간별(일/주/월/년) 승/패 횟수와 순위를 여러 각도로 볼 수 있는 통계 화면을 제공한다.

## 2. 데이터 모델

파일: `src/lib/types.ts`

```ts
interface Participant {
  id: string;
  name: string;
  active: boolean;   // 비활성화된 참가자는 새 게임 참석자 목록에서 제외되지만 과거 기록은 유지
  createdAt: string;
}

type GameType = "hoola" | "citadels" | "6nimmt"; // 훌라 / 시타델 / 젝스님트

interface GameResult {
  id: string;
  date: string;           // yyyy-MM-dd, Asia/Seoul 기준, 기록 시점에 서버가 설정 (사용자 입력 불가)
  time?: string;          // "HH:mm", Asia/Seoul 기준. 이 필드 이전 레코드에는 없음
  gameType?: GameType;    // 이 필드 이전 레코드에는 없음
  points?: number;        // Win이 Lose에게서 가져가는 점수. 없으면 1로 간주
  active?: boolean;       // false = 소프트 삭제(잔액/통계/목록에서 제외). 없으면 true로 간주
  attendeeIds: string[];  // 해당 게임에 참석한 사람들
  winnerId: string;       // Win (내부 필드명은 유지)
  loserId: string;        // Lose (내부 필드명은 유지)
  note?: string;
  createdAt: string;      // ISO datetime, 정렬 기준·소프트 삭제와 무관한 롤백 기준으로 사용됨
}

// "waiver"는 예전 값을 계속 파싱하기 위해 남아있을 뿐, 새로 쓰는 값은 항상
// "payment" | "donation" — 읽을 때는 normalizeSettlementType()으로 정규화한다.
type SettlementType = "payment" | "donation" | "waiver";

interface Settlement {
  id: string;
  type?: SettlementType; // 없으면 "payment"로 간주
  fromId: string;  // 주는 사람 (채무자가 갚거나, 누구든 자유롭게 기부)
  toId: string;    // 받는 사람 (채권자이거나, 누구든 자유롭게 기부받음)
  amount: number;
  date: string;
  note?: string;
  createdAt: string;
}

interface LedgerAdjustment {   // 과거 누적기록 — 승패 없이 채무자→채권자, 금액만 존재
  id: string;
  fromId: string;  // 채무자
  toId: string;    // 채권자
  amount: number;
  note?: string;
  date: string;
  createdAt: string;
}

interface DB {
  participants: Participant[];
  games: GameResult[];
  settlements: Settlement[];
  adjustments: LedgerAdjustment[];
}
```

DB 전체가 하나의 JSON 문서로 저장되며, 4개 컬렉션 외의 구조는 없다 (관계형 정규화 없음, 매번 전체를 읽고 쓰는 방식). 위 옵셔널 필드들은 모두 마이그레이션 없이 기존 레코드를 읽을 수 있도록 설계되어 있다 — 없으면 위 주석에 적힌 기본값으로 처리된다.

## 3. 도메인 로직

### 3.1 잔액 계산 및 최소 거래 간소화 (`src/lib/settle.ts`)

- `computeNetBalances(games, settlements)`: 모든 게임과 정산 기록을 순회하여 참가자별 순잔액(net balance)을 계산한다. 승자는 +1, 패자는 -1. 정산 시 `fromId`는 +amount(빚 감소), `toId`는 -amount(받을 돈 감소). 잔액이 정확히 0이 되는 참가자는 결과에서 제거한다.
- `simplifyDebts(balances)`: 잔액을 채무자/채권자로 나눠 각각 금액 내림차순 정렬 후, 가장 큰 채무자와 가장 큰 채권자를 그리디하게 매칭하는 방식(Splitwise류 앱에서 쓰는 표준 알고리즘)으로 거래를 생성한다. N명의 0이 아닌 잔액에 대해 최대 N-1개의 거래로 정리된다.
- 이 로직은 `scripts/verify-settle.ts`로 별도 검증되어 있다 (사용자가 제시한 예시 포함 3개 케이스 모두 통과).

### 3.2 통계 (`src/lib/stats.ts`)

- `computeParticipantStats`: 참가자별 승/패/참석 횟수, 승률, 순포인트(netPoints = wins - losses)를 계산.
- `groupGamesByPeriod`: 일/주/월/년 단위로 게임을 버킷팅하여 기간별 게임 수, 승/패 집계를 생성.
- `filterGamesByPreset`: 최근 7일/30일/90일/올해/전체 범위로 게임을 필터링.
- 통계 화면(`StatsClient.tsx`)에서 이 함수들을 클라이언트 사이드에서 재사용하여 필터를 즉시 반영한다 (서버에서 전체 데이터를 내려주고 클라이언트에서 그룹핑/필터링).

## 4. 화면/기능 명세

| 라우트 | 설명 |
| --- | --- |
| `/login` | 공유 비밀번호 로그인 폼 |
| `/` (대시보드) | 상위 5명 리더보드, 간소화된 정산 미리보기, 최근 게임 목록 |
| `/games/new` | 참석자 선택(기본값 = 직전 게임 참석자) → 드래그 앤 드롭으로 결과 입력 → 날짜/메모 확인 후 기록 |
| `/games` | 전체 게임 기록 목록, 삭제 가능 |
| `/participants` | 참가자 추가/이름 변경/활성-비활성 토글 |
| `/settlements` | 간소화된 거래 목록(부분/전체 정산 기록 폼 포함), 참가자별 순잔액 표, 정산 이력(삭제/취소 가능) |
| `/stats` | 기간/그룹핑 필터, 리더보드 표, 승패 막대 차트, 기간별 게임 수 차트, 참가자별 누적 순포인트 추이 라인 차트 (Recharts) |

인증: 모든 `(app)` 라우트 그룹은 `src/proxy.ts`에서 세션 쿠키(`gl_session`)를 검사하며, 쿠키가 없거나 `SITE_PASSWORD` 기반 SHA-256 해시와 일치하지 않으면 `/login`으로 리다이렉트한다.

## 5. 아키텍처

### 5.1 스택

- Next.js 16.3.0 (App Router, Turbopack), React 19.2.8
- `proxy.ts` (Next 16의 middleware 대체 파일 컨벤션, Node.js 런타임이 기본값)
- React Server Components + Server Actions (`"use server"`), 일부는 JSX 내부의 인라인 서버 액션 사용
- `@dnd-kit/core` — 드래그 앤 드롭 (게임 결과 입력)
- `recharts` — 통계 차트
- `@vercel/blob` — 프로덕션 데이터 저장소
- `date-fns` — 기간 계산
- `uuid`, `zod`

### 5.2 저장소 계층 (`src/lib/storage.ts`)

DB를 "딱히 관계형 DB 없이 파일로" 관리한다는 요구사항에 따라, 로컬 개발과 프로덕션에서 서로 다른 백엔드를 쓰는 어댑터 구조다.

- 로컬 개발: `data/db.json` 파일을 직접 읽고 쓴다. 쓰기는 임시 파일에 쓴 뒤 rename하는 방식으로 원자성을 보장한다.
- 프로덕션(Vercel): 서버리스 파일시스템이 요청마다 초기화되므로 Vercel Blob(Private 접근 모드)을 사용한다. `BLOB_READ_WRITE_TOKEN` 환경변수 존재 여부로 로컬/Blob 모드를 자동 전환한다 (`USE_BLOB = !!process.env.BLOB_READ_WRITE_TOKEN`).
- **모든 읽기/쓰기는 DB 전체 JSON을 대상으로 한다** — 부분 업데이트가 아니라 매번 전체 문서를 읽고, 메모리에서 수정하고, 전체를 다시 쓴다 (`mutateDB`).
- 동시성 제어: 같은 Node 프로세스 안에서 `mutateDB` 호출들을 in-memory promise 체인으로 직렬화하여 lost-update를 방지한다. 단, 이는 단일 프로세스 범위에서만 유효하며, Vercel의 여러 서버리스 인스턴스가 동시에 뜨는 경우까지는 커버하지 않는다 (트래픽이 적은 사적 용도 앱이라는 전제하에 의도적으로 감수한 트레이드오프).

### 5.3 페이지 렌더링

Cache Components가 아닌 기존 라우트-세그먼트 설정 모델을 사용하며, 실시간 DB 데이터를 읽는 모든 `(app)/` 하위 페이지에 `export const dynamic = "force-dynamic"`을 명시해 정적 프리렌더링을 막고 있다. 즉 **모든 페이지가 매 요청마다 서버에서 새로 렌더링**되고, 그때마다 스토리지 계층에서 DB 전체를 읽어온다.

## 6. 배포 환경 및 트러블슈팅 기록

### 6.1 필수 환경변수

| 변수 | 설명 |
| --- | --- |
| `SITE_PASSWORD` | 공유 비밀번호 |
| `BLOB_READ_WRITE_TOKEN` | Vercel Blob 스토어 연결 시 자동 생성 (Storage 탭에서 프로젝트에 Connect, 이때 "Add a read-write token env var" 체크 필요) |

환경변수를 추가/변경한 뒤에는 반드시 **Redeploy**해야 반영된다.

### 6.2 겪었던 문제와 해결

- **Private Blob 스토어 읽기 실패**: 초기 구현은 `access: "public"`으로 `put()`하고 raw `fetch()`로 읽었으나, 사용자가 Private 접근 모드로 스토어를 생성하면서 실패. `@vercel/blob`의 `get(pathname, { access: "private" })` API로 교체하여 해결.
- **사이트 전체 404 (`NOT_FOUND`, Runtime Logs에 요청 자체가 안 찍힘)**: 빌드는 항상 성공했고 로컬에서도 정상 동작했음에도, 배포된 도메인/배포 전용 URL 모두 플랫폼 레벨 404가 발생. 원인은 배포 실패가 반복되며 **Vercel 프로젝트 내부 라우팅 매니페스트가 손상**된 것으로 추정(Vercel 커뮤니티에 동일 사례 존재). **해결책은 새 Vercel 프로젝트를 GitHub 저장소에서 다시 import하는 것**이었다 (기존 프로젝트 삭제 후 재생성이 아니라, 같은 리포를 가리키는 새 프로젝트를 만들고 검증 후 기존 것을 삭제). 재발 시 같은 방법으로 우회 가능.
- **재배포 누락으로 인한 ENOENT 에러**: Blob 스토어를 프로젝트에 연결한 직후에는 `BLOB_READ_WRITE_TOKEN`이 아직 실행 중인 배포에 반영되지 않아, 앱이 로컬 파일 쓰기를 시도하다 `ENOENT: no such file or directory, mkdir '/var/task/data'`로 실패. Redeploy로 해결.

## 7. 알려진 제약사항 / 성능 관련 잠재 이슈 (향후 검토 대상)

성능 개선의 구체적인 우선순위는 아직 정해지지 않았으나, 코드 구조상 다음 지점들이 트래픽/데이터가 늘어날 경우 성능·비용에 영향을 줄 수 있어 참고용으로 정리한다.

- **매 요청마다 전체 DB JSON을 Blob에서 통째로 읽고, 쓸 때도 전체를 다시 씀.** 게임/참가자/정산 기록이 많아질수록 요청당 지연과 Blob 트래픽 비용이 선형적으로 증가한다. 부분 갱신이나 캐싱, 혹은 실제 DB로의 전환이 향후 고려 대상.
- **모든 페이지가 `force-dynamic`이라 캐싱이 전혀 없다.** 대시보드/통계처럼 자주 안 바뀌는 화면도 매번 새로 렌더링 + DB 재조회한다.
- **통계 계산이 매 요청마다 서버에서 전체 게임 목록을 순회**하며 이루어지고, 클라이언트에서 필터를 바꿀 때도 전체 데이터셋을 다시 그룹핑한다 (데이터양이 커지면 클라이언트 번들에 실리는 초기 payload도 커짐).
- **동시성 제어가 단일 프로세스 in-memory 큐에만 의존**하므로, Vercel이 여러 인스턴스를 띄우는 트래픽 상황에서는 이론상 lost-update가 발생할 수 있다 (현재는 저트래픽 사적 용도라 문제되지 않음).
- **드래그 앤 드롭 컴포넌트(`NewGameForm.tsx`)는 SSR 하이드레이션 불일치 회피를 위해 마운트 후에만 DndContext를 렌더링**한다 — 초기 로드 시 약간의 레이아웃 시프트가 있을 수 있음.

## 8. 다음 릴리스 요구사항 (구현 완료)

2026-08-13 확정된 스펙(같은 날짜에 세 차례에 걸쳐 정리·수정됨). 이 섹션에 정리된 8.1~8.9 요구사항은 모두 Claude Code 세션에서 구현이 완료되었다.

### 8.1 관리자 모드

- 목적: 참가자 풀 관리, 과거 누적기록 입력, 데이터 롤백(8.8)처럼 일반 참여자에게 노출할 필요가 없고 실수 시 장부 정합성에 영향이 큰 기능들을 분리한다.
- 기존 `SITE_PASSWORD`(전체 접근용) 위에 **`ADMIN_PASSWORD`** 환경변수를 새로 추가하고, 별도의 관리자 세션(쿠키)을 발급한다.
- 관리자 모드에서만 접근 가능한 화면:
  - **참가자 풀 관리** (`/participants`) — 기존에는 로그인만 하면 누구나 접근 가능했으나, 관리자 전용으로 전환한다.
  - **과거 누적기록 입력** (신규 화면) — 8.2 참고.
  - **데이터 롤백** (신규 화면) — 8.8 참고.
- 관리자 인증이 안 된 상태에서 위 화면에 접근하면 관리자 비밀번호 입력 화면으로 유도한다.

### 8.2 과거 누적기록 입력 (Legacy Ledger Adjustment)

- 이 앱 도입 이전부터 쌓여 있던 채권-채무 관계를 게임 기록 없이 반영하기 위한 관리자 전용 입력 기능.
- 승패 개념이 없고 **채무자 → 채권자, 금액**만 존재한다 (예: "B가 A에게 3만큼 빚짐").
- 잔액 계산에는 게임/정산과 동일하게 반영되어야 하지만, 통계(승/패/참석 횟수)에는 영향을 주지 않는다.
- 관리자가 입력/수정/삭제 가능. 다른 기록(게임, 정산)과 시각적으로 구분되어 표시되어야 한다.

### 8.3 게임 기록 항목 확장

- **게임 종목**: `훌라` / `시타델` / `젝스님트` 중 하나를 선택하는 필수 항목.
- **날짜·시간은 사용자가 입력하지 않는다.** 게임을 기록하는 시점의 실제 날짜·시각이 서버에서 그대로 저장된다 (수정 불가 — 기존에 있던 날짜 선택 UI는 제거).
- **점수(포인트)**: 게임 하나가 몇 점짜리였는지 기록한다. 기본값은 1점이고, 필요할 때 다른 값(예: 2점)을 선택할 수 있다. Win한 사람이 Lose한 사람으로부터 받는 점수는 이 값을 그대로 사용한다 (기존의 "항상 1점" 로직을 일반화).
- **일별 회차**: 같은 날짜의 게임들을 시간순으로 정렬했을 때 몇 번째 게임인지("N차전")를 표시. 별도 저장 없이 조회 시 계산.
- 메모는 기존과 동일하게 선택 사항 유지.
- 게임 기록/이력 조회 화면에서 **당시 참석자 명단**, **게임 종목**, **Win/Lose**, **점수(몇 점짜리 판이었는지)**를 함께 볼 수 있어야 한다.
- **게임 삭제는 실제 삭제가 아니라 비활성화(soft delete) 처리.** 비활성화된 게임은 잔액 계산·통계·일반 목록에서 제외되지만 데이터 자체는 남아 있는다 (완전 삭제는 8.8의 관리자 롤백을 통해서만 가능).

### 8.4 통계/조회 필터 확장

- 통계(`/stats`) 화면의 기존 기간(7일/30일/90일/올해/전체)·그룹핑(일/주/월/년) 필터에 더해 **게임 종목** 필터를 추가한다.
- **게임 목록(`/games`) 화면에도 년/월/일 단위 기간 필터를 추가**하고, 필터링된 기간 안의 **점수 합계**를 함께 보여준다.
- **참가자별 상대 전적(head-to-head) 조회**: 리더보드나 참가자 목록에서 특정 참가자를 선택하면, 그 사람이 다른 참가자 각각을 상대로 얼마나 땄고(Win으로 가져간 점수) 얼마나 잃었는지(Lose로 내준 점수)를 상대방별로 볼 수 있는 화면/패널을 제공한다. 기간 필터를 적용할 수 있어야 한다. 이 집계는 게임의 Win/Lose 점수 이동만 대상으로 하며(정산·기부는 제외), 같은 게임에 3명 이상이 참석했더라도 실제 점수 이동은 그 게임의 winner↔loser 사이에서만 발생한다는 점을 반영한다.
- **기부(도네이션) 랭킹**: 8.7에서 정의하는 "기부"를 기준으로, 가장 많이 기부한 사람과 가장 많이 받은 사람을 보여주는 통계를 추가한다 (기간 필터 적용 가능하면 더 좋음).

### 8.5 용어 변경: "1등/꼴찌" → "Win/Lose"

- 게임 결과 입력 화면, 확인 문구, 게임 목록, 대시보드, 통계 등 UI 전반에서 "1등"은 "Win"으로, "꼴찌"는 "Lose"로 표기를 변경한다.
- 내부 필드명(`winnerId`/`loserId`)은 유지해도 무방하며 UI 라벨만 바꾸면 된다.

### 8.6 참가자 선택 UI 개선

- 게임 참석자 선택 등 "선택/미선택" 토글이 쓰이는 UI에서, 현재는 검은 배경(선택)과 흰 배경(미선택)의 대비만으로 상태를 구분하고 있어 한눈에 어떤 상태인지 파악하기 어렵다. 체크 아이콘 등 색상 대비 이외의 시각적 신호를 추가하고, 선택 상태의 색상 대비를 명확히 강화한다.

### 8.7 정산 화면 개선

- **표현 방식 변경**: 현재 간소화된 채무-채권 관계를 "OOO가 XXX에게 N점 줄 차례" 같은 문장으로 표시하는데, 이를 이름 + 화살표 + 금액 등으로 구성된 **단순하고 시각적인 카드/뱃지 형태**로 교체한다.
- **"탕감" → "기부"로 개념 확장**: 기존에는 채권자가 채무자의 빚을 면제해주는 "탕감"만 상정했는데, 이를 더 일반적인 "기부"로 바꾼다. 기부는 특정 채권-채무 쌍에 종속되지 않고 **임의의 두 참가자 사이에서 자유롭게** 기록할 수 있어야 한다 (예: 채무자가 채권자에게 원래 갚아야 할 금액보다 더 많이 기부하는 것도 가능).
  - 데이터 모델: `Settlement.type: "payment" | "donation"`.
  - 잔액 반영 방식은 "실제 정산"과 "기부" 모두 동일하다: `fromId`(주는 사람)의 잔액은 `+amount`, `toId`(받는 사람)의 잔액은 `-amount` — 즉 누군가에게 가치를 전달하면 주는 사람의 부담은 줄고 받는 사람이 "이미 받은 몫"만큼 채권이 줄어드는 기존 정산 공식을 그대로 따른다. 다만 기부는 계산된 특정 거래에 묶이지 않고 임의의 금액·상대를 자유롭게 선택해 기록할 수 있다는 점이 다르다.
  - **주의(가정)**: 이 방향은 "채무가 있는 사람도 채권이 있는 사람에게 가산해줄 수 있다"는 요구사항 예시를 근거로 판단한 설계 결정이다. 실제 구현 전 사용자에게 한 번 더 확인하는 것을 권장한다.
- 정산 이력에서 "실제 정산"/"기부" 유형별로 구분 표시 및 필터 조회가 가능해야 한다.

### 8.8 관리자 데이터 롤백

- 관리자가 특정 날짜·시각을 지정하면, **그 시점 이후에 생성된 게임 기록·정산 기록·과거 누적기록(8.2)을 실제로(하드) 삭제**한다 — `createdAt`이 지정 시각 이후인 레코드가 대상이며, 8.3의 개별 게임 삭제(소프트 삭제)와 달리 되돌릴 수 없는 완전 삭제다.
- 참가자 풀 정보(이름, 활성 상태 등)는 시간 기반 이벤트가 아니므로 롤백 대상에서 제외한다 (기본 방침 — 구현 시 합리적으로 판단).
- 실행 전 삭제될 대상(개수·목록)을 미리 보여주고, 명확한 확인 절차를 거친 뒤에만 실행되어야 한다.

### 8.9 전반적 디자인 개선

- 구체적인 방향은 정해지지 않았으나, 전반적인 시각적 완성도(레이아웃, 타이포그래피, 여백, 카드/뱃지 기반 컴포넌트화, 색상 사용 등)를 개선한다.

## 9. 다음 단계

위 요구사항 구현은 이 문서를 기준 자료로 삼아 Claude Code 세션에서 진행한다. Claude Code에 전달할 구체적인 작업 지시는 `CLAUDE_CODE_PROMPT.md`에 정리되어 있다.
