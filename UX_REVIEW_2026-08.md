# Hoola Ledge — UX/UI 개선 제안서

**기준 코드**: v2.18 구현 직후 (2026-08-15 로컬 저장소 실측)
**평가 축**: 시인성 · 심미성 · 편의성
**전제**: 주 사용 기기는 폰(390px), 게임 밤에는 한 번에 5~6판을 연속 기록

---

## 총평

기능·데이터 모델은 이미 이 규모의 앱치고 과하게 잘 짜여 있다. 반면 **표현 계층은 기능이 늘어난 속도를 못 따라왔다.** 화면이 8개로 늘고 지표가 티어·성향맵까지 확장되는 동안 색·타입·간격 규칙은 처음 그대로라, 지금은 "정보는 다 있는데 무엇이 중요한지 화면이 말해주지 않는" 상태다.

세 축의 문제를 한 줄씩 요약하면:

- **시인성** — 화면 텍스트의 상당수가 WCAG AA(4.5:1) 미달이다. 특히 `text-slate-600`(2.36:1)이 **삭제 버튼**과 **정산 취소 버튼**에 쓰이고 있다. 정보 위계도 뒤집혀서, 장부 앱의 핵심 숫자인 **점수가 타임스탬프보다 작고 흐리다.**
- **심미성** — 디자인 토큰이 없다. 같은 카드가 화면마다 `p-3/p-4/p-5`, `rounded-xl/2xl`로 다르고, 흰 알약(`bg-slate-100`)이 주요 버튼·활성 필터·금액 뱃지·재활성화 버튼 **5가지 의미**로 동시에 쓰인다. 그래서 화면에서 가장 눈에 띄는 게 종종 필터 칩이다.
- **편의성** — `/games`는 **첫 게임 행까지 760~970px**(폰 화면 1.1~1.4개 분량)를 스크롤해야 한다. 그리고 가장 많이 쓰는 동작인 "게임 기록"에 **연속 입력 경로가 없다** — 한 판 기록할 때마다 페이지가 이동하고 종목이 훌라로 리셋된다.

---

## 우선순위 요약

| # | 항목 | 축 | 효과 | 비용 |
|---|---|---|---|---|
| **P0-1** | 연속 기록 모드 (기록 후 폼 유지) | 편의성 | 게임 밤 조작 횟수 절반 | 중 |
| **P0-2** | `slate-600` 전면 폐기 + 텍스트 색 하한을 `slate-400`으로 | 시인성 | 앱 전체 | 소 |
| **P0-3** | 행 액션(수정/삭제) 44px 히트 영역 + 삭제 확인 | 편의성 | 오터치 제거 | 소 |
| **P0-4** | `/games` 상단 압축 (달력 접기 / 컴팩트화) | 편의성 | 두 번째로 많이 보는 화면 | 중 |
| **P1-1** | 타입 스케일 4단계 도입 (섹션 제목 18px) | 시인성·심미성 | 앱 전체 | 소 |
| **P1-2** | 흰 알약 토큰 분리 (Primary는 화면당 1개) | 심미성 | 앱 전체 | 소 |
| **P1-3** | 필터 상태 URL 동기화 | 편의성 | /games /stats /records | 중 |
| **P1-4** | 게임 행 정보 위계 재배치 (점수 승격) | 시인성 | /games 대시보드 | 소 |
| **P1-5** | 카드/보더 레시피 통일 + 서피스 2단계로 축소 | 심미성 | 앱 전체 | 소 |
| **P2-1** | Pretendard + `tabular-nums` | 심미성 | 앱 전체 | 소 |
| **P2-2** | `/stats` 종목 필터 6개 → 1개(페이지 레벨) | 편의성 | /stats | 중 |
| **P2-3** | 차트 색상 고정 매핑 + 히트맵 하한 상향 | 시인성 | /stats /records | 중 |
| **P2-4** | 빈 상태에 액션 붙이기 | 편의성 | 앱 전체 | 소 |

---

# A. 시인성

## A-1. 색 대비 — 지금 상태는 "안 보이는 게 정상"

실측 대비율(배경 `slate-900` 기준):

| 토큰 | 대비 | AA(4.5:1) | 현재 쓰이는 곳 |
|---|---|---|---|
| `text-slate-300` | 11.4:1 | ✅ | 비선택 필터 칩 |
| `text-slate-400` | 7.06:1 | ✅ | 폼 라벨, 페이지 설명 |
| `text-slate-500` | **3.74:1** | ❌ | **거의 모든 보조 텍스트** — 필터 라벨, 테이블 헤더, 빈 상태 7곳, 게임 행 타임스탬프, **점수**, 메모, 참석자 목록 |
| `text-slate-600` | **2.36:1** | ❌❌ | **삭제 버튼**, **완전삭제 버튼**, 2시간 만료 안내, 달력 비게임일, 정산 이력 **취소 버튼**, 티어 "− 유지" |

특히 심각한 두 곳:

**(1) 삭제 계열 버튼이 안 보인다.**
```tsx
// GamesListClient.tsx:345
className="text-xs text-slate-600 hover:text-red-400"
```
폰에는 hover가 없다. 즉 사용자가 실제로 보는 것은 **12px, 2.36:1 회색 글자**뿐이고, "이건 위험한 동작"이라는 빨간 신호는 영원히 안 뜬다. `settlements/page.tsx:317`의 정산 취소 버튼도 동일(2.35:1).

**(2) 비활성 행에서는 1.9:1까지 떨어진다.**
비활성 게임 행은 `bg-slate-800`인데 참석자 목록이 `text-slate-600`(1.91:1), 결과 줄에는 `opacity-60`까지 겹친다.

### 제안 — 색 하한 규칙

```
text-primary    slate-100  #f1f5f9   16.3:1   값·이름·강조
text-secondary  slate-300  #cbd5e1   11.4:1   본문, 인터랙티브 텍스트
text-muted      slate-400  #94a3b8    7.06:1  ★ 라벨·헬퍼·테이블 헤더·빈 상태의 새 하한
text-faint      slate-500  #64748b    3.74:1  화살표(→)·구분점(·) 같은 장식 전용
slate-600                              폐기
```

**규칙 한 줄: 읽어야 하는 텍스트는 `slate-400` 이상, 누를 수 있는 텍스트는 `slate-300` 이상.**

빨강도 `red-500`(slate-800 위 3.9:1) → **`red-400`(6.5:1 / 5.3:1)** 로 올리면 두 배경 모두에서 AA를 통과한다. 초록 `emerald-400`(9.3:1)은 그대로 둬도 된다.

## A-2. 타입 스케일이 사실상 3단계다

현재 실측: **24px(h1) → 16px(h2, 굵기만 다름) → 14px(본문) → 12px(나머지 전부)**.

`text-xs`(12px)가 필터 라벨 · 폼 라벨 · 테이블 헤더 · **점수 값** · 메모 · 참석자 목록 · 경고 문구 · 액션 버튼을 전부 담당한다. 위계 4단계를 한 사이즈가 떠맡고 있으니 화면이 "균일한 회색 벽"으로 읽힌다.

또 `<h2 className="font-semibold">`는 크기 지정이 없어 **16px 본문과 굵기로만 구분**된다. 게다가 `/games`는 `h2`를 `text-sm`(14px), `/participants`는 16px로 써서 같은 위계가 화면마다 다르다.

### 제안 — 4단계 스케일

```
display   text-2xl  24px  bold        페이지 h1
section   text-lg   18px  semibold    섹션 h2   ← 신설
body      text-sm   14px  regular     본문·테이블·입력
caption   text-xs   12px  regular     라벨·헬퍼 (색은 slate-400 이상)
```

섹션 제목만 18px로 올려도 대시보드/통계처럼 섹션이 8개씩 쌓인 화면의 스캔 속도가 눈에 띄게 달라진다.

## A-3. 정보 위계가 뒤집힌 곳

**게임 행(`GamesListClient.tsx:371-390`)** — 장부 앱에서 가장 중요한 숫자가 가장 작고 흐리다.

| 요소 | 현재 | 문제 |
|---|---|---|
| 타임스탬프 `2026-08-15 23:30 · 3차전` | 14px slate-500 | 가장 왼쪽·가장 김. 실제로는 참조 정보 |
| 승자/패자 이름 | 14px semibold emerald/red | 적절 |
| **점수 `· 1점`** | **12px slate-500** | ★ 핵심 값인데 가장 약함 |
| 메모 | 12px slate-500 | 점수와 동일 스타일이라 구분 안 됨 |
| 참석자 8명 나열 | 12px slate-500, 2줄 wrap | 행 높이의 1/4을 차지하는데 정보 밀도는 최저 |

**제안된 행 구조** (높이는 오히려 줄어든다):

```
[훌라] [8/14 게임밤]                          3차전 · 23:30
민수  →  지훈                                        2점
참석 5명                                        [⋯]
```
- 점수를 우측 정렬 + `text-base font-semibold`로 승격
- 참석자는 **인원수만** 기본 노출, 이름은 탭하면 펼침(또는 `title` 대신 접기)
- 날짜는 이미 날짜별로 그룹핑돼 있으니 행에서는 **시각만** 표시하고 날짜는 그룹 헤더로 (달력·필터와도 중복)

**정산 잔액(`settlements/page.tsx:163-170`)** — `b.amount > 0 ? emerald : red` 라 **잔액 0원인 사람이 빨간 0으로 표시된다.** 정산 완료가 채무자처럼 보인다. 3분기(양수/0/음수)로 나눠야 한다.

## A-4. 색으로만 인코딩된 정보

- **`/stats` 상대전적 히트맵** — `alpha 0.12 + t*0.53`. 하한 0.12는 셀 배경 대비 **1.07~1.14:1**로 사실상 무색이다. 상한 0.65도 2.7:1. 게다가 색 스케일 범례가 없고 12px slate-500 산문 한 줄로만 설명된다.
  → 하한을 0.25로 올리고, **5단계 이산 스케일 + 작은 범례 칩**으로 바꾸면 읽힌다.
- **`/records` 성향 맵 사분면 배경** — `fillOpacity 0.06`. 안 보인다. 0.12~0.15로 올리고, 사분면 라벨(승부사/불나방/실속파/조공러)을 11px `#dc2626`(3.7:1) 대신 `slate-300` + 배경 칩으로.
- **배치 중 진행바** — 트랙 `slate-700` / 채움 `slate-600` = **1.41:1**. 진행도가 안 보이고 숫자도 없다. 채움을 `emerald-500`으로, 그리고 `3.2 / 9판` 같은 수치를 옆에 붙일 것.
- **티어 델타 "− 유지"** — `slate-600` on `slate-800` = 1.93:1. 대부분 참가자가 이 상태라, 실질적으로 델타 칸이 비어 보인다.

## A-5. 서피스 3단이 구분되지 않는다

`slate-950`(페이지) → `slate-900`(섹션 카드) → `slate-800`(내부 카드). 인접 단계 간 명도 차가 1.4:1 수준이고, 결정적으로 내부 카드가 **`bg-slate-800`에 `border-slate-800`** 을 쓰고 있어 보더가 무효다(`RecordsClient.tsx:303,507`, `SettlementsClient.tsx:230`).

→ 서피스를 **2단(페이지 / 카드)** 으로 줄이고, 내부 그룹은 배경 대신 **구분선 + 여백**으로 처리. 카드 보더는 `slate-700/60`으로 올려 실제로 보이게.

---

# B. 심미성

## B-1. 디자인 토큰이 없다 — 이게 나머지 전부의 원인

`globals.css`에는 `--background` / `--foreground` 딱 두 개뿐이고, 나머지 색·간격·반경은 전부 각 컴포넌트에 하드코딩돼 있다. 그래서 다음이 전부 제각각이다:

| 요소 | 발견된 변형 |
|---|---|
| 카드 패딩 | `p-3`(달력) · `p-4`(게임 카드) · `p-5`(대시보드/통계/정산) |
| 카드 반경 | `rounded-xl` · `rounded-2xl` |
| 섹션 제목 | `text-sm`(games) · 16px 기본(participants/stats/records) |
| 같은 "확인 및 기록" 버튼 | `text-xs px-3 py-1.5`(카드) vs `text-sm px-4 py-2`(폼) |
| 섹션 간격 | `space-y-4` · `space-y-6` · `space-y-8` |

### 제안 — `globals.css`에 시맨틱 토큰 정의

```css
@theme {
  --color-surface:        #0f172a;   /* 카드 */
  --color-surface-raised: #1e293b;   /* 강조 블록(제한적) */
  --color-border:         #33415599; /* slate-700/60 — 실제로 보이는 보더 */
  --color-text:           #f1f5f9;
  --color-text-muted:     #94a3b8;   /* 하한 */
  --color-win:            #34d399;
  --color-lose:           #f87171;
  --color-accent:         #818cf8;   /* indigo-400 — 신설 */
}
```
그리고 `Card` / `SectionTitle` / `Button` / `FilterChip` 4개 컴포넌트만 만들어 전 화면이 이걸 쓰게 하면, 위 표의 변형이 한 번에 사라진다. (현재 공용 컴포넌트는 `badges.tsx` 하나뿐이다.)

## B-2. 흰 알약(`bg-slate-100 text-slate-900`)이 5가지 의미를 겸한다

| 쓰이는 곳 | 의미 |
|---|---|
| `+ 새 게임 기록`, `이 결과로 기록하기` | Primary 액션 |
| `/stats` 기간·종목 필터 선택 상태 | 선택됨 |
| `/records` 종목 탭 선택 상태 | 선택됨 |
| 정산 카드 금액 뱃지 | 데이터 표시 |
| `다시 활성화` 버튼 | 보조 액션 |

결과적으로 `/stats`에서 화면상 가장 강한 요소가 **"최근 30일" 필터 칩**이고, 비활성 참가자 목록에서 가장 강한 요소가 **"다시 활성화"** 다. 시선 유도가 완전히 어긋난다.

### 제안 — 토큰 분리

```
Primary   bg-accent(indigo-500) text-white          ← 화면당 1개 원칙
Selected  bg-slate-700 text-slate-100 ring-1 ring-slate-500
Neutral   bg-slate-800 text-slate-300
Danger    bg-red-500/15 text-red-300 border-red-800/60
Data      bg-slate-700 text-slate-100 (뱃지)
```

부수 효과로 **앱에 처음으로 액센트 색이 생긴다.** 지금은 slate + 의미색(초록/빨강)뿐이라 정체성이 없고, 사실상 "부트스트랩 다크 테이블"처럼 보인다. 이미 훌라 뱃지에 indigo를 쓰고 있으니 indigo를 브랜드 색으로 승격하는 게 자연스럽다.

## B-3. 폰트

현재는 시스템 스택 + `Malgun Gothic` 폴백이다. 윈도우/안드로이드에서 한글이 눈에 띄게 투박하게 렌더된다.

→ **Pretendard Variable** (`next/font/local` 또는 CDN) 하나만 바꿔도 전체 인상이 가장 크게 달라진다. 비용 대비 효과가 이 문서에서 가장 좋은 항목.

추가로 **숫자에 `tabular-nums`**(`font-variant-numeric`)를 적용할 것. 순위표·점수 테이블·TR 값처럼 세로로 정렬되는 숫자가 지금은 자릿수마다 폭이 달라 미세하게 흔들린다. Tailwind에 `tabular-nums` 유틸이 있다.

## B-4. 인터랙션 피드백이 hover에만 있다

`focus-visible:` 스타일이 **앱 전체에서 2곳**(participants의 입력 2개)뿐이고, `active:` 는 0곳이다. 나머지는 전부 `hover:`.

폰에는 hover가 없으므로 **버튼을 눌렀을 때 화면상 아무 일도 일어나지 않는다.** 서버 왕복이 끝나 리렌더될 때까지 무반응이라, 사용자는 자연스럽게 한 번 더 누른다(실제로 참가자 추가/이름저장/비활성화 3개 폼에는 중복 제출 방어가 아예 없다).

→ 공용 `Button`에 `active:scale-[0.97] active:brightness-90` + `focus-visible:ring-2 ring-accent` 를 기본 탑재.

## B-5. 아이콘 체계 부재

전체 앱에 아이콘이 **직접 그린 `CheckIcon` 1개**, 그리고 경고 배너의 리터럴 `⚠` 글자 하나뿐이다. 나머지 액션은 전부 한글 텍스트 링크다. 그래서 행 액션이 "수정 삭제 완전삭제"라는 12px 글자 나열이 되고, 이게 시인성·터치 문제로 직결된다.

→ `lucide-react` 도입 후 행 액션을 아이콘 버튼(연필/휴지통) 또는 `⋯` 오버플로 메뉴로. 텍스트 3개보다 훨씬 좁고 훨씬 잘 보인다.

## B-6. 차트 디테일

- `PALETTE[0] = #e2e8f0` — **본문 텍스트 색과 거의 같다.** 첫 번째 참가자의 선이 UI 텍스트처럼 보인다.
- 색이 **위치 기반 배정**(`PALETTE[i % len]`)이라, 필터를 바꾸면 같은 사람의 선 색이 바뀐다. → 참가자 ID 해시 기반 **고정 매핑**으로.
- 누적 추이 라인차트는 버킷 20개 초과 시 **X축을 통째로 숨긴다**(`hide={len > 20}`). "일별 + 최근 90일"을 고르면 시간축 없는 그래프가 나온다. → `interval="preserveStartEnd"` 로 솎아내기.
- `dot={false}` + 툴팁 전용이라 터치에서 값을 읽을 방법이 없다.
- 차트 높이가 280/320/420px 고정. 성향맵 420px는 폰 화면의 절반이다.

---

# C. 편의성

## C-1. ★ 연속 기록 경로가 없다 (가장 큰 문제)

게임 밤에 훌라 5~6판 + 시타델 1~2판을 친다. 그런데 한 판을 기록하면:

```
router.push("/games") → router.refresh()   // NewGameForm.tsx:236-237
```
폼이 언마운트되면서 **종목이 훌라로, 점수가 1로 리셋**되고, 다음 판을 넣으려면 `/games`에서 `+ 새 게임 기록`을 다시 눌러 **페이지 전환을 한 번 더** 거쳐야 한다.

한 판당 실제 비용: **탭 3~5회 + 페이지 로드 2회.** 6판이면 페이지 로드만 12회다.

### 제안

기록 화면 하단에 버튼 2개:

```
[ 기록하고 계속 입력 ]  ← Primary. 종목·참가자 유지, 승패만 초기화, 상단에 "N판 기록됨" 토스트
[ 기록하고 목록으로 ]   ← Secondary. 현재 동작
```

부가로:
- **종목 기본값을 "직전 게임의 종목"으로** (현재는 `hoola` 하드코딩). 시타델 2연판을 칠 때 매번 다시 고르는 낭비가 사라진다.
- 결과 선택 시 칩에 `Lose 선택됨 · Win을 탭하세요` 부제가 **추가되면서 칩 높이가 커져 그리드가 리플로우된다** → 아직 안 누른 Win 대상이 손가락 밑에서 움직인다. 부제는 칩 밖 고정 위치(섹션 헤더)에 두고 칩 높이는 고정할 것.

## C-2. `/games` 상단이 화면 1.4개를 먹는다

첫 게임 행까지의 세로 비용(390×844 기준):

| 구간 | 높이 |
|---|---|
| 스티키 헤더 + 페이지 헤더 | ~136px |
| 연/월/일 필터 카드 | ~130px |
| **달력** | **~275-310px** |
| 구간 인별 점수 테이블 | ~150-356px |
| **첫 게임 행까지** | **≈ 760 ~ 970px** |

달력은 그 300px를 써서 **"이 날 게임이 있었다" 불리언 한 비트만** 인코딩한다. 판 수도, 종목도, 승자도 안 보여준다. 게다가 비게임일 셀은 `slate-600`(2.36:1)이라 한 달의 85~90%가 안 보이는 회색이다.

### 제안 (셋 중 택1)

1. **접기** — 기본 접힘, `📅 날짜로 찾기` 한 줄. 필터가 이미 3개 드롭다운으로 존재하므로 달력은 보조 수단이다. (가장 저렴)
2. **컴팩트 스트립** — 최근 6주만 가로 스크롤 1줄로. 높이 ~60px.
3. **정보 밀도 올리기** — 셀에 게임 수를 점 개수/농도로 인코딩하고 높이를 44px로 올려 터치 타깃 문제도 같이 해결. (달력을 남길 거면 이게 정답)

추가로: **`/games`에 종목 필터가 없다.** 모든 행에 종목 뱃지를 달아놓고 그걸로 거를 수는 없다.

그리고 **일 드롭다운이 1~31 고정**이라 2월 30일을 선택할 수 있고, **월 드롭다운을 바꿔도 달력 그리드는 이전 달에 남아 있다**(`cursorMonth` 미동기화). 두 컨트롤이 같은 상태를 가리키는데 서로 어긋난다.

## C-3. 터치 타깃 — 44px에 도달하는 요소가 하나도 없다

전 화면 최대가 40px이고, 실제 문제는 이것들이다:

| 크기 | 요소 |
|---|---|
| **16px** | 수정 / 삭제 / 완전삭제 / 하드삭제 확인·취소 / 정산 취소 / 이름 저장 / 이전·다음 게임일 / 로그아웃 |
| 28px | 달력 월 이동 화살표, 참가자 비활성화 |
| 32px | 점수 스테퍼 −/+ |
| 44×28px | 달력 날짜 셀 (세로만 부족, 간격 4px) |

**16px짜리 삭제 버튼에는 확인 절차가 없다.** 반면 완전삭제에는 인라인 확인이 있다. 위험도와 마찰의 순서가 뒤집혀 있다.

정산 쪽은 더 극단적이다 — **기록**할 때는 2단계 확인 + 5점 이상이면 경고 배너까지 뜨는데, **취소(삭제)** 는 2.35:1 회색 12px 글자를 한 번 누르면 끝이다.

### 제안

- 행 액션을 `⋯` 오버플로 버튼(44×44) → 시트에 `수정 / 삭제`. 좁고, 잘 보이고, 오터치가 없다.
- 소프트 삭제에도 확인 1단계. 단, 이미 2시간 취소 배너 패턴이 있으니 **"삭제됨 · 취소" 스낵바**로 처리하면 확인 없이도 안전하고 더 빠르다.
- 점수 스테퍼 −/+ 를 44×44로.

## C-4. 2시간 창의 잔여 시간이 안 보인다

`isWithinEditWindow`는 **렌더 시점에 1회 평가**되고 타이머가 없다. 사용자 입장에서는:

- 지금 몇 분 남았는지 알 수 없다
- 화면을 열어둔 채 2시간이 지나도 버튼이 남아 있다가, 누르면 서버에서 거절된다
- 만료된 행은 `2.36:1`짜리 22자 안내문(`기록 후 2시간이 지나 수정·삭제할 수 없습니다.`)이 들어와서 **오래된 행일수록 더 시끄러워진다**

→ 수정 가능한 행에만 작은 칩 `수정 가능 1시간 23분`, 만료된 행은 안내문 삭제(버튼이 없는 것 자체가 신호). 1분 간격 타이머로 갱신.

## C-5. 필터 상태가 어디에도 저장되지 않는다

| 화면 | 필터 개수 | 저장 |
|---|---|---|
| `/games` | 연·월·일 3개 + 달력 커서 | 없음 (기본값 = 오늘) |
| `/stats` | **9개** (기간 1 + 종목 6 + 추이단위 2) | 없음 |
| `/records` | 3개 (분기 + 종목 2) | 없음 |

전부 `useState`고 URL 동기화가 없다. 게임 하나 수정하러 들어갔다 나오면 필터가 오늘로 리셋되고, 뒤로가기도 무의미하며, 링크 공유도 안 된다.

→ `nuqs` 또는 `useSearchParams` + `router.replace`로 URL 동기화. `/stats?h2h=` 파라미터를 이미 쓰고 있으니 패턴은 이미 있다.

**`/stats`의 종목 필터 6개는 그 자체로 문제다.** 섹션마다 따로 있어서 "훌라만 보기"를 하려면 같은 컨트롤을 6번 눌러야 한다. 페이지 레벨 1개(+필요한 섹션만 개별 오버라이드)로 줄일 것.

## C-6. 빈 상태에 출구가 없다

빈 상태 문구는 **총 15곳** 있고 전부 `text-sm text-slate-500` 한 줄이다. 아이콘도, 액션도, 원인 설명도 없다.

가장 문제인 조합: `/games`의 기본 필터가 **오늘**이라, 게임 밤이 아닌 날 앱을 켜면 화면이 이렇게 된다 —

```
연도 2026  월 8  일 15
[달력 300px]
"이 구간에 집계할 게임이 없습니다."
"조건에 맞는 게임 기록이 없습니다."
```

**빈 상태 2개가 300px 달력 아래에 연달아 쌓인다.**

→ 빈 상태에 액션을 붙일 것: `필터 초기화` / `최근 게임일로 이동` / `+ 새 게임 기록`. 그리고 `/games` 기본 필터를 **"가장 최근 게임일"** 로 바꾸는 것도 검토할 만하다(대시보드의 "최근 게임" 섹션이 이미 그 로직을 쓴다).

## C-7. 기타 실사용 마찰

- **정산 이력 무한 렌더** — `history.map`에 상한이 없고 페이지네이션도 없다. 필터 탭도 클라이언트 상태가 아니라 **서버 왕복 네비게이션**이라 매번 전체 리로드된다.
- **되돌리기 배너가 슬롯 1개** — 연속으로 2건을 기록하면 첫 번째 취소 버튼이 사라지고, 이력 목록의 2.35:1 회색 취소로만 접근 가능하다.
- **"변제"가 이력에서 "실제 정산"으로 표시된다** — 둘 다 `type: "payment"`로 저장되는데 입력 화면에는 별도 폼이 있어 명칭이 어긋난다.
- **정산 폼 확인 단계에서 입력값이 사라진다** — early return으로 폼 전체가 언마운트돼서, 확인 화면에서 오타를 발견해도 뒤로 가야 한다.
- **`/records` 카드가 폰에서 1열** — `sm:grid-cols-2`는 640px부터라 티어 8장 + 명예의전당 8장 = **16장이 세로로 쌓이고**, 그 사이에 420px 차트가 있다. 티어 카드는 390px에서도 2열이 충분히 들어간다.
- **성향맵 이름 라벨 8개가 10px로 겹친다** — 도메인이 고정이라 대부분 중앙에 뭉치는데 충돌 회피가 없다.
- **관리자 모드 칩이 `/participants`에서 2번 뜬다** (헤더 + 페이지 상단).
- **관리자 네비게이션 8탭이 폰에서 2개만 보인다** — 가로 스크롤인데 스크롤 가능하다는 시각 신호(그라데이션 페이드 등)가 없다.

---

# 부록 — 30분 안에 끝나고 체감이 가장 큰 5가지

이 문서 전체를 하지 않더라도, 아래 5개만으로 인상이 크게 달라진다.

1. **Pretendard 적용** (`next/font/local`, 파일 1개 수정)
2. **`slate-600` → `slate-400` 전역 치환**, `red-500` → `red-400` (정규식 치환 수준)
3. **섹션 `h2`에 `text-lg` 부여** (전 화면 일괄)
4. **`/games` 달력 기본 접기** (`useState(false)` + 토글 버튼)
5. **`NewGameForm`에 "기록하고 계속 입력" 버튼 추가**

---

*작성: 2026-08-15 · 기준 커밋: v2.18 구현 직후 로컬 저장소 실측*
